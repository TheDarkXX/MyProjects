const fs = require('fs');
const text = 'การเวนวักถูกต้องหรือไม่และคำเชื่อมต่างๆรวมถึงคำถามต่างๆจะออกมาได้เป็นอย่างไรเทพแค่ไหน ดูจากการเขียนรอบนี้ได้เลยและที่สำคัญเราปรับกันมาหลายรอบแล้วนะฉันเริ่มท้อแล้วเข้าใจไหม โอเคมาดูกันว่าจะขี้กากหรือว่าเทพขั้นสุด';
let result = text.replace(/(?<!ตั้ง)(แต่ว่า|แต่|และ|หรือว่า|หรือ|เพราะว่า|เพราะ|ถ้าเกิด|ถ้าหาก|ถ้า|จึง|เพื่อ|คือ|ดังนั้น)/gu, ' $1');
fs.writeFileSync('electron_test2.txt', result.replace(/[ \t]{2,}/g, ' '));
process.exit(0);
