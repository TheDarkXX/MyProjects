# V2.2.1 Credit Card Automated Sync & Notification

**Schedule:** 08:30 AM every day between 15th - 30th of the month.
**Goal:** Automate credit card statement scraping, parsing via Kie.ai (Gemini 2.5 Flash), and reporting to Discord.

## Implementation Tasks
- [x] 1. **Update API Providers:** 
  - [x] Modify `discord-bot/lib/credit-card-agent.js` to rigidly prioritize and exclusively use `Kie.ai` (gemini-2.5-flash) via `https://api.kie.ai/v1/chat/completions`.
  - [x] Update `discord-bot/scripts/test-doctorbank.js` to use `Kie.ai`.
- [x] 2. **Create Cron Script (`cron-cc-statements.js`):**
  - [x] Query DB `/api/cc/credit-cards` or `/api/summary` to check unique cards saved this month (KBank, SCB, Krungsri, BBL).
  - [x] If count >= 4, log "All 4 statements collected" and exit safely.
  - [x] If count < 4, search latest statements (`newer_than:2d` or `3d`) in the `doctorbank` account.
  - [x] Extract items, log to DB via internal API.
  - [x] Send Discord notification with a summary card payload.
- [x] 3. **Register Cron Job:**
  - [x] Add entry to `cc_crons.json` (or `cc_crons_utf8.json`) with schedule `30 8 15-30 * *`.
- [x] 4. **Testing and Verification:**
  - [x] Run the new cron script manually via `node` to test.
  - [x] Check Discord output for summary messages.
- [x] 5. **Deployment:**
  - [x] Commit and `git push vps`.

