# V3.2.3 Gateway Restabilization Plan

## Current Status (2026-04-08)
- ระบบ Gateway ถูกเปลี่ยนจาก Systemd ปกติ มาใช้ **PM2** (`openclaw-runner.mjs`) เพื่อการันตีการอ่านตัวแปรจากไฟล์ `.env` ได้ถูกต้อง (ลดปัญหา Gateway Crash วนลูป ทุก 10 วินาที จากการที่มันหา `KIE_API_KEY` ไม่เจอ)
- Gateway สามารถเชื่อมต่อ `KIE_API_KEY` และ `TELEGRAM_BOT_TOKEN` จาก `.env` ได้สมบูรณ์ ผ่าน PM2 id: `openclaw` (`pm2 logs openclaw`)
- อย่างไรก็ตาม `cron-ai-monitor` แบบเก่ายังได้ค่า 404/fetch failed เมื่อทดสอบยิงไปที่ `http://127.0.0.1:18790/v1/chat/completions` (API แบบ Native ถูก disable จาก config ปัจจุบัน)

## Task List (To-Do สำหรับกลับมาทำต่อ)
- [x] เช็ค Health Status ของตัว Openclaw ผ่าน Dashboard อีกครั้ง (เพื่อดูว่า GPT-5.1 สามารถทำงานต่อได้หรือยัง หลังจาก Gateway เลิกค้าง)
- [x] หาก AI Monitor ยังพัง (fetch failed 404): ตรวจสอบว่าโมเดล OpenAI ถูก proxy ข้าม Gateway ถูกต้องหรือไม่ หรือจำเป็นต้องผูก API เข้ากับ proxy ตรงๆผ่านช่องทางเก่า
- [x] รันการตรวจสอบ `pm2 logs openclaw` เพื่อ Confirm อีกครั้งเมื่อระบบกลับมาทำงานลื่นไหล ไม่มี Process กินเครื่อง (เนื่องจาก SSH command ก่อนหน้าอาจทำให้มันหน่วงชั่วคราว)

## Deployment Checklist
- [x] Create `openclaw-runner.mjs` and link strictly to `.env`
- [x] Configure PM2 daemon properly (`pm2 restart openclaw`)
- [x] (Pending validation) Confirm `fetch` against HTTP proxy works securely and resolves `fetch failed` error on dashboard.
