# Gmail + Calendar Agent — Task List

## สถานะ: ✅ สำเร็จ — ใช้งานได้จริงแล้ว

---

### Setup — ✅ เสร็จแล้ว
- [x] สร้าง Google Cloud Project + enable Gmail API + Calendar API
- [x] สร้าง OAuth 2.0 Client ID (Desktop app) → ดาวน์โหลด `client_secret.json`
- [x] วาง `client_secret.json` ที่ `config/client_secret.json` บน VPS
- [x] ตั้ง `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` ใน `.env` บน VPS
- [x] ตั้ง `GMAIL_BRIEF_CHANNEL_ID` ใน `.env` บน VPS
- [x] รัน `node scripts/gmail-auth.js add account1@gmail.com` → authorize เรียบร้อย

### Implementation — ✅ เสร็จแล้ว
- [x] สร้าง `scripts/gmail-auth.js` — OAuth CLI
- [x] สร้าง `lib/gmail-agent.js` — core library (Gmail + Calendar + AI + Expense)
- [x] สร้าง `scripts/cron-gmail.js` — cron entry point
- [x] สร้าง `config/expense-template.json` — default template
- [x] อัปเดต `.gitignore` — เพิ่ม gmail-tokens.json, client_secret.json
- [x] อัปเดต `.env.example` — เพิ่ม GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GMAIL_BRIEF_CHANNEL_ID

### Deployment & Testing — ✅ เสร็จแล้ว
- [x] `git push vps` — code อยู่บน VPS
- [x] Crontab registered: `0 0 * * * cd /root/brain-app/discord-bot && node scripts/cron-gmail.js >> /tmp/cron-gmail.log 2>&1`
- [x] ทดสอบแล้ว — ส่งข้อมูลใน Discord เรียบร้อย ✅
- [x] ดู output ใน Discord ถูกต้อง

---

> **Pending**: `expense-template.json` ยังไม่ได้สร้าง/sync ไป `/root/brain-app/discord-bot/config/` บน VPS
> ✅ ทุกอย่างอื่นสมบูรณ์แล้ว — รอแค่ user กำหนด template categories
