---
version: "12.20.7"
type: hotfix
status: complete
outcome: shipped
date: 2026-06-24
brain_task_id: "~"
source: dev-agent
tags: [auth, hotfix, brain-api, discord-bot]
conversation: "90a8a35e-b700-460b-b450-367094fff22e"
conversation_title: "Honey LINE ID Card Memory Fix & Auth Hotfix"
related_plans_same_conversation:
  - "V12.20.6_[hotfix]_honey_line-memory-save-bug.md"
related:
  - routes/auth.js
  - discord-bot/lib/brain.js
  - discord-bot/lib/command-parser.js
  - discord-bot/lib/ai.js
  - discord-bot/index.js
summary: >
  Fixed 401 Unauthorized errors in discord-bot internal API fetches. A previous security update in routes/auth.js removed the localhost bypass, requiring a token, but the internal bots' fetch calls were not updated to send the GATEWAY_AUTH_TOKEN header.
---

# 🔒 Internal API Auth Hotfix (V12.20.7)

## Root Cause
บอสรายงานว่า Pinky บน Discord แจ้งเตือน:
`⚠️ ตอนนี้ Brain Database ยังเชื่อมต่อไม่ได้ค่ะ`
`🔧 Error: Brain API 401: {"error":"Unauthorized","login":"/login.html"}`

**สาเหตุ:** 
ก่อนหน้านี้มีการแก้ไข Security ใน `routes/auth.js` โดยยกเลิกการ Bypass `Host` header สำหรับการเรียก API ภายใน (Localhost) ทำให้คำสั่ง `fetch` จากบอททุกตัวใน VPS จะต้องส่ง `Authorization` header ไปด้วย
แต่พบว่าโค้ด `discord-bot` เก่าที่ถูกเรียกใช้งาน (เช่น `brain.js`, `command-parser.js`, `ai.js` และ interactive buttons ใน `index.js`) ยังคงดึงข้อมูลจาก API โดยไม่มีการส่ง Authentication ทำให้โดนปฏิเสธและขึ้น 401

## Proposed Changes

### [MODIFY] `discord-bot/lib/brain.js`
- เพิ่ม header `Authorization: Bearer <GATEWAY_AUTH_TOKEN>` ลงในฟังก์ชัน `callBrainAPI` ทุก request

### [MODIFY] `discord-bot/lib/command-parser.js`
- เพิ่ม header `Authorization` ลงใน `fetch` ของ `findTaskByName` และ `findBillByName`

### [MODIFY] `discord-bot/lib/ai.js`
- เพิ่ม header `Authorization` ในการเรียก `/api/knowledge/search` เพื่อดึงข้อมูลเข้าสู่บริบท AI

### [MODIFY] `discord-bot/index.js`
- เพิ่ม header `Authorization` ให้กับ Interactive Buttons สำหรับ `pay-bill` และ `pay-subscription`

## Status
Deploy ขี้น VPS เรียบร้อยแล้ว ระบบสามารถเชื่อมต่อฐานข้อมูลได้โดยไม่ติด 401

## 🔗 GBRAIN Backlinks
### enables
- **2026-06-24 16:50** | [V12.22.3_[hotfix]_infra_internal-auth-completion.md](C:\My Claw\Openclaw-VPS\Quick Save\Active\V12.22.3_[hotfix]_infra_internal-auth-completion.md) -- Completes cron and LINE Hermes internal auth.
