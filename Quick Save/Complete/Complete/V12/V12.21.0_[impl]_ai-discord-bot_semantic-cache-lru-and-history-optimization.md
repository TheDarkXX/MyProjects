---
aliases: [ai.js, semantic cache, LRU, token optimization, mcp_codex, discord]
---
# [Core-VPS] AI Discord Bot: Semantic Cache LRU & Token Optimization

## 📌 Context (Compiled Truth)
- Added `_id` field to all model providers (`google`, `openrouter`, `kie`, `byteplus`, `mcp_codex`) inside `ai.js`.
- Introduced a new `mcp_codex` provider fallback chain.
- Refined the Semantic Cache by adding LRU-style eviction (cleaning entries if `CACHE_EMBEDDINGS.size > 100`), preventing unbound memory growth.
- **Major Context Optimization:** Stopped recursively appending the entire brain database context into the conversational history array (`history.push({ role: 'user', content: userMessage })` instead of `fullUserMessage`). 
- The extra context (DB context and Wake-up protocol) is now safely prepended *only* to the system prompt, massively reducing token bloat on subsequent conversation turns.

## 📦 RAW ARTIFACT BACKUP (Iron Rule)

<details>
<summary>Modified `ai.js` excerpts</summary>

```javascript
  const h1 = crypto.createHash('sha256').update(text.toLowerCase()).digest();
  const h2 = crypto.createHash('sha256').update(text + 'variant').digest();
  const h3 = crypto.createHash('sha256').update(text + 'paraphrase').digest();

// LRU Cache Eviction implementation
function setSemanticCache(prompt, response) {
  const key = crypto.createHash('md5').update(prompt).digest('hex');
  const embedding = computePromptHash(prompt);

  // Clean old entries if cache gets too large (LRU-style)
  if (CACHE_EMBEDDINGS.size > 100) {
    const entries = Array.from(CACHE_EMBEDDINGS.entries());
    entries.sort((a, b) => a[1].timestamp - b[1].timestamp);
    entries.slice(0, 20).forEach(([k]) => CACHE_EMBEDDINGS.delete(k));
  }

// MCP Codex chain
  } else if (p._id === 'mcp_codex') {
    // MDC Codex primary → DeepSeek fallback → Kie last resort
    fallbackChain.push(
      { providerId: 'mcp_codex', model: modelOverride ?? 'openai-codex/gpt-5.4' },
      { providerId: 'openrouter', model: 'deepseek/deepseek-v3.2' },
      { providerId: 'kie', model: 'gemini-2.5-flash' }
    );

// Context Prepend Optimization
  let extraContext = `[System Context: Today is ${todayDate} ${currentTime}]\n`;
  if (coreFacts) {
    extraContext += `[Core Identity & Facts (Wake-up Protocol)]\n${coreFacts}\n`;
  }
  if (brainContext) {
    extraContext += `\n[ข้อมูลจาก Brain Database ล่าสุด (ใช้อ้างอิงและเป็นความจำหลักของคุณ)]\n${brainContext}\n`;
  }

  if (systemPrompt) {
    systemPrompt = extraContext + '\n' + systemPrompt;
  } else {
    systemPrompt = extraContext;
  }

  const messages = [
    ...history.slice(-20),
    { role: 'user', content: userMessage }, // ❌ Stopped saving entire DB context into history here
  ];

    history.push({ role: 'user', content: userMessage }); // ❌ STOP SAVING ENTIRE DB CONTEXT INTO HISTORY!
    history.push({ role: 'assistant', content: result.content });
```

</details>

## 🔬 Timeline & Debugging Log
- User modified `c:\My Claw\Openclaw-VPS\discord-bot\lib\ai.js` behind the scenes during a marketing discussion (Pricing Strategy).
- Changes discovered via system metadata tracking. Captured seamlessly into a dedicated save file.

## 🔗 GBRAIN Backlinks
### related_to
- **2026-06-22 15:36** | [V12.22.1_[hotfix]_security_hydra-batch.md](c:\My Claw\Openclaw-VPS\Quick Save\Complete\Core-VPS\V12.22.1_[hotfix]_security_hydra-batch.md) -- Applied security hotfixes (EVO-1426, EVO-6943, EVO-6449)
- **2026-06-15 23:26** | [V12.20.0_[infra]_ag-skills_root-folder-hygiene-and-reviewchat.md](file:///C:/My%20Claw/Openclaw-VPS/Quick%20Save/Complete/Core-VPS/V12.20.0_[infra]_ag-skills_root-folder-hygiene-and-reviewchat.md) -- Precedes this AI infrastructure upgrade


- **2026-06-22 15:20** | [V12.22.1_[hotfix]_security_hydra-batch.md](c:\My Claw\Openclaw-VPS\Quick Save\Active\V12.22.1_[hotfix]_security_hydra-batch.md) -- Security hotfix for vulnerabilities introduced in this deployment
