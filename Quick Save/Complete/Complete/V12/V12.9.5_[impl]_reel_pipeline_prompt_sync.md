---
version: "12.9.5"
type: impl
status: complete
outcome: shipped
date: 2026-05-22
brain_task_id: ~
source: dev-agent
tags: [discord, reel-pipeline, prompt-sync, blueprint]
conversation: "92c6b44b-453b-4406-9635-e1a63477a7f6"
conversation_title: "Sync VPS Reel Pipeline Prompts with Perfected Local blueprint.py"
related_plans_same_conversation: 
  - "V12.9.4_[hotfix]_discord_reel-pipeline-model-fix.md"
related:
  - scripts/off-peak-agents/reel-blueprint-pipeline.js
summary: >
  Synchronized the background Discord Reel Pipeline prompts with the perfected local blueprint.py script to ensure high quality analysis. Added Veo Sequence Mapping, Continuity Coherence Audit, Hook variations, and Thai language fields, and auto-generates the storyboard_prompt text.
---

# 🎬 Sync VPS Reel Pipeline Prompts

## 📌 Context (Compiled Truth)
- **Problem:** The local `blueprint.py` pipeline was updated to include advanced prompt instructions like Director's Eye, Phase 3.5 Veo Sequence Mapping, Phase 3.7 Continuity Coherence Audit, and A/B hook variations. The VPS background pipeline (`reel-blueprint-pipeline.js`) was lacking these advanced features, meaning discord uploads produced lower-quality output.
- **Solution:** 
  1. Replaced the system and user prompts in `reel-blueprint-pipeline.js` with the perfected local python prompts.
  2. Implemented `buildStoryboardPrompt()` logic directly in JS to automatically generate the 1-click storyboard prompt grid.
  3. Updated the DB `UPDATE` query to properly store the `storyboard_prompt` and `hook_variations_json` fields.

---

## 📦 RAW ARTIFACT BACKUP (Iron Rule)

<details>
<summary>implementation_plan.md</summary>

# Sync VPS Reel Pipeline Prompts with Perfected Local blueprint.py

อัปเดต prompt ใน `reel-blueprint-pipeline.js` (VPS) ให้ตรงกับ `blueprint.py` (Local) ที่ perfect แล้ว 100%

## สรุปความแตกต่างที่พบ

ผมเทียบ **blueprint.py** (Local/perfect) กับ **reel-blueprint-pipeline.js** (VPS) ทีละส่วนแล้วครับ:

### ✅ ส่วนที่เหมือนกันแล้ว (ไม่ต้องแก้)
| ส่วน | สถานะ |
|------|--------|
| Character Lock: Holy Man | ✅ เหมือนกัน |
| Visual Rules | ✅ เหมือนกัน |
| Master Instruction & Knowledge Base loading | ✅ เหมือนกัน |
| Grid creation logic (6 cols, 540x960, dynamic fps) | ✅ เหมือนกัน |
| JSON repair fallback | ✅ เหมือนกัน |
| DB insert flow | ✅ เหมือนกัน |

### ❌ ส่วนที่ต่างกัน (ต้องแก้)

| # | ส่วนที่ต่าง | Local (blueprint.py) ✅ | VPS (reel-blueprint-pipeline.js) ❌ | Impact |
|---|-------------|------------------------|--------------------------------------|--------|
| 1 | **MIRACLE FORMULA block** | ❌ ไม่มี (อยู่ใน master_instruction.txt แทน) | ✅ มี hardcode ในโค้ด บรรทัด 83-89 | **ลบออก** — เป็นข้อมูลซ้ำกับ master_instruction.txt ที่โหลดอยู่แล้ว |
| 2 | **DIRECTOR'S EYE** instruction | ✅ มี ("Long take action" vs "Switch/Cut scenes") | ❌ ไม่มี | **เพิ่ม** — ช่วยให้ AI แยก scene ถูกต้องมากขึ้น |
| 3 | **Phase 1: `short_story_th`** | ✅ มี (สรุปภาษาไทย) | ❌ ไม่มี | **เพิ่ม** |
| 4 | **Phase 2: PROMPT STRUCTURE RULE** | ✅ มี (Start with subject, rules AT END) | ❌ ไม่มี (เขียนแค่สั้นๆ) | **เพิ่ม** — ส่งผลให้ prompt structure ดีขึ้นมาก |
| 5 | **Phase 2: Scene 1 Hook cloning** | ✅ มี (CLONE 100% extreme visual) | ❌ ไม่มี | **เพิ่ม** |
| 6 | **Phase 2: `hook_variations`** | ✅ มี (3 A/B variations สำหรับ Scene 1) | ❌ ไม่มี | **เพิ่ม** |
| 7 | **Phase 3: Motion Prompt rules** | ✅ มี (C-XXX prefix + subject first, no "From reference image") | ❌ เขียนแค่สั้นๆ ("Define subject motion") | **เปลี่ยน** — ทำให้ Veo ตีความ prompt ดีขึ้น |
| 8 | **Phase 3.5: VEO SEQUENCE MAPPING** (ใหม่ทั้งหมด) | ✅ มี (Cross-Scene Anchor Method) | ❌ ไม่มีเลย | **เพิ่มทั้ง Phase** |
| 9 | **Phase 3.7: CONTINUITY COHERENCE AUDIT** (ใหม่ทั้งหมด) | ✅ มี (`continuity_verdict` per scene) | ❌ ไม่มีเลย | **เพิ่มทั้ง Phase** |
| 10 | **Phase 4: `reasoning_th`** | ✅ มี (reasoning ภาษาไทย) | ❌ ไม่มี | **เพิ่ม** |
| 11 | **JSON Output Schema** | ✅ มี `veo_sequence`, `continuity_verdict`, `hook_variations` (top-level), `short_story_th`, `reasoning_th` | ❌ ขาดทั้งหมด | **อัปเดต schema** |
| 12 | **Duplicate user prompt block** (บรรทัด 159-179) | ❌ ไม่มี (prompt เขียนครั้งเดียว) | ✅ มี append ซ้ำ (ซ้ำซ้อนกับด้านบน) | **ลบทิ้ง** — ทำให้ AI สับสนเพราะได้คำสั่ง 2 ชุดที่ขัดกัน |
| 13 | **Post-analysis: `build_storyboard_prompt()`** | ✅ มี (สร้าง storyboard prompt อัตโนมัติ) | ❌ ไม่มี | **เพิ่ม** + บันทึกลง DB |
| 14 | **DB write: `storyboard_prompt`, `hook_variations_json`, `ai_model`** | ✅ ส่งไปที่ VPS ผ่าน `/import` | ❌ ไม่ได้ write ใน Discord pipeline | **เพิ่ม** DB columns ใน UPDATE statement |

## Proposed Changes

### Component 1: Pipeline Prompt & Logic

#### [MODIFY] [reel-blueprint-pipeline.js](file:///c:/My%20Claw/Openclaw-VPS/scripts/off-peak-agents/reel-blueprint-pipeline.js)

**A. System Prompt (บรรทัด 54-90):**
- ลบ MIRACLE FORMULA block (บรรทัด 83-89) ออก เพราะซ้ำกับ master_instruction.txt ที่โหลดอยู่แล้ว

**B. User Prompt (บรรทัด 92-179):**
- **แทนที่ทั้งหมด** ด้วย prompt จาก `blueprint.py` (บรรทัด 349-446) ซึ่งรวม:
  - เพิ่ม DIRECTOR'S EYE instruction
  - Phase 1: เพิ่ม `short_story_th`
  - Phase 2: เพิ่ม PROMPT STRUCTURE RULE, Hook cloning, `hook_variations`
  - Phase 3: อัปเดต Motion Prompt rules (C-XXX prefix first, no "From reference image")
  - Phase 3.5: เพิ่ม VEO SEQUENCE MAPPING ทั้ง Phase
  - Phase 3.7: เพิ่ม CONTINUITY COHERENCE AUDIT ทั้ง Phase
  - Phase 4: เพิ่ม `reasoning_th`
  - อัปเดต JSON output schema ให้ครบทุก field
- **ลบ** duplicate user prompt append block (บรรทัด 159-179) ออกทั้งหมด

**C. Post-analysis Logic (หลังบรรทัด 301):**
- เพิ่มฟังก์ชัน `buildStoryboardPrompt(analysis)` — แปลจาก Python `build_storyboard_prompt()` เป็น JS
- เรียกหลัง AI analysis เสร็จ เพื่อสร้าง `storyboard_prompt` อัตโนมัติ

**D. DB UPDATE Statement (บรรทัด 311-346):**
- เพิ่ม `storyboard_prompt`, `hook_variations_json`, `ai_model` ใน UPDATE query
- เขียนค่า `storyboard_prompt` = ผลจาก `buildStoryboardPrompt()`
- เขียนค่า `hook_variations_json` = `JSON.stringify(analysis.hook_variations || [])`
- เขียนค่า `ai_model` = `"google/gemini-2.5-flash"`

## Verification Plan

### Automated Tests
1. SSH เข้า VPS → run test script `node test-pipeline.js` ด้วย URL เดิม
2. ตรวจว่า JSON output มี field ใหม่ครบ: `veo_sequence`, `continuity_verdict`, `hook_variations`, `short_story_th`, `reasoning_th`
3. ตรวจ DB ว่า `storyboard_prompt` ไม่เป็น empty
4. ตรวจ DB ว่า `hook_variations_json` มี 3 variations
5. ตรวจ DB ว่า `ai_model` = `google/gemini-2.5-flash`

</details>

## 🔬 Timeline & Debugging Log
### Update 1 — Per-Scene and Hook Generation (2026-05-22)
- ✅ Implemented automated generation of images for all 6 panels sequentially using Higgsfield CLI directly in `blueprint.py`.
- ✅ Implemented automated generation for Hook Variations (A, B, C).
- ✅ Added support for multiple reference images (`--image <storyboard> --image <holyman>`) to lock character and composition.
- ✅ Injected URLs directly into `generated_image_url` and prompt strings to sync instantly to the VPS UI.
- ✅ Cleaned up test/duplicate entries directly in the VPS SQLite database `brain.db`.

## 🔗 GBRAIN Backlinks
- Conversation: [92c6b44b-453b-4406-9635-e1a63477a7f6](file:///C:/Users/Admin/.gemini/antigravity-ide/brain/92c6b44b-453b-4406-9635-e1a63477a7f6)
- **2026-05-22 07:30** | [V12.9.6 [impl] reel_pipeline_full_loop_run](V12.9.6_[impl]_reel_pipeline_full_loop_run.md) -- Background cloud generation daemon verification for all blueprints.
- **2026-05-22 09:50** | [V12.9.8 [impl] skills: Agentic raw.py Pipeline Refactor](../../The-Viral/V12.9.8_[impl]_skills_agentic-raw-pipeline.md) -- Migration of the /raw pipeline to an Agentic workflow.
- **2026-05-22 10:25** | [V12.9.9 [hotfix] reel: Storyboard-Only Cloud Generation & Hook Prompt Refinements](V12.9.9_[hotfix]_reel_storyboard-only-and-hook-fix.md) -- Restricting image generation to Storyboard only and refining Hook variations.
