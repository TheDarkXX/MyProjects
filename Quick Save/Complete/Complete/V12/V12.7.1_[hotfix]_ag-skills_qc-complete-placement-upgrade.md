---
version: "12.7.1"
type: hotfix
status: complete
outcome: shipped
date: 2026-05-08
brain_task_id: ~
source: dev-agent
tags: [ag-skills, qc, quick-save, file-management, powershell-bug]
conversation: "71707f64-c327-4c3f-8a59-ed15c55a6481"
conversation_title: "QC Skill Upgrade — Complete Placement & PowerShell Fix"
aliases: [qc, quick-clear, complete-placement, powershell-bracket-bug]
related_plans_same_conversation: []
related:
  - docs/skills/qc.md
  - docs/AG_SKILLS_INDEX.md
summary: >
  Upgraded the /qc skill from "Quick Clear" (ghost file cleanup only) to "Quick Clear & Complete Placement"
  which now also: (1) auto-moves files with status:complete from Active/ to Complete/, (2) organizes
  loose .md files in Complete/ root into version subfolders under Complete/Complete/Vxx/, (3) includes
  a CRITICAL WARNING about PowerShell silently failing on filenames containing brackets [], and (4) adds
  a mandatory Post-Recheck Verification step to confirm files actually moved. The PowerShell bracket bug
  was discovered during this session when Move-Item silently skipped 45+ files because [impl]/[hotfix]
  in filenames were parsed as wildcard patterns. Fix: use -LiteralPath or Python shutil.move.
---

# V12.7.1 — /qc Skill Upgrade: Complete Placement & PowerShell Fix

## 📌 Context (Compiled Truth)

### Why This Was Done
User ran `/qc` and discovered that while ghost files and stubs were clean, **6 files in `Active/` had `status: complete` but were never moved to `Complete/`**, and **45+ files in `Complete/` root were never sorted into version subfolders**. This revealed two gaps in the original `/qc` skill:

1. `/qc` only cleaned duplicates and stubs — it never checked for completed-but-unmoved files.
2. There was no step to organize `Complete/` root into `Vxx/` subfolders.

### What Was Decided
Instead of creating a new `/cp` skill, user decided to **merge the placement logic into the existing `/qc` skill** to avoid overlap. The skill was renamed from "Quick Clear" to "Quick Clear & Complete Placement."

### The PowerShell Bracket Bug (Root Cause of Previous Failures)
During execution, PowerShell's `Move-Item -Path` silently failed on all files containing `[` and `]` in their names (e.g., `V12.3.4_[hotfix]_...`). PowerShell interprets brackets as **wildcard character class patterns** (similar to regex `[abc]`), so the filename `[hotfix]` gets parsed as "match any single character h, o, t, f, i, or x" instead of the literal text `[hotfix]`.

**The fix:** Always use `-LiteralPath` instead of `-Path` in PowerShell, or bypass PowerShell entirely with Python's `shutil.move()`.

### Folder Structure Change
User created an additional nesting layer `Complete/Complete/` to house all `Vxx/` subfolders, keeping the `Complete/` root clean with only MOC files and the `Complete/` subfolder visible.

```
Quick Save/
├── Active/           ← Work in progress
├── Complete/
│   ├── Complete/     ← NEW: Houses all version folders
│   │   ├── V7/
│   │   ├── V8/
│   │   ├── V10/
│   │   ├── V11/
│   │   ├── V12/
│   │   └── ...
│   ├── MOC_hydra.md
│   ├── MOC_infra.md
│   ├── MOC_payment.md
│   └── MOC_ui.md
├── _incoming/
├── Rejected/
├── Icebox/
└── DeepFreeze/
```

### Updated /qc Execution Steps (Final)
1. **Scan Active/ for Ghosts** — cross-reference with `Complete/Complete/`
2. **Scan Active/ for Unmoved Complete Files** — grep `status: complete`
3. **Scan Complete/ for Disorganized Files** — move to `Complete/Complete/Vxx/`
4. **Scan _incoming/ for Abandoned Stubs** — archive or delete
5. **User Confirmation & Report** — flag ambiguous files
6. **Post-Recheck Verification (MANDATORY)** — verify source empty, destination has files, fail-safe if counts don't match

### Iron Rule Added
> ⚠️ PowerShell treats brackets `[]` as wildcard patterns. You MUST use `-LiteralPath` or Python `shutil.move()` to move Quick Save files. Using `-Path` will silently fail.

## 📦 RAW ARTIFACT BACKUP (Iron Rule)

No artifacts were created in this conversation (no implementation_plan.md or task.md). All work was done via direct file edits and terminal commands.

### Final State of `docs/skills/qc.md`:

```markdown
# 🧠 Skill: `/qc` (Quick Clear & Complete Placement)

## Objective
To actively scan the `Quick Save/` directory and auto-clean workspace clutter while ensuring proper file organization. Specifically, it handles:
1. Deleting duplicate ghost files in `Active/`.
2. Archiving old resolved stubs in `_incoming/`.
3. Auto-moving completed tasks from `Active/` to `Complete/`.
4. Auto-organizing files in `Complete/` root into their correct version subfolders.

> ⚠️ **CRITICAL WARNING FOR AI AGENTS:**
> All files in `Quick Save/` use the format `Vxx.x.x_[type]_...` (e.g. `[impl]`, `[hotfix]`). **PowerShell treats brackets `[]` as wildcard patterns.** If you use `Move-Item` or `Remove-Item` with the default `-Path` flag, it will silently fail. **You MUST use `-LiteralPath` in PowerShell or use a Python script (`shutil.move`)** to guarantee files are moved correctly.

## Execution Steps

1. **Scan `Active/` for Ghosts:**
   - Execute `list_dir` on `Quick Save/Active/`.
   - Execute `list_dir` on `Quick Save/Complete/Complete/` (including version subfolders).
   - If a file exists in `Active/` and a file with the exact same name exists in `Complete/Complete/`, it is a **Ghost File**.
   - **Action:** Delete the ghost file from `Active/` using terminal commands (`rm` or `Remove-Item`).

2. **Scan `Active/` for Unmoved Complete Files:**
   - Use `grep_search` to find `status: complete` in `Quick Save/Active/`.
   - **Action:** Move these files from `Active/` to the root of `Complete/` (Step 3 will then sort them).

3. **Scan `Complete/` for Disorganized Files:**
   - Execute `list_dir` on the root of `Quick Save/Complete/`.
   - Identify any `.md` files sitting directly in the root (e.g., `V12.x.x_...`).
   - Parse the major version (e.g., `V12`).
   - **Action:** Move the file into its corresponding subfolder inside the extra layer (e.g., `Complete/Complete/V12/`). If the folder doesn't exist, create it.

4. **Scan `_incoming/` for Abandoned Stubs:**
   - Execute `list_dir` on `Quick Save/_incoming/` and its subfolders (`dev-agent/`, `hydra/`, etc.).
   - Identify any `.md` stubs.
   - For each stub, use `grep_search` or cross-reference the `Complete/` directory or its `archive/` folder to see if the stub's ID (e.g., `EVO-xxx`) or exact topic has already been implemented or archived.
   - **Action:** Move the abandoned stub to its respective `archive/` folder or delete it if it's a direct duplicate.

5. **User Confirmation & Report:**
   - Compile a concise list of files that were deleted, archived, or moved.
   - Present the cleanup report to the user.
   - Flag any ambiguous duplicate files for manual review.

6. **Post-Recheck Verification (MANDATORY):**
   - After ALL move operations are complete, run `list_dir` on **both** the source and destination directories.
   - **Source check:** Confirm moved files are **gone** from `Active/` and `Complete/` root. If any file you attempted to move still exists at its source → the move silently failed. Re-run with `-LiteralPath` or Python.
   - **Destination check:** Confirm moved files **exist** in their target `Complete/Complete/Vxx/` folder. Count must match the number of files you attempted to move.
   - **Fail-safe:** If source count didn't decrease or destination count didn't increase → **STOP and report failure** to the user. Do NOT claim success.
```

### Final State of `/qc` entry in `docs/AG_SKILLS_INDEX.md`:

```markdown
| `/qc` | `docs/skills/qc.md` | Quick Clear & Complete Placement: Scans Quick Save for ghost duplicates, archives stubs, auto-moves completed tasks, and organizes `Complete/` root into version folders. |
```

## 🔬 Timeline & Debugging Log

### 12:16 — Initial /qc Run
- Scanned Active/, Complete/, _incoming/
- Found 0 ghost files, 0 abandoned stubs
- Found 6 files with `status: complete` stuck in Active/
- Found 1 ambiguous pair: `[study]` vs `[impl]` for fb-reels-download-all

### 12:21 — User Request: Add Complete Placement to /qc
- User asked for `/cp` skill but agreed to merge into `/qc` instead
- Rewrote `docs/skills/qc.md` with new steps 2 (unmoved complete) and 3 (disorganized files)
- Updated `AG_SKILLS_INDEX.md` description

### 12:22 — First Move Attempt (PowerShell — FAILED SILENTLY)
- Used PowerShell `Move-Item` to move 6 files from Active/ → Complete/
- **Result: 0 files moved** — PowerShell silently skipped all files because `[hotfix]`, `[impl]` etc. were parsed as wildcard patterns
- Organized 45 files from Complete/ root into Vxx/ subfolders — **also silently failed** for the same reason

### 12:34 — User Creates Complete/Complete/ Layer
- User created `Complete/Complete/` to house Vxx/ folders
- Updated qc.md references to use new path
- Moved 13 V-folders into Complete/Complete/ (folders don't have brackets, so PowerShell worked)

### 12:37 — User Reports: Files Still Not Moved!
- Complete/ root still had 45+ .md files sitting loose
- Root cause confirmed: PowerShell bracket wildcard bug
- Fixed by switching to Python `shutil.move()` — all 45 files moved + 6 from Active/

### 12:47 — User Asks: What Was the Root Cause?
- Explained PowerShell `[]` wildcard parsing
- Added CRITICAL WARNING to qc.md

### 12:59 — User Suggests Post-Recheck Rule
- Added Step 6: Post-Recheck Verification (MANDATORY) to qc.md
- Source check + Destination check + Fail-safe

## 🔗 GBRAIN Backlinks

### related_to
- **2026-05-08 13:00** | [V12.7.0 Universal Search Manifest](Quick%20Save/Complete/Complete/V12/V12.7.0_[impl]_search_universal-manifest-v3.md) -- Previous version, search-manifest indexer also runs during /qc
- **2026-05-08 13:00** | [V12.0.0 Smart Versioning v2](Quick%20Save/Complete/Complete/V12/V12.0.0_[impl]_ag-skills_smart-versioning-v2.md) -- Versioning rules that /qc enforces
- **2026-05-08 13:00** | [V12.1.0 VdoBatch & SmartDL Skills](Quick%20Save/Complete/Complete/V12/V12.1.0_[impl]_ag-skills_vdobatch-smartdl.md) -- Sister AG skills in the same skill index
