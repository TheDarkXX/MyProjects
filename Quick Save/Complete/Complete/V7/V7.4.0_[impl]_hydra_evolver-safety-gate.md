---
version: "7.4.0"
type: impl
status: complete
outcome: shipped
date: 2026-04-20
brain_task_id: EVO-20260419-145
source: dev-agent
tags: [hydra, evolver, safety-gate, high-risk]
conversation: "86d03a61-7084-429f-a7b0-910dc3332a50"
conversation_title: "Hydra /evo Review - Evolver Safety Gates"
related_plans_same_conversation:
  - V7.4.1_[hotfix]_hydra_block-self-modify-deploy.md
  - V7.3.1_[study]_hydra_evo-validation-review.md
related:
  - scripts/hydra/evolver.js
summary: >
  Lead plan combining EVO-145, EVO-615, and EVO-781 to add pre-deploy validation gates, syntax checks, and post-deploy auto-rollback to the Evolver auto-deploy pipeline.
---
# Evolver Safety Gate: Validation & Auto-Rollback

## Reference Evaluation (From V7.3.1)
**Overall Score:** ⭐ 4.75 / 5
(Merges EVO-145, EVO-615, EVO-781. Overall verdict: ✅ APPROVE & MERGE)
| Dimension     | ⭐ Score | Reasoning |
|---------------|----------|-----------|
| 🚀 Impact     | 5/5      | Auto-rollback is a true safety net — the exact gap that caused the incident |
| ⚙️ Feasibility| 4/5      | `node --check` + git rollback is straightforward; async fire-and-forget is safe |
| ⏱️ Urgency    | 5/5      | Active bleed — without this, any bad patch repeats the 21-error incident |
| 🎯 Alignment  | 5/5      | Core self-healing property of Awakened architecture |

## Combined Proposed Changes

```json
[
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Add CONFIG object with DEPLOY_HEALTH_CHECK_THRESHOLD: 5, DEPLOY_HEALTH_CHECK_TIMEOUT_MS: 30000 to make thresholds configurable"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Add helper function escapeRegExp at top of file: function escapeRegExp(string) { return string.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); }"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Add occurrence counting in validateEdits: after checking fileContent.includes(edit.find), add count = (fileContent.match(new RegExp(escapeRegExp(edit.find), 'g')) || []).length; if (count > 1) return { ok: false, error: `String '${edit.find.substring(0, 30)}' appears ${count} times in ${edit.file} — make find string more specific` }"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "In buildJsonEditPrompt, add instruction: 'find string ต้องจำเพาะพอที่จะปรากฏแค่ครั้งเดียวในไฟล์ ถ้าชื่อฟังก์ชัน/ตัวแปรซ้ำกันให้ใส่ context รอบๆ เช่น บรรทัดก่อนหน้า 2-3 บรรทัด'"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Add validateEditsAndSyntax() function that runs validateEdits() then does syntax check via node --check on each modified JS file before allowing deploy to proceed"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Add post-edit validation: after applyEdits, iterate each modified file, call vm.compile to check syntax, and reject if compilation fails"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Add postDeployHealthCheck() function that runs 'node --check' on all modified files within 30 seconds of deploy, counts stderr errors, and triggers rollback if errors > threshold (e.g., 5)"
  },
  {
    "file": "scripts/hydra/evolver.js",
    "action": "Modify the main deploy flow to: 1) call validateEditsAndSyntax() before git add/commit/push 2) spawn postDeployHealthCheck() as async fire-and-forget after successful push 3) if health check fails, call rollback(snapshotHash) and notify Discord"
  }
]
```

## Changelog
* **2026-04-20**: Implemented Visual Feedback Loop UI (Feedback Memory Tab) inside `hydra-evo.js` and `index.html`. Added API endpoint `/api/hydra/feedback-log` in `routes/hydra.js` to parse `feedback-log.jsonl` into a clean Markdown table and inject it seamlessly into the Awakened Protocol environment.
* **2026-04-20 (Hook Upgrade)**: Rewrote the `vps-auto-markdone.js` deploy hook regex to capture multiple `EVO-xxx` IDs from the Quick Save YAML frontmatter. Upgraded `docs/skills/evo.md` to mandate the use of `merged_task_ids: [...]`, ensuring all fuzed/merged child-tickets deploy and close simultaneously alongside their parent ticket instead of hanging endlessly on the Kanban board.
