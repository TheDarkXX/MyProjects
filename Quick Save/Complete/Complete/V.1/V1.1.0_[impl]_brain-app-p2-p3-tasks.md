# Brain App P2/P3 — Task List

## Phase 1: AI Chat Panel (P2)
- [x] Backend: `/api/ai/chat` endpoint (Gemini 2.0 Flash)
- [x] Frontend: `chat.js` — slide-in panel, markdown render, typing indicator
- [x] HTML: FAB button + chat panel markup
- [x] CSS: panel styles, message bubbles, animations
- [x] Context injection: note title + content แนบไปกับ AI
- [x] Context hint: header แสดง "Note: ชื่อ note" สีม่วง
- [x] Error handling: quota exceeded message ชัดเจน
- [x] Keyboard shortcut: Ctrl+/ toggle panel
- [x] Clear / Close buttons ทำงาน
- [ ] AI response ทำงานจริง (รอ deploy backend script บน VPS)
- [x] AI model dropdown — user เลือก model เอง (free/paid grouped)

## Phase 2: Agent Command Center (P2)
- [x] Backend: `routes/agents.js` — list agents, status, sessions
- [x] Backend: GET `/api/agents` — list all agents + last active
- [x] Backend: GET `/api/agents/:id/sessions` — session history
- [x] Backend: POST `/api/agents/:id/command` — send command
- [x] Frontend: `agents.js` — agent cards, status dots
- [x] Frontend: session browser with filters (date, cost)
- [x] Frontend: command input → send to agent
- [x] HTML: Agents nav item / tab UI
- [x] Register route in `server.js`


## Phase 3: Skill/Config Manager (P3)
### 3A. Skill/Workflow Manager
- [x] Backend: `routes/files.js` — scoped file browser API
- [x] GET `/api/files/skills` — list SKILL.md files
- [x] GET `/api/files/workflows` — list workflow .md files
- [x] GET/PUT `/api/files/:path` — read/save file
- [x] Frontend: file tree + editor UI
- [x] Path traversal prevention

### 3B. Config Manager
- [x] Backend: `routes/config.js` — env vars API
- [x] GET `/api/config` — list vars (masked)
- [x] PUT `/api/config/:key` — update var
- [x] POST `/api/config/restart` — pm2 restart
- [x] Frontend: config panel with masked values
- [x] Password confirmation before save

---
*ทำเสร็จทั้งหมดค่อยลบไฟล์นี้*
