# System Health Page — Animation Plan

## เป้าหมาย
เพิ่ม entrance animation ตอนเปิดหน้า System Health ให้ดูมีชีวิตชีวาเหมือนหน้า OpenClaw  
(ตัวเลขวิ่ง, กราฟขยับจาก 0, card slide-in เป็นลำดับ)

---

## ✅ Implementation Complete (2026-03-24)

### Files Modified
- `brain-app-public/style.css` — เพิ่ม keyframes + animation classes
- `brain-app-public/health.js` — เพิ่ม counter, ring anim, stagger logic, sparkline reveal

### What Was Implemented
- [x] 1.1 `@keyframes hlthFadeSlideUp` — translateY(18px) + opacity 0 → 1
- [x] 1.2 `@keyframes hlthBarGrow` — scaleX(0) → scaleX(1)
- [x] 1.3 `@keyframes hlthCounterPop` — scale(0.7) → scale(1.05) → scale(1) bounce
- [x] 1.4 `.hlth-anim-up`, `.hlth-anim-fade`, `.hlth-anim-pop` classes
- [x] 1.5 `.hlth-stagger-N` delays (0-9) for stagger effects
- [x] 2.1 `hlthAnimateCounter()` — numbers count from 0 → target (easeOutCubic)
- [x] 2.2 Stat Cards counter animation (CPU, RAM%, Disk%, Restarts, Uptime)
- [x] 2.3 `hlthAnimateRing()` — RAM/Disk ring draws from 0 → target
- [x] 3.1 Banner fade-in slide-up
- [x] 3.2 Stat Cards stagger pop (50ms × index)
- [x] 3.3 Cron Job rows stagger slide-in
- [x] 3.4 Cron progress bars grow animation
- [x] 3.5 Service Cards stagger fade-in
- [x] 3.6 Uptime bars grow from 0
- [x] 4.1 `drawSparklineAnimated()` — sparklines draw progressively left to right
- [x] 4.2 Heatmap cells fade-in per row
- [x] 4.3 Cron History grid fade-in per row
- [x] 4.4 Health Log stagger fade-in (max 12 rows)
- [x] Animation only on first load — `healthFirstLoad` flag prevents re-animation on 60s refresh
