---
aliases: [config, doctorbank, vps, network, lazyread]
---
# V0.1.0 [impl] Config DoctorBank Folders

## 📌 Context (Compiled Truth)
The user wanted to read folders from the `DoctorBank-Brand` directory on their iPad using the `LazyRead` app. 
- We updated `config.json` to include 4 specific folders (`Products`, `docs`, `Sleep-Journal`, `Content`) in the `local-vault` roots.
- We confirmed that `LazyRead` listens on `0.0.0.0:3700`, meaning it's accessible from any device on the local network (LAN) via the host PC's IP address (`192.168.1.44`).
- Discussed future deployment to a VPS (Option 2) using `pm2` and mapping `brain.doctorbankonline.com` or a subdomain to the VPS IP for remote access without relying on the local PC.

## 📦 RAW ARTIFACT BACKUP (Iron Rule)
```json
// C:\My Claw\MyProjects\LazyRead\config.json (Updated Section)
    {
      "id": "local-vault",
      "name": "คอมพิวเตอร์",
      "type": "local",
      "roots": [
        { "name": "Products", "path": "C:/My Claw/DoctorBank-Brand/Products" },
        { "name": "docs", "path": "C:/My Claw/DoctorBank-Brand/docs" },
        { "name": "Sleep-Journal", "path": "C:/My Claw/DoctorBank-Brand/Sleep-Journal" },
        { "name": "Content", "path": "C:/My Claw/DoctorBank-Brand/Content" }
      ]
    }
```

## 🔬 Timeline & Debugging Log
- 23:52: User requested how to use `LazyRead` on iPad and how to link `DoctorBank-Brand` folders.
- 23:52: Updated `config.json` with the new folder paths. Checked `server.js` and confirmed it listens on `0.0.0.0`. Found local IP is `192.168.1.44`.
- 23:54: User asked if it needs to be on the same LAN. Explained that it does, unless hosted on a VPS.
- 23:56: User decided to host on a VPS. Provided architecture steps (upload to VPS, update config paths, run with pm2, open port 3700).
- 23:58: User asked if it takes long and if they can use static web hosting instead. Explained Node.js requirement and suggested using the local LAN method for tonight.
- 23:58: User suggested using `brain.doctorbankonline.com`. Confirmed it's a great idea for tomorrow.
- 23:59: User requested `/save and all git push`. Code was committed and pushed to `master`.

## 🔗 GBRAIN Backlinks
(First save for LazyRead, no prior links available in Quick Save/Complete/LazyRead)
