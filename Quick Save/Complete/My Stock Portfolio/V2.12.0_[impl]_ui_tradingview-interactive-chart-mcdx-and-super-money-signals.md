---
title: "TradingView Interactive Chart, Pine Script MCDX & Super Money Signals (V2.12.0)"
date: "2026-09-09"
version: "V2.12.0"
type: "impl"
component: "ui"
aliases: ["tradingview-chart", "banker-mcdx", "super-money-signals", "candle-stretch-drag", "dynamic-scales", "project-2x", "pine-script-mcdx"]
conversation_id: "376d577e-f097-48f6-9731-44a0646ab6c4"
---

# TradingView Interactive Chart, Pine Script MCDX & Super Money Signals (V2.12.0)

## 🧠 Context & Implementation (Compiled Truth)

### 1. ภาพรวมและเหตุผลในการพัฒนา (Core Architectural Purpose)
ในเซสชันนี้ได้ทำการยกระดับระบบกราฟเทคนิคอลและอินดิเคเตอร์เจ้ามือในหน้า **Project 2X (`/#project2x`)** จากเดิมที่เป็นกราฟจำลองเบื้องต้น ให้กลายเป็น **Interactive TradingView-Grade Professional Chart Engine** เต็มรูปแบบที่รันบน SVG + Canvas Client-side โดยไม่ต้องพึ่งพาไลบรารีภายนอกขนาดใหญ่ (Zero External Heavy Charting Bloat) ตอบสนองทุกพฤติกรรมการวิเคราะห์หุ้นของเทรดเดอร์สถาบันและนักลงทุนแนวไฮบริดตามยุทธศาสตร์ **The 3-Year Doubler Rule**

---

### 2. เจาะลึก 10 สถาปัตยกรรมและฟีเจอร์หลัก (Deep Architecture & Implementation)

#### 2.1 Full 5-Year OHLCV Data Lake & Fixing Flat Dash Candles
- **ปัญหาเดิม**: แท่งเทียนในกราฟแสดงเป็นขีดแนวนอนแบน (Flat Dashes) ไร้เนื้อเทียนและไส้เทียน
- **ต้นตอ**: ตาราง `historical_prices` ใน SQLite (`stock.db`) ถูกสร้างขึ้นในยุคแรกที่มีเฉพาะคอลัมน์ `close` (ราคาปิด) ต่อมาเมื่อเพิ่มคอลัมน์ `open, high, low` ข้อมูลเก่าไม่มีค่า OHLC (เป็น `NULL`) ขณะที่ฟังก์ชัน `syncCandleDelta` เดิมตรวจสอบเพียง `count >= 50` จึงข้ามการดึงข้อมูลใหม่
- **การแก้ไข**:
  1. ปรับปรุงเงื่อนไข `syncCandleDelta` ให้ตรวจสอบแถวที่มี `NULL` หรือ `high <= low` เพื่อบังคับ Refresh ข้อมูลจริงจาก Yahoo Finance
  2. รัน Script `scripts/backfill-candles.js` ซิงค์ OHLCV ย้อนหลัง 5 ปี (1,825 วัน) ให้กับหุ้นทุกตัวในพอร์ตและ Watchlist บน VPS (มากกว่า 923 วันทำการต่อตัว)
  3. เพิ่ม Fallback Synthesis ในหน้าบ้าน ป้องกันแท่งแบนในทุกกรณี

#### 2.2 Banker MCDX Pine Script Standard Formula & Sub-pane Architecture
- ปรับสูตรการคำนวณ Banker MCDX ใน `technicalAnalysis.js` ให้ตรงตาม Pine Script ต้นฉบับ:
  ```javascript
  // Banker (Smart Money / Institutional): RSI-50 base scaled to 0-20
  // Formula: EMA(1.5 * (RSI(Close, 50) - 50), 10), clamped to [0, 20]
  // Hot Money (Speculative): RSI-40 base scaled to 0-20
  // Formula: EMA(0.7 * (RSI(Close, 40) - 50), 10), clamped to [0, 20]
  // Retail (Floating): Remaining balance up to 20
  // Formula: max(0, 20 - (Banker + Hot Money))
  ```
- **การแสดงผลแบบ 3-Tier Stacked Histogram (Scale 0 - 20)**:
  - ฐานล่าง (Base): สีแดงสด `#FF3D00` (เงินสถาบัน/เจ้ามือ)
  - ชั้นกลาง (Mid): สีเหลืองทอง `#FFD600` (เงินเก็งกำไร)
  - ชั้นบน (Top): สีเขียว `#2E7D32` (เงินรายย่อยลอยตัว)
- **Ultra-Dense Hairline Gap**: ปรับระยะห่างระหว่างแท่งฮิสโตแกรมให้ชิดกันแบบ Hairline Slit (~0.75px) ไม่เว้นช่องว่างหนาเตอะขัดตา
- **Header Space Isolation**: แยกส่วนหัวชื่ออินดิเคเตอร์ `BANKER MCDX FLOW (SUPER MONEY) • 20 SCALE` สูง 16px (`bankerBarsTopY = bankerTopY + 16`) ป้องกันแท่งกราฟเขียวล้นขึ้นมาทับตัวหนังสือ
- **White MA Curve (SMA 9)** & **Pink Trigger Threshold Line (#FC2D79)** ที่ระดับ 10.0 scale

#### 2.3 Major Bedrock Support EMA 200
- ปรับเส้นค่าเฉลี่ยสถาบัน 3 เส้น:
  - **EMA 50**: เส้นสีขาว `#FFFFFF` (2.2px) สำหรับโมเมนตัมระยะสั้น
  - **EMA 150**: เส้นสีน้ำเงินรอยัลบลู `#2962FF` (2.5px) สำหรับแนวรับแนวโน้มกลาง
  - **EMA 200**: **หนาพิเศษ 3.6px สีทองอำพัน `#FFB300`** เพื่อทำหน้าที่เป็น **"Bedrock Support (ฐานรากหินผาชี้เป็นชี้ตาย)"** พร้อมแถบเส้นประ Upper/Lower Channel

#### 2.4 กลยุทธ์สัญญาณเทรด 3 ระดับบนแท่งเทียน (3-Step Super Money Signals)
เพื่อเปลี่ยนกราฟให้กลายเป็นระบบชี้นำการเทรดอัตโนมัติ ออกแบบระบบตรวจจับแท่งเทียน 3 ขั้นตอน + 1 ขั้นตอนออก:
1. 🟡 **`••• READY` (เตรียมกระสุน / Setup Zone)**:
   - เงื่อนไข: ราคาลงมาจูบแนวรับใหญ่ EMA 150 หรือ EMA 200 ในระยะ +/-2.5% โดยที่ Banker MCDX ยังนิ่งสนิทอยู่ที่ 0 (อยู่ในช่วงสะสมพลังก่อนเจ้ามือเข้า)
   - ป้ายกำกับ: แคปซูลสีเหลืองมินิมอลเหนือแท่งเทียน
2. 🟢 **`▲ BUY` (จุดเข้าซื้อซุ่มยิง / Bedrock Strike)**:
   - เงื่อนไข: ราคายืนเหนือแนวรับ EMA 150 หรือ EMA 200 พร้อมกับการผงกหัวครั้งแรกของแท่ง Banker MCDX > 0 บนแท่งเทียนสีเหลือง (Bullish Candle)
   - ป้ายกำกับ: สามเหลี่ยมสีเขียวสดพร้อมคำว่า `▲ BUY`
3. ⭐ **`⭐ SUPER` (จุดเร่งเครื่อง Super Money / Acceleration Breakout)**:
   - เงื่อนไข: แท่ง Banker พุ่งทะลุเกินระดับ 10.0 scale (ตัดเส้นประสีชมพู) ยืนยันเงินเจ้ามือไหลเข้าทะลักเต็มพิกัด
   - ป้ายกำกับ: ดาวทองสีส้ม `⭐ SUPER`
4. 🔴 **`▼ EXIT` (สัญญาณถอนทัพ / Risk-Off Breakdown)**:
   - เงื่อนไข: ราคาหลุดแนวรับใหญ่ EMA 200 ลึกเกิน -4.0% หรือ Banker MCDX หดตัวเหลือ 0 อย่างกะทันหัน
   - ป้ายกำกับ: สามเหลี่ยมสีแดงเข้ม `▼ EXIT`
- **First-Bar Lock & Anti-Spam**: สัญญาณจะปรากฏเฉพาะ "แท่งแรกที่เกิดเงื่อนไข (Transition Bar)" เท่านั้น ไม่แสดงซ้ำซ้อนให้รกกราฟ
- **Toolbar Toggle**: มีปุ่มสวิตช์ `[ ⚡ Signals ON/OFF ]` บน Toolbar ให้ผู้ใช้เลือกเปิด/ปิดการแสดงผลได้ทันที

#### 2.5 อัตราส่วนความสูงใหม่ (+80px Price Height)
- ขยาย ViewBox Height รวมจากเดิม 650px เป็น **730px** โดยยกความสูงทั้งหมด +80px ให้กับพื้นที่กราฟราคาหลัก (ขยายพื้นที่ราคาเป็น ~557px) ทำให้แท่งเทียนมีพื้นที่แกว่งตัวโปร่งโล่ง สมส่วนระดับ TradingView Desktop
- ปรับ Container CSS จาก `h-[560px]` เป็น `h-[640px]`

#### 2.6 Dynamic Interactive Scale Dragging (ลากแกน X / Y อิสระ)
- **แกน Y (แถบราคาฝั่งขวา - Right Price Scale)**:
  - เมื่อเลื่อนเมาส์ไปที่แถบราคา ขอบเขต `x >= chartW` เคอร์เซอร์จะเปลี่ยนเป็น `ns-resize`
  - คลิกซ้ายค้างแล้วลากขึ้น/ลง เพื่อยืด-หดสเกลความสูงของแท่งเทียน (Vertical Price Zoom Factor: `priceScale` ตั้งแต่ 0.3x ถึง 5.0x)
  - **Double-Click** บนแถบราคาเพื่อรีเซ็ตความสูงกลับมาที่ 1.0x ทันที
- **แกน X (แถบวันที่ด้านล่าง - Bottom Date Scale)**:
  - เมื่อเลื่อนเมาส์ไปที่แถบวันที่ ขอบเขต `y >= priceChartH && y < bankerTopY` เคอร์เซอร์จะเปลี่ยนเป็น `ew-resize`
  - คลิกซ้ายค้างแล้วลากซ้าย/ขวา เพื่อขยาย/บีบความกว้างของแท่งเทียน (Horizontal Candle Bar Width / Span Adjustment)
  - **Double-Click** บนแถบวันที่เพื่อรีเซ็ตช่วงเวลากลับมาที่ Default Timeframe ทันที
- **พื้นที่กราฟหลัก (Chart Canvas Body Drag)**:
  - คลิกซ้ายค้างแล้วลากซ้าย-ขวา เพื่อเลื่อนดูกราฟย้อนหลัง (Pan Drag)
  - ผูก Event Listeners ที่ `window` เพื่อให้การลากเมาส์ต่อเนื่อง นุ่มนวล แม้เมาส์จะหลุดออกนอกขอบ SVG ชั่วคราว

#### 2.7 แถบสเกลราคาและวันที่แบบ Auto-Adjusting หนาแน่น (TradingView Style)
- **แถบราคา (Price Y-Axis)**:
  - ใช้ **"Nice Numbers Algorithm"** คำนวณช่วงห่างราคาที่ลงตัวตามหลักคณิตศาสตร์สถาบัน (ทวีคูณของ 1, 2, 2.5, 5, 10, 20, 50, 100)
  - คำนวณความถี่อัตโนมัติทุกๆ ~42px ส่งผลให้มีเส้นบอกราคาหนาแน่น 10-16 ระดับ พร้อมเส้นบอกระยะย่อย (Tick Notches) และเส้นขอบตารางบางเฉียบ
- **แถบวันที่ (Date X-Axis)**:
  - คำนวณความถี่อัตโนมัติทุกๆ ~48px (แสดง 16-22 Ticks ตลอดแกน)
  - **Year Boundary Visual Accent**: เมื่อถึงแท่งแรกที่เปลี่ยนปี จะแสดงข้อความปีตัวหนาสีขาวสว่าง `#F8FAFC` (เช่น `2023`, `2024`, `2025`, `2026`) ขณะที่เดือนปกติแสดงเป็นสีเทา Slate `#64748B` พร้อมเส้นกริดแนวตั้งสีจาง

#### 2.8 โทนสีแท่งเทียนขาลงใหม่: แดงสดเข้ม 30% (Vivid Red + Dark 30%)
- เปลี่ยนจากสีชมพูสะท้อนแสง `#FF2A6D` เป็นสีแดงเข้มสดที่มีน้ำหนักตามคำสั่งผู้ใช้: **`#C62828` (Deep Crimson Scarlet)** ให้ความรู้สึกเด็ดขาด จริงจัง และมีคอนทราสต์สูงตัดกับแท่งสีเหลือง `#FFE600` ได้อย่างสมบูรณ์แบบ

#### 2.9 กำหนด Timeframe เริ่มต้นเป็น 10 เดือน (Default 10M)
- ปรับ Timeframe เริ่มต้นของกราฟเมื่อเปิดหน้าเว็บเป็น **`10M` (~215 แท่งเทียน)** ซึ่งเป็นช่วงเวลาที่แสดงแนวโน้มใหญ่ได้ชัดเจน เห็นทั้งจุดตัด EMA 50/150/200 และพฤติกรรมเจ้ามือย้อนหลังได้อย่างพอดี
- เพิ่มปุ่ม `10M` บนชุดปุ่มเลือก Timeframe (`7D`, `1M`, `3M`, `6M`, `10M`, `1Y`, `ALL`)
- ปรับปุ่มรีเซ็ตให้แสดงเป็น `[ 🔄 Reset 10M ]`

---

### 3. โค้ดสำคัญที่นำมาใช้งานจริง (Key Implemented Code Snippets)

```typescript
// 1. Nice Numbers Algorithm สำหรับแกนราคา Y-Axis แบบ TradingView
const priceGridLevels = useMemo(() => {
  if (priceRange <= 0) return [];
  const targetTickCount = Math.max(5, Math.min(18, Math.round(priceChartH / 42)));
  const rawStep = priceRange / targetTickCount;
  const power = Math.pow(10, Math.floor(Math.log10(rawStep)));
  const frac = rawStep / power;
  let niceStep = power;
  if (frac > 5) niceStep = 10 * power;
  else if (frac > 2.5) niceStep = 5 * power;
  else if (frac > 1.5) niceStep = 2.5 * power;
  else if (frac > 1) niceStep = 2 * power;

  const firstTick = Math.ceil(minPrice / niceStep) * niceStep;
  const levels: { price: number; y: number; label: string }[] = [];
  for (let p = firstTick; p <= maxPrice; p += niceStep) {
    const y = priceChartH - ((p - minPrice) / priceRange) * priceChartH;
    levels.push({ price: p, y, label: p >= 100 ? p.toFixed(0) : p >= 10 ? p.toFixed(1) : p.toFixed(2) });
  }
  return levels;
}, [minPrice, maxPrice, priceRange, priceChartH]);

// 2. สัญญาณ 3-Step Super Money บนแท่งเทียน
const signals = useMemo(() => {
  const list: { index: number; type: 'READY' | 'BUY' | 'SUPER' | 'EXIT'; text: string; candle: CandleData }[] = [];
  let prevBanker = 0;
  visibleCandles.forEach((cdl, i) => {
    const banker = cdl.banker ?? 0;
    const isBull = cdl.close >= cdl.open;
    const nearBedrock = cdl.ema200 ? Math.abs(cdl.close - cdl.ema200) / cdl.ema200 <= 0.025 : false;

    if (banker >= 10 && prevBanker < 10) {
      list.push({ index: i, type: 'SUPER', text: '⭐ SUPER', candle: cdl });
    } else if (nearBedrock && banker > 0 && prevBanker === 0 && isBull) {
      list.push({ index: i, type: 'BUY', text: '▲ BUY', candle: cdl });
    } else if (nearBedrock && banker === 0 && (!list.length || list[list.length - 1].index < i - 10)) {
      list.push({ index: i, type: 'READY', text: '••• READY', candle: cdl });
    } else if (cdl.ema200 && (cdl.close - cdl.ema200) / cdl.ema200 < -0.04 && prevBanker > 0) {
      list.push({ index: i, type: 'EXIT', text: '▼ EXIT', candle: cdl });
    }
    prevBanker = banker;
  });
  return list;
}, [visibleCandles]);
```

---

## 📂 Files Changed This Session

| File | What Changed | Why |
|------|-------------|-----|
| `src/components/project2x/TradingViewChart.tsx` | อัปเกรดระบบแสดงผล SVG ทั้งหมด: สีแท่งเทียน `#C62828`, ขยายความสูง +80px, ระบบ 3-step signals, ลากแกน X/Y ยืดหดสเกล, Nice Numbers Y-Axis, Dense Year-highlighted X-Axis, Default 10M | ปรับปรุงกราฟให้เทียบเท่า TradingView ตามข้อเรียกร้องของผู้ใช้ทุกประการ |
| `index.html` | อัปเดต Cache Buster Version (`?v=...`) ผ่าน `node bump-cache.js` | ป้องกันเบราว์เซอร์ผู้ใช้จำสคริปต์เก่าบน Production VPS |
| `server/services/technicalAnalysis.js` | ปรับสูตร Banker MCDX ให้ตรงตาม Pine Script (Banker 50/10/1.5, Hot Money 40/10/0.7) | ให้ผลลัพธ์ของเงินสถาบันตรงกับ TradingView Indicator ต้นฉบับ |
| `server/services/project2xEngine.js` | แก้ไข Bug ยกกำลังสองในการคำนวณ Net Worth และเชื่อมต่อ Fundamental Matrix | ให้ตัวเลขพอร์ตตรงกับ Dashboard และข้อมูลตาราง Inflow Slip สมบูรณ์ |
| `scripts/backfill-candles.js` | สคริปต์ดึง OHLCV ย้อนหลัง 5 ปีจาก Yahoo Finance ลง SQLite `stock.db` | แก้ปัญหาแท่งเทียนเดิมเป็นขีดแบนเนื่องจากข้อมูล OHLC ขาดหาย |

---

## 📦 RAW ARTIFACT BACKUP (Iron Rule)

### 1. Implementation Plan (`implementation_plan.md`)

```markdown
# 🚀 Project 2X — Master Implementation Plan (v6 Final — มหาเทพ Review Complete)

## Part 1: ตอบคำถาม & วิเคราะห์เชิงลึกจาก User Feedback

### 1. "เราทยอยดึงมาเติมใน DB เราให้มีถึง 5 ปี 10 ปีเลยได้ไหม?"
คำตอบ: "ทำได้ 100% — Local Financial Data Lake"
- ฐานข้อมูล SQLite stock.db ปรับปรุง schema เก็บ OHLCV
- Backfill 5-10 ปีย้อนหลังผ่าน Staggered Queue
- Delta Sync ดึงเฉพาะแท่งใหม่

### 2. "Auto CAGR คำนวณอย่างไร ถูกต้องแม่นยำหรือยัง?"
- ใช้ Cash Flow Daily Timestamp คำนวณ MWRR / IRR จริง
- Annualization Safety Guard: ถ้าพอร์ต < 6 เดือน แสดงดิบ ไม่ Annualize ป้องกันตัวเลขเพี้ยน

### 3. Net Worth USD แก้ไข Bug ยกกำลังสอง
- totalSecuritiesUsd คิดจาก stockMarketValues ตรงๆ ไม่คูณ shares ซ้ำ

### 4. กราฟ TradingView ปรับปรุงสีและสเกล
- Bull: #FFE600 | Bear: #C62828 (Vivid Red + Dark 30%)
- EMA 50 ขาว | EMA 150 น้ำเงิน | EMA 200 ทองหนาพิเศษ 3.6px
- Banker MCDX 3-Tier Stacked Histogram พร้อมเส้น SMA 9 ขาว และเส้นประชมพู 10
- Timeframe 10M Default
```

### 2. Walkthrough (`walkthrough.md`)

```markdown
# Walkthrough: TradingView Chart Upgrade & Prototype Alignment

อัปเกรดระบบกราฟ TradingView บนหน้า Project 2X (/#project2x) ให้ตรงตามภาพต้นแบบ 100%

1. สาเหตุที่แท่งเทียนเดิมเป็นขีดแบน: คอลัมน์ open, high, low ใน SQLite มีค่า NULL บังคับรัน Full Backfill 5 ปีสำเร็จ
2. การปรับแต่งกราฟให้เหมือนต้นแบบ: แท่งเทียนจริง, EMA Ribbon, Super Money MCDX 3-Tier
3. ระบบซูมและเลื่อนกราฟ: Drag to Pan, Y-axis Stretch Drag, X-axis Stretch Drag, Ctrl+Scroll Zoom
4. สัญญาณ 3-Step Super Money: READY, BUY, SUPER, EXIT
```

---

## ⏱️ Timeline & Debugging Log

- **13:20**: ตรวจสอบปัญหากราฟแท่งเทียนเดิมแบนเป็นขีดแนวนอน พบว่า SQLite ขาดค่า Open, High, Low
- **13:45**: สร้างสคริปต์ `backfill-candles.js` รันดึงข้อมูล OHLCV ย้อนหลัง 5 ปีบน VPS พบว่าข้อมูลแท่งเทียนสมบูรณ์ทันที
- **14:30**: อัปเดตสูตร Banker MCDX ให้ตรงตาม Pine Script โดยใช้ RSI 50 และ RSI 40 ดึงเงินเจ้ามือ เงินเก็งกำไร และเงินรายย่อย
- **15:10**: ผู้ใช้ขอให้ลดช่องว่างระหว่างแท่งฮิสโตแกรม MCDX -> ปรับเป็น Hairline Gap (~0.75px)
- **15:35**: ผู้ใช้ขอให้เพิ่มความหนาเส้น EMA 200 เป็นแนวรับใหญ่ -> ปรับเป็น `3.6px` สร้อยทองหนาแน่น
- **15:50**: ออกแบบและพัฒนา 3-Step Tactical Super Money Signals (`READY`, `BUY`, `SUPER`, `EXIT`) พร้อมปุ่ม Toggle บน Toolbar
- **16:05**: ขยายพื้นที่ความสูงกราฟราคา +80px (vbHeight 650 -> 730) และเพิ่มระบบลากแกน Y (Price Stretch) และแกน X (Date Stretch)
- **16:20**: พัฒนาระบบสเกลราคาและวันที่อัตโนมัติแบบ TradingView (Nice Numbers algorithm ทุก ~42px, เส้นกริดพร้อมตัวเลขปีสีขาวสว่าง)
- **16:35**: ผู้ใช้ขอปรับสีแท่งเทียนขาลงเป็นสีแดงสดเข้ม 30% -> อัปเดตเป็น `#C62828`
- **16:40**: กำหนด Default Timeframe เป็น `10M` (~215 แท่งเทียน)
- **16:42**: แก้ไขข้อผิดพลาด `customRange` missing declaration ผ่าน `npx tsc --noEmit` build ผ่าน 0 error และ Deploy ขึ้น VPS สำเร็จ

---

## 🔗 GBRAIN Backlinks

### depends_on
- **2026-09-09 12:45** | [Project 2X Execution Playbook & 5-Pillar Selling Protocol (V2.11.3)](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.11.3_%5Bdesign%5D_strategy_project-2x-execution-playbook-and-selling-protocol.md) -- แผนยุทธศาสตร์พอร์ตจริงและเกณฑ์ The 3-Year Doubler Rule ที่เป็นฐานของสัญญาณเทคนิคอล
- **2026-09-09 09:30** | [Sniper Inflow Execution & MCDX Banker Protocol (V2.11.2)](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.11.2_%5Bdesign%5D_allocation_sniper-inflow-execution-and-mcdx-banker-protocol.md) -- สูตรตั้งต้นของระบบจับจังหวะซื้อ Sniper Inflow และทฤษฎี MCDX Banker

### enables
- **2026-09-09 16:45** | [TradingView Interactive Chart, Pine Script MCDX & Super Money Signals (V2.12.0)](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.12.0_%5Bimpl%5D_ui_tradingview-interactive-chart-mcdx-and-super-money-signals.md) -- หน้าระบบกราฟและเครื่องมือปฏิบัติการจริงที่พร้อมใช้งานสำหรับ Sniper Inflow Execution

### related_to
- **2026-09-08 18:00** | [SP500 Benchmark Alpha, YoC & TWR Cashflow Engine (V2.11.0)](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.11.0_%5Bimpl%5D_dashboard_sp500-benchmark-alpha-yoc-and-twr-cashflow-engine.md) -- เครื่องมือวิเคราะห์ผลตอบแทนและ Cashflow Engine พื้นฐาน
