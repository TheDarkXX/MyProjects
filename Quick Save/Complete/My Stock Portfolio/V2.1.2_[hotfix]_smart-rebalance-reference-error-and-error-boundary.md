---
id: V2.1.2
title: "Hotfix: Smart Rebalance ReferenceError Crash, Blueprint v2 System, and Smart Selective Buy"
date: 2026-09-04
type: hotfix
status: complete
project: "My Stock Portfolio"
conversation: "5d520e1c-4f81-4820-9a08-65def1748307"
aliases: [rebalance, hotfix, error-boundary, currentVal, black-screen, blueprint, selective-buy, price-gate, drip-simulator, source-of-truth, tiger, backups, dashboard-option-b, 3-views]
---

# V2.1.2 Hotfix: Smart Rebalance ReferenceError Crash, Blueprint v2 System, and Smart Selective Buy

## 🎯 Context & Implementation (Compiled Truth)

### 1. Root Cause Analysis & Runtime Crash Hotfix
The user reported a black screen bug ("หน้า rebalance มีบัคจอดำ") immediately upon navigating to the **Smart Rebalancer** tab (`#rebalance`).

During browser subagent execution with live database state on `https://stock.doctorbankonline.com/#rebalance`, the JavaScript console surfaced:
```
ReferenceError: currentVal is not defined
    at Array.map (<anonymous>) inside useMemo in SmartRebalancePage.tsx
```
In `src/components/rebalance/SmartRebalancePage.tsx`, inside `cashflowRecommendations`:
```typescript
const deficits = sortedHoldings.map(h => {
  const tgtPct = targetWeights[h.symbol] || 0;
  const tgtVal = (tgtPct / 100) * newTotalVal;
  const currVal = h.currentValue || 0; // <-- declared as 'currVal'
  const deficit = Math.max(0, tgtVal - currVal);
  return {
    ...
    currentVal, // <-- object shorthand for currentVal: currentVal (throws ReferenceError!)
    targetVal: tgtVal,
    deficit,
  };
});
```
When `holdings` contained live user securities, this code executed and threw `ReferenceError: currentVal is not defined`, crashing the entire React fiber. Furthermore, because `App.tsx` lacked an `ErrorBoundary`, the uncaught render crash unmounted the page tree, leaving the user with an empty black viewport.

**Comprehensive Remediation:**
1. **Resolved Variable Naming in `SmartRebalancePage.tsx`**:
   - Renamed `const currVal` to `const currentVal = h.currentValue || 0;`.
   - Ensured `Math.max(0, tgtVal - currentVal)` and `{ currentVal }` properly reference the declared constant.
2. **Defensive Nullish & NaN Guards**:
   - `formatMoney(usd)`: Added numeric verification (`typeof usd === 'number' && !isNaN(usd) ? usd : 0`).
   - `rec.currentWeight` and `rec.projectedWeight`: Added safe fallback `(rec.currentWeight ?? 0).toFixed(1)` and bounded progress bars `Math.max(0, Math.min(100, ...))`.
   - Matrix delta & gap values: Protected with nullish coalescing `?? 0`.
   - Trim slider: Fallback `trimHolding.quantity || 1` to ensure `Math.floor` never returns `NaN`.
3. **Global React `ErrorBoundary` in `src/App.tsx`**:
   - Built an institutional-grade `ErrorBoundary` wrapping `<Suspense>` across all route tabs.
   - Any future unforeseen runtime component error will render an elegant recovery card with error description and a 1-click **"รีโหลดหน้าเว็บ (Refresh)"** button instead of a black screen.

---

### 2. Smart Rebalance v2: Portfolio Blueprint & DRIP Simulator
1. **Portfolio Blueprint Architecture**:
   - Dedicated SQLite table `portfolio_blueprints` (`id`, `portfolio_id`, `symbol`, `target_percent`, `target_price`, `status`, `category`).
   - Full REST CRUD API at `/api/blueprints/:portfolioId` with 1-click auto-generation from active holdings.
   - `BlueprintEditor.tsx` with 7 preset templates (Buffett Quality Growth, Peter Lynch GARP, Cathie Wood Hyper Growth, Shay Boloor Alpha, Joseph Carlson Compounders, Core & Satellite, Ray Dalio All-Weather).
   - Real-time market price column, inline Target % editing, and 1-click technical level chips (`EMA150`, `SMA200`, `SMA50`).
2. **Dividend DRIP Simulator Mode**:
   - Pulled `annual_dividend` and `dividend_yield` directly from SQLite `stock_metadata` (synced via Yahoo Finance).
   - Added Mode 4 in `SmartRebalancePage.tsx`: Total Annual Dividend Income hero banner, Yield on Cost (YOC), and automated DRIP (+Shares/yr) estimator based on live prices.
3. **Cross-Asset Trim Simulator**:
   - Replaced `targetHolding` with `targetAsset` from `combinedPortfolio` allowing profit rotation directly into Blueprint Watchlist stocks.

---

### 3. Smart Selective Buy & Option C Remainder Strategy
The previous Cash-Flow Rebalance forced budget allocation across ALL underweight positions in the Blueprint simultaneously. In real-world conditions, only 1–2 stocks might trade near their target prices or at technical support levels (EMA150 / SMA200), while others are overextended.

**Architecture of Smart Selective Buy:**
1. **Interactive Checkbox Selection**:
   - Every Blueprint card includes an interactive checkbox to pick which assets to buy in the current deposit round.
   - Unselected cards enter `⏸️ ข้ามการซื้อรอบนี้` state with an instant `+ เลือกซื้อ` toggle.
   - Quick Filter Bar: `เลือกทั้งหมด`, `🟢 เฉพาะ Strong Buy`, `🟢+🟡 ตัวที่ราคาน่าซื้อ`, and `ล้างที่เลือก`.
2. **Price Gate Valuation Engine**:
   - Compares live market price against user-defined `target_price` (Priority 1) or technical indicators (`EMA150` -> `SMA200` -> `SMA50`) fetched on-demand from Yahoo Finance (Priority 2):
     - `🟢 STRONG BUY`: Live price <= target price or <= technical anchor.
     - `🟡 FAIR`: Live price within 10% above target/anchor, or unpriced asset.
     - `🔴 EXPENSIVE`: Live price > 10% above target/anchor.
   - Displays clear badge with exact distance and levels (e.g. `ต่ำกว่าแนวรับ EMA150 -0.8% (แนวรับ $615.74)`).
3. **Option C Remainder Strategy Toggle**:
   - **🔄 เกลี่ยให้ตัวที่เลือก (Redistribute)**: Concentrates 100% of the deposit amount into selected assets based on recalculated relative deficits.
   - **💰 เก็บเป็นเงินสด (Cash Reserve)**: Allocates only the baseline deficit share to selected assets; unspent budget from unselected stocks is retained as Cash Reserve.
4. **Real-Time KPI Banner & 1-Click Copy**:
   - Summarizes Deploying Budget, Cash Reserve total, and Portfolio Price Signals (`🟢 X`, `🟡 Y`, `🔴 Z`).
   - `คัดลอกแผนเคาะซื้อ` outputs only selected buy orders alongside the Cash Reserve balance.

---

## 📁 Files Changed This Session

| File | What Changed | Why |
|------|-------------|-----|
| `src/components/rebalance/SmartRebalancePage.tsx` | Fixed `currVal` ReferenceError; added Blueprint integration, DRIP simulator mode, Smart Selective Buy checkboxes, Price Gate engine, and Option C toggle | Core page overhaul for stability, Blueprint alignment, and selective capital allocation. |
| `src/App.tsx` | Implemented class-based `ErrorBoundary` wrapping all lazy tabs | Shield user from black screens if any child module encounters an unexpected error. |
| `src/components/rebalance/BlueprintEditor.tsx` | Added inline editing, live market prices, auto-status detection, and 1-click EMA150/SMA200 chips | Deliver streamlined Blueprint authoring experience. |
| `src/components/rebalance/StrategyConfigs.ts` | Created 7 preset allocation templates | Provide instant institutional portfolio benchmarks. |
| `src/stores/blueprintStore.ts` | Created Zustand store for Blueprint CRUD and template application | Manage client-side Blueprint state. |
| `server/db/init.js` & `server/db/migrate.js` | Added `portfolio_blueprints` table and `stock_metadata` dividend columns | Persist Blueprints and dividend stats in SQLite. |
| `server/routes/blueprints.js` | Created CRUD endpoints for `/api/blueprints/:portfolioId` | Backend API for Blueprint data. |
| `server/services/yahoo.js` | Fixed stray syntax brace; upgraded `fetchYahooDividend` for ETFs; added `fetchYahooTechnicals` | Provide accurate dividend rates and technical EMA/SMA anchors. |
| `server/routes/metadata.js` | Implemented on-demand self-healing dividend fetching | Guarantee non-null metadata for portfolio stocks. |
| `server/routes/prices.js` | Added `/technicals/:symbol` endpoint | Expose EMA150, SMA200, SMA50 technical levels to frontend. |
| `src/components/rebalance/BlueprintPieChart.tsx` | [NEW] Created Donut Pie Chart and Health Diagnostic panel | Real-time visual allocation check and quantitative health checks. |
| `src/components/transactions/TransactionFormModal.tsx` | Updated Strategy Role select to 8 standardized Strategy Categories with color icons | Ensure all future transactions align with 8 Strategy Categories. |
| `src/components/transactions/TransactionTable.tsx` | Updated `resolveStockType` and badge styling to match 8 Strategy Categories | Deliver consistent category badge rendering across transaction history. |
| `src/components/analysis/DistributionPieChart.tsx` | Color-coded Strategy mode to match Blueprint Pie Chart colors | Unify color language between Rebalancer and Portfolio Analysis. |
| `docs/STRATEGY_GUIDE.md` | Comprehensive reference manual for 8 Strategy Categories | Authoritative taxonomy documentation with metrics and screening rules. |
| `server/db/stock.db` | Updated `stock_type` on all transactions and initialized `portfolio_blueprints` | Synchronize historical and blueprint data with new categories. |
| `index.html` / `dist/index.html` | Bumped cache version string to `v=20260904_194422` | Invalidate client-side browser caching immediately. |
| `self-improving/corrections.md` | Logged object shorthand reference mismatch and ErrorBoundary architecture rule | Close learning loop. |

---

## 🧪 Build, Deploy & Verification

1. **Production Build**: Ran `npm run build` -> Exited 0 with all 2,854 modules bundled cleanly.
2. **Cache Bump**: Ran `node bump-cache.js` -> Bumped cache version to `v=20260904_144940`.
3. **VPS Deploy**: Executed `scp -r dist/* root@185.250.38.247:/root/stock-portfolio/dist/` and `pm2 reload stock-api`.
4. **Live Visual Verification (`https://stock.doctorbankonline.com/#rebalance`)**:
   - Screen rendered without black screen or errors.
   - Recommended buying order cards displayed with interactive checkboxes and price signal pills.
   - Tested unselecting stocks: Deploying amount recalculated instantly to $574.97.
   - Toggled to `Cash Reserve`: Retained $425.03 in cash reserve.
   - Verified price signals: `🟢 STRONG BUY` on META, `🟡 FAIR` on MELI and CASH.
   - Captured screenshot artifact: `smart_rebalance_ui_1788508262806.png`.

---

### Update 2 — 13:45 (Smart Rebalance v2: Blueprint System, DRIP Simulator & Data/Currency Recheck)
- ✅ **Phase 1: Portfolio Blueprint System**: Added `portfolio_blueprints` table, CRUD routes, `BlueprintEditor.tsx`, and 7 strategy templates.
- ✅ **Phase 2: Dividend DRIP Simulator Mode**: Integrated dividend metadata, hero banner, YOC, and DRIP shares estimator.
- ✅ **Phase 3: Comprehensive Recheck & Currency/Data Audit**: Fixed Yahoo syntax brace, enabled ETF dividends, live price watchlist sync, and 2-decimal precision.

---

### Update 3 — 15:00 (Smart Selective Buy, Price Gate & Option C Remainder Strategy)
- ✅ **Interactive Selection**: Added individual checkboxes and quick filter bar (`เลือกทั้งหมด`, `เฉพาะ Strong Buy`, `ตัวที่ราคาน่าซื้อ`, `ล้างที่เลือก`).
- ✅ **Price Gate Valuation**: Real-time evaluation against Target Price and technical levels (EMA150/SMA200/SMA50).
- ✅ **Option C Remainder Strategy**: Toggle between `Redistribute` (เกลี่ยให้ตัวที่เลือก) and `Cash Reserve` (เก็บเป็นเงินสด).
- ✅ **Live KPI Summary Banner**: Real-time Deploying budget, Cash Reserve, and signal breakdown.
- ✅ **Production Deploy & Visual Check**: Live test on VPS confirmed correct recalculation and zero errors.

---

### Update 4 — 19:40 (Strategy Categories 8-Tier Taxonomy, Blueprint Health Diagnostic Pie Chart, and Docs)
- ✅ **8 Strategy Categories & Color Taxonomy**:
  - `Compounders` (🟣 Purple `#8B5CF6`), `Growth` (🔴 Red `#EF4444`), `Mid-Tier` (⚪ White/Gray `#94A3B8`), `Defensive` (🟢 Green `#10B981`), `Small Cap` (🟡 Yellow `#F59E0B`), `Bets` (🟤 Brown `#B45309`), `Cash` (⚫ Slate `#475569`), `ETF` (🔵 Deep Blue `#1D4ED8`).
- ✅ **Decouple Yahoo Sector Override**: Removed auto-fill logic that was overwriting user's strategic category with Yahoo Sector. Added read-only gray sector badge below symbol name.
- ✅ **Blueprint Health Diagnostic (`BlueprintPieChart.tsx`)**: Recharts Donut PieChart with dynamic hover tooltip, category legend, and 4 health metrics (Category Balance ≥3, Single Stock Concentration Risk >30%, Top 2 Weight >50%, Cash Reserve ratio).
- ✅ **Portfolio Holdings Category Sync**: Updated SQLite `stock.db` (`transactions` table `stock_type` and `portfolio_blueprints`) for all user holdings in `Doctorbank Growth` (NVDA, RBRK, COST, AMZN, MELI, ASTS, RKLB, CRWD, HIMS, CRWV, ISRG) and `Tiger` (SCHG).
- ✅ **Global Strategy Alignment**: Updated `TransactionFormModal.tsx`, `TransactionTable.tsx`, and `DistributionPieChart.tsx` to utilize unified 8 Strategy Categories and colors.
- ✅ **Comprehensive Reference Manual**: Saved complete investment taxonomy, financial metrics, and screening rules in `docs/STRATEGY_GUIDE.md`.
- ✅ **Cache Bump & VPS Deploy**: Bumped cache to `v=20260904_193939`, compiled bundle, and synced to live VPS with PM2 reload.

---

### Update 5 — 19:45 (Color Scheme Refinement & Final Push)
- ✅ **Palette Refinement**:
  - `Compounders` updated to 🔵 Deep Blue (`#1D4ED8`)
  - `ETF` updated to 🌸 Pink (`#EC4899`)
  - `Mid-Tier` updated to 🔘 Dark 60% Gray (`#64748B`)
- ✅ **Synchronized Modules**: Updated `StrategyConfigs.ts`, `TransactionFormModal.tsx`, `TransactionTable.tsx`, and `docs/STRATEGY_GUIDE.md`.
- ✅ **Production Re-bundle & VPS Sync**: Re-compiled with Vite (8.35s, 0 errors), bumped cache to `v=20260904_194422`, scp to VPS, reloaded PM2 `stock-api`, and pushed cleanly to `origin/master` (`bb6a910f`).

---

## 📜 RAW ARTIFACT BACKUP (Iron Rule)

### Artifact: `implementation_plan.md`
<details>
<summary>Click to expand Implementation Plan (13,791 bytes)</summary>

```markdown
# Smart Selective Buy — ระบบ "เลือกซื้อเฉพาะตัวที่น่าสนใจ" แทนบังคับซื้อทั้งหมด

## 📌 Context & Problem

ระบบ Cash-Flow Rebalance ปัจจุบัน (**Mode 1: Cash-Flow เติมเงิน**) ทำงานแบบ **"กระจายงบไปทุกตัวที่ underweight"** อัตโนมัติ

> ❌ **ปัญหาจริงในสนามรบ:** เมื่อมึงตั้งงบมาจะซื้อเพิ่ม ช่วงนั้นราคาที่ลงมาอาจมีแค่ 1-2 ตัวที่น่าสนใจ (ราคาต่ำกว่า target / ใกล้แนวรับ) ไม่ใช่ทุกตัว แต่ระบบปัจจุบัน**บังคับจ่ายงบให้ทุกตัว**เท่าๆ กันตาม deficit ratio

### สาเหตุรากเหง้า (Root Cause)

```
cashflowRecommendations logic (line 120-170):
- คำนวณ deficit ของทุกหุ้นใน Blueprint
- กระจาย depositAmount ตาม deficit ratio
- ทุกตัวที่ allocatedUsd >= $5 ขึ้นคำแนะนำซื้อ
→ ไม่มี gate ราคา, ไม่มีตัวกรอง, ไม่มีทางเลือก
```

---

## 🎯 Proposed Design: "Smart Selective Buy"

### แนวคิดหลัก: 3 Layers of Intelligence

```mermaid
flowchart TD
    A["มึงตั้งงบ $1,000"] --> B{"Layer 1: Price Gate\nราคาปัจจุบัน vs Target Price\nvs เส้น EMA/SMA"}
    B -->|ผ่าน: ราคาน่าสนใจ| C["✅ ELIGIBLE"]
    B -->|ไม่ผ่าน: ราคายังแพง| D["⏸️ SKIP (แสดงเหตุผล)"]
    C --> E{"Layer 2: User Selection\nCheckbox เลือกตัวที่จะซื้อ"}
    E --> F{"Layer 3: Smart Allocation\nกระจายงบเฉพาะตัวที่เลือก\nตาม deficit ratio ที่ recalculate"}
    F --> G["📋 Final Buy Plan\n(เฉพาะตัวที่เลือก)"]
    D --> H["💰 งบเหลือ\n→ เก็บเป็น Cash Reserve\nหรือ redistribute ให้ตัวที่เลือก"]
```

---

## User Review Required

> [!IMPORTANT]
> **ตัดสินใจ 2 จุด ก่อนกูลงมือ:**
>
> 1. **Price Gate Logic** — กูเสนอ 3 ระดับ ซึ่งต้องการ `target_price` หรือ Technical Levels (EMA150 / SMA200) จาก Blueprint:
>    - `🟢 STRONG BUY` = ราคาต่ำกว่า Target Price หรือ SMA200 (แนวรับสถาบัน)
>    - `🟡 FAIR` = ราคาอยู่ระหว่าง Target Price กับ +10% เหนือ Target
>    - `🔴 EXPENSIVE` = ราคาสูงกว่า Target Price +10% หรือ สูงกว่า SMA50
>    - ถ้าไม่มี Target Price ให้ใช้ EMA 150 เป็น proxy (เส้น Trend ระยะกลาง)
>    - **ถามมึง:** ถ้าหุ้นตัวนั้นไม่มีทั้ง Target Price ทั้ง Technical Levels — ให้ระบบจัดเป็น `🟡 FAIR` (ซื้อได้ แต่ไม่ Force) ใช่ไหม?
>
> 2. **เมื่อ Checkbox ถูกเลือกบางตัว** — งบที่เหลือจากตัวที่ไม่ได้เลือกจะไปไหน?
>    - **Option A**: Redistribute ให้ตัวที่เลือก (เท่ากับมึงซื้อเยอะขึ้นเฉพาะตัวที่ชอบ)
>    - **Option B**: เก็บเป็น "Cash Reserve" ไว้รอจังหวะ (แสดงเป็นยอดงบเหลือ)
>    - **Option C**: ให้มึงเลือกได้ทั้ง 2 แบบ ผ่าน Toggle

## Open Questions

> [!NOTE]
> - Blueprint field `target_price` มีอยู่แล้วในสคีมา + มึงเพิ่ง implement 1-click EMA150/SMA200 chips ไปเมื่อกี้ — ข้อมูลพร้อมใช้ได้ทันที ไม่ต้องเพิ่ม column ใหม่ในฐานข้อมูล
> - ต้องดึง technical levels (EMA150/SMA200) แบบ real-time เพื่อเทียบราคา ตอนนี้ endpoint `/api/prices/technicals/:symbol` พร้อมอยู่แล้ว

---

## Proposed Changes

### Component: SmartRebalancePage.tsx — Cash-Flow Mode Overhaul

#### [MODIFY] SmartRebalancePage.tsx

**1. State เพิ่ม:**
```typescript
// Selective buy state
const [selectedSymbols, setSelectedSymbols] = useState<Set<string>>(new Set());
const [remainderMode, setRemainderMode] = useState<'redistribute' | 'reserve'>('redistribute');
const [technicals, setTechnicals] = useState<Record<string, TechnicalData>>({});
const [techLoading, setTechLoading] = useState(false);
```

**2. Fetch Technical Levels สำหรับทุก Blueprint symbol:**
```typescript
useEffect(() => {
  if (blueprints.length === 0) return;
  const symbols = blueprints.map(b => b.symbol);
  setTechLoading(true);
  Promise.allSettled(symbols.map(s => api.prices.technicals(s)))
    .then(results => {
      const map: Record<string, TechnicalData> = {};
      results.forEach((r, i) => {
        if (r.status === 'fulfilled' && r.value) map[symbols[i]] = r.value;
      });
      setTechnicals(map);
    })
    .finally(() => setTechLoading(false));
}, [blueprints]);
```

**3. Price Signal Logic (ใน `cashflowRecommendations`):**
แทนที่จะปล่อยทุกตัวผ่าน ให้เพิ่ม `priceSignal` field:
```typescript
const getBuySignal = (symbol: string, currentPrice: number) => {
  const bp = blueprints.find(b => b.symbol === symbol);
  const tech = technicals[symbol];
  const targetPrice = bp?.target_price;
  
  if (targetPrice && targetPrice > 0) {
    if (currentPrice <= targetPrice) return { signal: 'strong_buy', reason: 'ราคาต่ำกว่าเป้าหมาย', icon: '🟢' };
    if (currentPrice <= targetPrice * 1.10) return { signal: 'fair', reason: 'ราคาใกล้เป้าหมาย (±10%)', icon: '🟡' };
    return { signal: 'expensive', reason: `ราคาสูงกว่าเป้า ${((currentPrice/targetPrice -1)*100).toFixed(0)}%`, icon: '🔴' };
  }
  
  if (tech) {
    const anchor = tech.ema150 || tech.sma200 || tech.sma50;
    if (anchor && currentPrice <= anchor) return { signal: 'strong_buy', reason: `ราคาต่ำกว่า ${tech.ema150 ? 'EMA150' : 'SMA200'}`, icon: '🟢' };
    if (anchor && currentPrice <= anchor * 1.10) return { signal: 'fair', reason: 'ราคาใกล้แนวรับเทคนิคอล', icon: '🟡' };
    if (anchor) return { signal: 'expensive', reason: 'ราคาสูงกว่าแนวรับ', icon: '🔴' };
  }
  
  return { signal: 'fair', reason: 'ไม่มีข้อมูลราคาเป้าหมาย', icon: '🟡' };
};
```

**4. Recommendations แยกเป็น 2 กลุ่ม:**
- `eligibleBuys`: ตัวที่ user เลือก (checked) — คำนวณ allocation จริงจากงบ
- `skippedBuys`: ตัวที่ user ไม่เลือก — แสดงเหตุผลว่าทำไมถึงข้าม

**5. Auto-select logic:**
- เมื่อโหลดครั้งแรก: ระบบจะ auto-check ตัวที่มี signal `🟢 STRONG BUY` ให้
- ตัว `🟡 FAIR` จะ checked แต่ user สามารถ uncheck ได้
- ตัว `🔴 EXPENSIVE` จะ unchecked by default

**6. Remainder Budget:**
- ถ้า `redistribute`: งบจากตัวที่ uncheck จะ redistribute ให้ตัวที่เลือก (ตาม deficit ratio ใหม่)
- ถ้า `reserve`: แสดงเป็น "Cash Reserve: $XXX (รอจังหวะ)" ใน summary

---

### UI Changes ใน Cash-Flow Mode

**Before (ปัจจุบัน):**
```
[ช่องกรอกจำนวนเงิน]
[Card ซื้อ CRWD +X หุ้น] [Card ซื้อ MELI +X หุ้น] [Card ซื้อ META +X หุ้น]
→ บังคับซื้อทุกตัว ไม่มีตัวเลือก
```

**After (ใหม่):**
```
[ช่องกรอกจำนวนเงิน]   [Toggle: Redistribute / Cash Reserve]

┌─ Price Signal Summary ──────────────────────────────────┐
│ 🟢 STRONG BUY (2 ตัว)  🟡 FAIR (2 ตัว)  🔴 EXPENSIVE (1 ตัว) │
│ งบที่จะใช้: $800 / $1,000   Cash Reserve: $200            │
└──────────────────────────────────────────────────────────┘

[☑️ 🟢 CRWD  $214 → Target $180  BUY +3.2 หุ้น  $685]
[☑️ 🟢 HIMS  $27  → EMA150 $25   BUY +4.1 หุ้น  $115]
[☐  🟡 META  $610 → ไม่มีเป้า   SKIP              ]
[☐  🔴 MELI  $1991→ Target $1800 EXPENSIVE +10.6%  ]
[☐  🔴 CASH  ...                                    ]
```

---

### Scenarios ที่ครอบคลุม

| # | Scenario | ระบบทำอะไร |
|---|----------|------------|
| 1 | มี 5 ตัวใน Blueprint ทั้ง 5 ตัวราคาน่าซื้อ | Auto-check ทั้ง 5, กระจายงบตามปกติ (เหมือนเดิม) |
| 2 | มี 5 ตัว แต่ราคาน่าซื้อแค่ 2 ตัว | Auto-check 2 ตัว, งบทั้งหมดลง 2 ตัวนั้น (redistribute) หรือเก็บ reserve |
| 3 | User เลือกซื้อเพียง 1 ตัวจากทั้งหมด | งบทั้ง $1,000 ลงตัวเดียว (ถ้า redistribute) |
| 4 | ทุกตัวราคาแพงหมด (🔴 ทั้งหมด) | แสดง Warning "ไม่มีตัวไหนน่าซื้อตอนนี้ แนะนำเก็บ Cash" + uncheck ทั้งหมด |
| 5 | หุ้นใหม่ใน Watchlist ราคาลงมาถึงเป้า | ขึ้น 🟢 + auto-check ให้ทันที |
| 6 | User อยากซื้อตัวแพง (override) | คลิก checkbox ได้เลย + ระบบแสดง ⚠️ "กำลังซื้อเหนือเป้า X%" |
| 7 | ไม่มี Target Price หรือ Technical data | ขึ้น 🟡 FAIR → user ตัดสินใจเอง |
| 8 | งบน้อยมาก (< $50) ไม่พอซื้อหลายตัว | แนะนำซื้อตัวเดียวที่ underweight มากสุด + signal ดีสุด |

---

### Files Changed Summary

| File | Change |
|------|--------|
| `SmartRebalancePage.tsx` | เปลี่ยน Cash-Flow mode ให้มี checkbox selection, price signal, remainder toggle |
| `BlueprintEditor.tsx` | ไม่แก้ (ข้อมูล target_price + technicals พร้อมอยู่แล้ว) |
| `blueprintStore.ts` | ไม่แก้ |
| `api.ts` | ไม่แก้ (endpoint technicals พร้อมแล้ว) |
| Backend (server) | ไม่แก้ |

---

## Verification Plan

### Manual Verification
1. เปิดหน้า Rebalance > Cash-Flow Mode
2. ตรวจสอบ Price Signal (🟢🟡🔴) ว่าแสดงถูกต้องตาม Target Price / EMA150
3. ทดสอบ checkbox: เลือกเฉพาะ 1-2 ตัว → ดูงบ redistribute ให้ถูกต้อง
4. ทดสอบ Toggle Redistribute vs Cash Reserve
5. ทดสอบ Edge case: uncheck ทุกตัว → แสดง warning
6. Copy buying plan → ตรวจสอบว่ามีแค่ตัวที่เลือก
7. Build ต้อง pass zero errors
```

</details>

---

### Artifact: `walkthrough.md`
<details>
<summary>Click to expand Walkthrough (4,717 bytes)</summary>

```markdown
# Walkthrough: Smart Selective Buy & Option C Remainder Strategy

ระบบ **Smart Selective Buy** ช่วยแก้ปัญหาการถูกบังคับซื้อหุ้นทุกตัวพร้อมกันในโหมด Cash-Flow เติมเงิน โดยเปิดให้ผู้ใช้เลือกตัวที่จะซื้อได้ตามต้องการ พร้อมระบบประเมินราคา (Price Gate) และเลือกวิธีจัดการงบที่เหลือ (Option C)

---

## 🚀 ฟีเจอร์ที่พัฒนาสำเร็จ

### 1. Interactive Selection (เลือกซื้อเฉพาะตัวที่ต้องการในรอบนี้)
- ทุกการ์ดใน Blueprint มี Checkbox ขนาดใหญ่ คลิกเลือก/ปลดติ๊กได้โดยตรง
- มีปุ่มทางลัด (Quick Selection Filters):
  - `เลือกทั้งหมด`
  - `🟢 เฉพาะ Strong Buy`
  - `🟢+🟡 ตัวที่ราคาน่าซื้อ` (ตัดตัวแพงออก)
  - `ล้างที่เลือก`

### 2. Price Gate Signal (ระบบให้คะแนนความคุ้มค่าของราคา)
- วิเคราะห์ราคาตลาดเทียบกับ **Target Price** (ถ้าตั้งไว้ใน Blueprint) หรือ **Technical Support Levels** (EMA150 / SMA200 / SMA50 ดึงสดจาก Yahoo Finance):
  - `🟢 STRONG BUY`: ราคาต่ำกว่าเป้าหมาย หรือต่ำกว่าเส้นแนวรับสถาบัน (EMA150 / SMA200)
  - `🟡 FAIR`: ราคาใกล้เคียงเป้าหมาย (±10%) หรือไม่มีข้อมูลเป้าหมาย
  - `🔴 EXPENSIVE`: ราคาสูงกว่าเป้าหมายหรือแนวรับ (>10%) แนะนำรอจังหวะย่อ
- แสดง Badge พร้อมเหตุผลชัดเจน เช่น `ต่ำกว่าแนวรับ EMA150 -0.8% (แนวรับ $615.74)`

### 3. Option C Remainder Strategy (จัดการงบส่วนที่เหลือ)
- สลับโหมดได้ด้วยปุ่มเดียว:
  - **🔄 เกลี่ยให้ตัวที่เลือก (Redistribute)**: เทงบทั้งหมดให้กับหุ้นที่ผู้ใช้เลือกซื้อรอบนี้ตามสัดส่วน Deficit
  - **💰 เก็บเป็นเงินสด (Cash Reserve)**: จ่ายงบเฉพาะหุ้นที่เลือก ส่วนงบของหุ้นที่ไม่ได้เลือกจะถูกเก็บเป็น Cash Reserve รอจังหวะราคาลง

### 4. Summary KPI & Copy Buying Plan
- KPI Banner ด้านบนคำนวณแบบ Real-time:
  - **งบที่จะใช้เคาะซื้อ (Deploying)**
  - **เงินสดสำรองรอจังหวะ (Cash Reserve)**
  - **สัญญาณราคาทั้งพอร์ต (🟢, 🟡, 🔴)**
- ปุ่ม `คัดลอกแผนเคาะซื้อ`: คัดลอกเฉพาะตัวที่เลือกซื้อจริง พร้อมสรุปยอด Cash Reserve

---

## 🧪 ผลการทดสอบและการตรวจสอบความถูกต้อง

1. **Production Build**: ผ่านฉลุย 100% (`npm run build` ใน 10.72s ไม่มี error)
2. **Bump Cache & Deploy**: อัปเดต `v=20260904_144940` ซิงก์ขึ้น VPS และ Reload PM2 สำเร็จ
3. **Visual Verification (Live VPS)**:
   - ทดสอบปลดติ๊กหุ้นบางตัว (เช่น GOOG, CRWD) ยอด Deploying ปรับลดเป็น $574.97 ทันที
   - สลับเข้าโหมด `Cash Reserve` ระบบกันเงิน $425.03 เป็นเงินสดสำรองทันที
   - ตัวที่เลือก (CASH, MELI, META, HIMS) แสดงจำนวนหุ้นและงบที่ใช้อย่างถูกต้อง

![Smart Selective Buy Verification](file:///C:/Users/Admin/.gemini/antigravity-ide/brain/5d520e1c-4f81-4820-9a08-65def1748307/smart_rebalance_ui_1788508262806.png)
```

</details>

---

### 5. Dashboard Option B Layout & Sharp Trend Arrows
1. **Option B Layout Harmonization**:
   - **Timeframe Selector**: Set to `ALL` (All Time) as default across the dashboard header.
   - **Holdings Timeframe**: Locked to `1D` with subtle amber highlighted background (`bg-amber-400/[0.08] border-amber-400/20`).
   - **Top / Bottom Performers**: Splitted `Total Return` into two dedicated columns (Dollar Return `$XXX.XX` and Percentage Return `+XX.XX%`) for cleaner visual scanning.
   - **Grid Relocation**: Moved `Top Movers Today` to replace `Activity` in the top overview section. Relocated `Activity` down to form a clean split row next to `Holdings Summary`.
   - **Activity Table Format**: Redesigned to 5 columns: `DATE` (YYYY-MM-DD), `STOCK`, `TYPE` (colorful badge), `PRICE`, and `SHARES`.
2. **Sharp 42-Degree Long Zigzag Trendline Arrows**:
   - Built custom SVG components `LongZigzagTrendUp` (Green `#10B981`) and `LongZigzagTrendDown` (Rose `#FB7185`).
   - Integrated into **Top Movers Today** and the **Holdings Price Column**.
3. **Holdings Table Restructure**:
   - Added **AVG COST** column alongside **TOTAL COST**.
   - Moved **PRICE** (Live Market Price with trend arrow) into Section 3 (1D Group) as its primary column.
   - Set default sort to **`1D % Descending`** (highest gainers at the top).

---

### 6. Rebalance Buying Plan: 3-View Switcher & Master Toggle
1. **Default All-Unchecked**:
   - Opening the Rebalance Tools tab initializes `selectedSymbols` as empty (`0/Total`).
   - Full deposit budget (default $1,000) is held in **Cash Reserve ($1,000.00)** until securities are explicitly selected.
2. **Master Select / Deselect All Toggle**:
   - Dynamically toggles between `☑️ เลือกทั้งหมด (0/X)` (select all) and `☒ ปลดทั้งหมด (X/X)` (deselect all).
3. **3-View Switcher (Stored in `localStorage`)**:
   - **Compact View (Default)**: 50% height reduction, compact row layout with micro progress bar.
   - **Table View (Execution Terminal)**: Institutional table with Checkboxes, Symbols with WATCHLIST badges, Live Price with trend arrows, Price Gate badges, Target vs Actual weight progression, BUY orders, allocated budgets, and neon green glowing rows (`bg-emerald-500/[0.08]`) when selected.
   - **Card View**: Full-size rich cards.

---

### 7. Tiger Portfolio Disaster Recovery & NVDA Restored
- **Investigation**: User noticed NVIDIA (`NVDA`) missing from the Tiger portfolio.
- **Root Cause**: An earlier DB restore used `stock.db.bak_tiger`, a snapshot taken before `import_tiger.mjs` was executed on Sep 3. This reverted Tiger to an obsolete 8-transaction state without NVDA and with -$304.67 negative cash.
- **Restoration**:
  - Re-inserted all **57 reconciled transactions** for Tiger.
  - Restored **NVDA: 2.53993 shares** across 6 BUY lots + 3 dividends (Total Cost $465.01, Avg Price $183.08).
  - Restored **SCHG: 29.13602 shares** across 12 BUY lots + 4 dividends (Total Cost $925.96, Avg Price $31.78).
  - Restored 18 Deposits and 14 Interests.
  - Standardized 8 Strategy Categories: NVDA = `Growth`, SCHG = `ETF`, CASH = `Cash`.
  - Reconciled Cash Balance to exact target: **+$8.68**.
  - Verified `Doctorbank Growth` portfolio remained 100% untouched at 52 transactions.

---

### 8. 🛡️ SOURCE OF TRUTH TRIPLE-REDUNDANCY REPOSITORY (คำถามสำคัญ: ข้อมูลเก็บไว้ไหนบ้าง)

เพื่อแก้ปัญหาข้อมูลสูญหายถาวร ได้สร้างระบบสำรองข้อมูลดิบ (Raw Data) และจัดเก็บไว้ **3 แหล่ง (Triple Redundancy)**:

#### 📍 พิกัดการจัดเก็บ (Exact File Storage Locations)
1. **บนเครื่อง Local (Windows):**
   - Path: `c:\My Claw\MyProjects\My Stock Portfolio\server\backups\`
2. **บนเซิร์ฟเวอร์จริง (VPS Linux):**
   - Path: `/root/stock-portfolio/server/backups/`
3. **บน Cloud (GitHub Master Repository):**
   - Repository: `https://github.com/TheDarkXX/MyProjects`
   - Folder: `My Stock Portfolio/server/backups/`
   - Commit: `d99c2983`

#### 📦 บัญชีไฟล์สำรอง (Inventory of 11 Backup Files)

| ชื่อไฟล์ | ฟอร์แมต | วัตถุประสงค์ |
| :--- | :---: | :--- |
| `transactions_all.json` | JSON | ธุรกรรมจริงทั้งหมด **109 รายการ** (Doctorbank Growth 52 + Tiger 57) ครบทุกฟิลด์ |
| `transactions_all.csv` | CSV | ธุรกรรมทั้งหมด 109 รายการ ในรูปแบบ CSV เปิดด้วย Excel / Google Sheets ได้ทันที |
| `transactions_doctorbank_growth.json` / `.csv` | JSON / CSV | เฉพาะพอร์ต **Doctorbank Growth (52 รายการ)** |
| `transactions_tiger.json` / `.csv` | JSON / CSV | เฉพาะพอร์ต **Tiger (57 รายการ รวม NVDA 6 ไม้, SCHG 12 ไม้, ปันผล, ดอกเบี้ย)** |
| `transactions_dump.sql` | SQL | คำสั่ง SQL `INSERT OR REPLACE` ทั้งหมด พร้อมรันเข้า SQLite ได้ทุกเครื่อง |
| `portfolios.json` | JSON | ข้อมูล Config ของทั้ง 2 พอร์ต (ID, Currency, Icon, Goal) |
| `blueprints.json` | JSON | สัดส่วนเป้าหมาย Asset Allocation Blueprint (8 กฎของ Doctorbank Growth) |
| `stock_metadata.json` | JSON | ข้อมูลหุ้น, Sector, ปันผลต่อปี, ความถี่ปันผล, และโลโก้ |
| `latest_prices.json` | JSON | ราคาตลาดและ % การเปลี่ยนแปลงล่าสุดของหุ้นทุกตัว |
| `README.md` | Markdown | คู่มือขั้นตอนการกู้คืนฐานข้อมูลฉุกเฉิน |

#### ⚡ สคริปต์กู้คืนฉุกเฉินแบบ 1 คลิก (One-Click Disaster Recovery)
ไฟล์: `server/restore_source_of_truth.js`
คำสั่งเมื่อต้องการกู้คืนทั้งระบบ:
```bash
# บน VPS:
node /root/stock-portfolio/server/restore_source_of_truth.js
pm2 reload stock-api

# บน Local (ถ้ามี better-sqlite3):
node server/restore_source_of_truth.js
```
สคริปต์นี้จะอ่านไฟล์ JSON จากโฟลเดอร์ `server/backups/` และเขียนทับลง `stock.db` ทันที พร้อมรัน WAL Checkpoint ให้เสร็จสมบูรณ์ใน 1 วินาที

---

### 9. Smart Rebalance v2: Visual Analytics & Default Table View
1. **Table View as Default View**:
   - Initialized `planView` to `'table'` so the Execution Terminal renders automatically upon landing on the Rebalance Tools tab.
   - Preserved interactive switching between `Table`, `Compact`, and `Card` views with persistent `localStorage` synchronization.
2. **Tab 2 (Gap Matrix) Visualizations**:
   - **4 KPI Metric Strip**: `Total Tracked Assets`, `Overweight (> +1%)` (Blue), `On Target (±1%)` (Emerald), and `Underweight (< -1%)` (Amber).
   - **Actual vs Blueprint Target Donut Comparison**: Two synchronized side-by-side donut charts comparing current portfolio holdings vs Target Blueprint allocations, with matching asset palette colors and hover tooltips.
   - **Blueprint Gap Delta (%) Diverging Bar Chart**: Horizontal bar chart centered at 0% baseline. Bars diverging right (Blue) denote Overweight positions, and bars diverging left (Amber) denote Underweight positions requiring funding.
3. **Tab 4 (Dividend DRIP) Visualizations & Compounding Simulator**:
   - **3 KPI Summary Cards**: `Est. Annual Dividend` ($/yr), `Avg. Monthly Cashflow` ($/mo), and `Portfolio Weighted YOC` (Yield on Cost %).
   - **Dividend Income Contribution Donut**: Pie/Donut breakdown of dividend income sources by security, with yield and income metrics in tooltips.
   - **DRIP Snowball Compounding Projection (0, 1, 3, 5, 10 Years)**: Multi-year projection bar chart comparing the compound growth of reinvesting dividends (**With DRIP** - Emerald) vs withdrawing cash (**No DRIP** - Slate), highlighting the compounding bonus.
   - **Precision Clean-Up**: Formatted shares in the DRIP table to 4 decimal places (`Number(item.quantity.toFixed(4))`), eliminating JavaScript floating-point artifacts.

---

## ⏱️ Timeline & Debugging Log

- **2026-09-04 09:50**: Investigated Rebalance black screen crash; traced `ReferenceError: currentVal is not defined` inside `SmartRebalancePage.tsx`.
- **2026-09-04 09:55**: Fixed variable mismatch, wrapped tabs with class-based `ErrorBoundary` in `App.tsx`, built and deployed V2.1.2 hotfix.
- **2026-09-04 13:00**: Resumed Phase 1 Blueprint and Phase 2 Dividend DRIP Simulator implementation.
- **2026-09-04 13:30**: Discovered Yahoo syntax error, ETF dividend schema gap; deployed fixes to backend and frontend.
- **2026-09-04 14:35**: User raised strategic limitation of Cash-Flow mode: "ในความเป็นจริง ราคาทีลงมาอาจมีแค่ 1-2 ตัวที่จะซื้อได้ ไม่ใช่ทั้งหมด... ระบบบังคับซื้อทั้งหมด".
- **2026-09-04 14:40**: Drafted architecture plan for Smart Selective Buy (Price Gate + Option C Toggle). User reviewed and approved Option C.
- **2026-09-04 14:48**: Implemented selective buy logic, valuation engine, remainder toggle, and KPI bar in `SmartRebalancePage.tsx`.
- **2026-09-04 14:50**: Verified zero build errors (`npm run build`), deployed to VPS (`185.250.38.247`), and visually validated with browser subagent.
- **2026-09-04 16:00**: User initiated `/save`. Executed full context extraction, artifact backup, cache bump, and git sync.
- **2026-09-04 19:15**: User requested Strategy Categories refactoring and Pie Chart visualization for Blueprint. Created `BlueprintPieChart.tsx` and updated `StrategyConfigs.ts`.
- **2026-09-04 19:28**: Replaced Sector auto-fill in `BlueprintEditor.tsx` with interactive `<select>` dropdown with auto-save; fixed CASH price fetch bug. Verified build in 8.72s.
- **2026-09-04 19:33**: Synchronized SQLite `stock.db` (`transactions` table `stock_type` and `portfolio_blueprints`) for user's actual portfolio holdings in `Doctorbank Growth` and `Tiger`.
- **2026-09-04 19:38**: Authored comprehensive reference manual `docs/STRATEGY_GUIDE.md` detailing all 8 categories, metrics, and screening rules.
- **2026-09-04 19:44**: User customized color palette (Compounders -> Deep Blue, ETF -> Pink, Mid-Tier -> Dark 60% Gray). Updated all components, bumped cache to `v=20260904_194422`, deployed to VPS, and pushed to `origin/master`.
- **2026-09-04 20:30**: User requested Dashboard redesign: Option B layout (Holdings & Activity side-by-side, Top Movers Today moved up, Timeframe ALL default, soft amber 1D highlight, split return columns).
- **2026-09-04 21:00**: Designed sharp 42-degree long zigzag trendline arrows (`LongZigzagTrendUp` and `LongZigzagTrendDown`); integrated into Top Movers and Holdings Price column.
- **2026-09-04 21:30**: Restructured Holdings table with Avg Cost column, live price in 1D block, and default sort by 1D % Descending.
- **2026-09-04 22:30**: User requested Rebalance Buying Plan upgrades: 3 views (`Card`, `Compact`, `Table`), default all-unchecked, and master select/deselect toggle. Implemented and verified on live server.
- **2026-09-04 22:53**: User reported missing NVIDIA in Tiger portfolio. Traced to premature DB restore from snapshot taken prior to Sep 3 Tiger reconciliation.
- **2026-09-04 22:58**: Wrote and executed `restore_tiger_v2.mjs`. Fully restored all 57 reconciled transactions for Tiger with proper `stock_type`. Restored NVDA (2.54 shares, +26.15%), SCHG (29.14 shares), and positive Cash ($8.68). Confirmed Doctorbank Growth unchanged (52 txs).
- **2026-09-04 23:03**: Built Source of Truth export pipeline. Dumped all 109 transactions into JSON, CSV, and SQL formats across 3 layers (Local, VPS, GitHub). Created `restore_source_of_truth.js` one-click recovery tool.
- **2026-09-04 23:25**: User invoked `/save` to document storage coordinates and lock in the single Source of Truth.
- **2026-09-04 23:35**: User approved implementation plan for Rebalance Visualizations (Table view default, Gap Matrix charts, Dividend DRIP charts).
- **2026-09-04 23:42**: Added Recharts imports and calculated memo metrics (`matrixDivergingData`, `actualDonutData`, `targetDonutData`, `matrixSummary`, `dividendStats` with `weightedYoc`, `dividendDonutData`, `dripSnowballData`).
- **2026-09-04 23:43**: Implemented Mode 2 (Gap Matrix) and Mode 4 (Dividend DRIP) visual charts and KPI strips.
- **2026-09-04 23:44**: Production build passed in 8.95s. Bumped cache to `v=20260904_234413`, deployed to VPS, and reloaded PM2 `stock-api`.
- **2026-09-04 23:45**: Browser automation verified default Table View, Gap Matrix charts, and Dividend DRIP charts.
- **2026-09-04 23:47**: Polished share quantity decimals in DRIP table. Rebuilt in 9.27s, bumped cache to `v=20260904_234738`, deployed to VPS, reloaded PM2, and pushed to `master`.
- **2026-09-04 23:51**: User invoked `/save`. Updated Quick Save records, ran incremental indexer, and verified backup state.

---

## 📂 Files Changed This Session (Round 4)

| File | What Changed | Why |
| :--- | :--- | :--- |
| `src/components/rebalance/SmartRebalancePage.tsx` | Added Gap Matrix & Dividend DRIP charts, set table view as default, cleaned share decimals | Upgraded Rebalance UX with high-impact visual analytics |
| `index.html` | Bumped cache query string (`v=20260904_234738`) | Ensured instant browser cache invalidation across all clients |

---

## 📦 RAW ARTIFACT BACKUP (Round 4)

<details>
<summary><b>📄 Implementation Plan: Smart Rebalance Visualizations & Table View Default</b></summary>

```markdown
# Implementation Plan: Smart Rebalance Visualizations & Table View Default

ยกระดับหน้า **Smart Rebalance** ให้เห็นภาพชัดเจน เข้าใจง่ายในเสี้ยววินาที และสวยงามพรีเมียมตามมาตรฐานสถาบัน โดยครอบคลุม 3 ส่วนหลักตามบรีฟ:
1. **Buying Plan**: ตั้งค่า **Table View (Execution Terminal)** เป็นค่าเริ่มต้น (Default)
2. **Tab 2 (Gap Matrix)**: เพิ่มกราฟ Visualization เปรียบเทียบ **Actual vs Target Allocation** และ **Diverging Gap Delta** ชี้จุดขาด/เกินของหุ้นแต่ละตัว
3. **Tab 4 (Dividend DRIP)**: เพิ่มกราฟ **Dividend Income Contribution Donut** และ **DRIP Snowball Multi-Year Compounding Projection** จำลองพลังการทบต้น

---

## Proposed Visual Designs & Architecture

### 1. Buying Plan: Table View as Default
- ใน `src/components/rebalance/SmartRebalancePage.tsx`:
  - ปรับ Initial State ของ `planView` ให้เป็น `'table'` (เดิมเป็น `'compact'`)
  - อัปเดต `localStorage` fallback เป็น `'table'`
  - ผู้ใช้ยังสามารถคลิกสลับเป็น `Compact` หรือ `Card` ได้ตลอดเวลา และระบบจะบันทึกจำค่าผ่าน `localStorage`

---

### 2. Tab 2: Gap Matrix Visualizations (2 ชาร์ตคู่ขนาน)
เพิ่มกล่องชาร์ตด้านบนก่อนตาราง Matrix เพื่อให้เห็นภาพรวมของพอร์ตทันที:

```
+-------------------------------------------------------------+-------------------------------------------------------------+
| Chart A: Actual vs Target Allocation Donut                  | Chart B: Blueprint Gap Delta (Diverging Bar)                |
| - วงซ้าย: สัดส่วนจริงปัจจุบัน (%)                             | - กราฟแท่งแนวนอนชี้เบี่ยงเบนจากจุดศูนย์กลาง 0% (On Target)  |
| - วงขวา: สัดส่วนเป้าหมาย Blueprint (%)                       | - แท่งพุ่งไปขวา (น้ำเงิน/ม่วง): Overweight (ถือเกินเป้า)    |
| - สังเกตเห็นได้ทันทีว่าพอร์ตเอียงไปทางไหน                   | - แท่งพุ่งไปซ้าย (ส้ม/แดง): Underweight (ถือขาดเป้า ต้องเติม) |
+-------------------------------------------------------------+-------------------------------------------------------------+
```

1. **Chart A (Comparison Donut / Side-by-Side Rings)**:
   - **สัดส่วนจริง (Actual %)** เทียบกับ **สัดส่วนเป้าหมาย (Target %)**
   - ใช้โทนสีประจำหุ้น / Strategy Category เดียวกัน เพื่อให้เปรียบเทียบง่าย
   - ตรงกลางแสดงมูลค่าพอร์ตรวม และจำนวนหุ้นที่อยู่ใน Blueprint
2. **Chart B (Diverging Gap Delta Bar Chart)**:
   - มีแกนกลาง `0% (On Target)`
   - หุ้นที่สัดส่วนจริงต่ำกว่าเป้าหมาย (Underweight) แท่งจะยื่นไปทางซ้าย (สีส้ม/ทอง) พร้อมบอกจำนวนหุ้นและเงินที่ขาด
   - หุ้นที่สัดส่วนจริงสูงกว่าเป้าหมาย (Overweight) แท่งจะยื่นไปทางขวา (สีน้ำเงิน/ม่วง) พร้อมบอกมูลค่าที่เกินเป้า
   - ช่วยให้ตัดสินใจได้ทันทีว่า "ตัวไหนเร่งด่วนที่สุดที่ต้องเติมเงิน"

---

### 3. Tab 4: Dividend DRIP Visualizations (KPI Cards + Income Donut + Snowball Projection)
เพิ่มส่วนจำลองและกราฟวิเคราะห์รายได้ปันผล:

```
+---------------------------------------------------------------------------------------------------------------------------+
| Row 1: 3 Mini KPI Summary Cards                                                                                           |
| [ 💵 ปันผลคาดการณ์ต่อปี: $XX.XX ]   [ 🗓️ กระแสเงินสดเฉลี่ยต่อเดือน: ~$X.XX/ด ]   [ 📈 Yield on Cost (YOC) เฉลี่ย: X.XX% ]  |
+-------------------------------------------------------------+-------------------------------------------------------------+
| Chart A: Dividend Income Share (Donut)                      | Chart B: DRIP Snowball 10-Year Compounding (Bar/Area)       |
| - สัดส่วนที่มาของเงินปันผลว่าตัวไหนจ่ายเยอะสุด              | - จำลองการเติบโตของเงินปันผลเมื่อเปิดระบบ DRIP ทบต้น         |
|   (เช่น SCHG 65%, NVDA 35%)                                 | - เปรียบเทียบ: เส้น DRIP (ซื้อหุ้นเพิ่ม) vs ไม่ทบต้น (ถอนสด)|
| - Center: หุ้นที่จ่ายเงินปันผลให้พอร์ตมากที่สุด             | - แสดงพลังของ Compound Interest ในปีที่ 1, 3, 5, 10         |
+-------------------------------------------------------------+-------------------------------------------------------------+
```

1. **Row 1: 3 KPI Summary Cards**:
   - **Annual Income**: รายได้รวมต่อปี (`$XX.XX` / `฿X,XXX`)
   - **Monthly Cashflow**: เฉลี่ยต่อเดือน (`~$X.XX / เดือน`)
   - **Portfolio Weighted YOC**: ผลตอบแทนเงินปันผลเทียบกับต้นทุนจริง
2. **Chart A: Dividend Income Contribution Donut**:
   - ชาร์ตโดนัทแสดงสัดส่วนว่าเงินปันผลมาจากหุ้นตัวใดบ้าง (เช่น SCHG จ่ายกี่ % ของพอร์ต, NVDA จ่ายกี่ %)
   - โฮเวอร์ดูยอดเงินและ Yield ของแต่ละตัว
3. **Chart B: DRIP Snowball Compounding Projection (1-10 Years)**:
   - ชาร์ตกราฟแท่ง/พื้นที่ (Bar / Area Chart) จำลองการทบต้น:
     - ปีที่ 1 -> ปีที่ 3 -> ปีที่ 5 -> ปีที่ 10
     - เปรียบเทียบ 2 เส้น:
       - **With DRIP (Reinvest)**: ปันผลถูกซื้อหุ้นเพิ่ม จำนวนหุ้นโตขึ้นเรื่อยๆ ทำให้ปีถัดไปได้ปันผลทวีคูณ
       - **Without DRIP (Cash Out)**: ปันผลคงที่ตามจำนวนหุ้นเดิม
     - มีปุ่มสลับอัตราเติบโตของเงินปันผล (Dividend Growth Rate เช่น 5% หรือ 8% ต่อปี)
```

</details>

<details>
<summary><b>📄 Walkthrough: Smart Rebalance v2 Visual Analytics & Default Table View</b></summary>

```markdown
# Smart Rebalance v2: Visual Analytics & Default Table View Walkthrough

## สรุปงานที่อัปเกรดเรียบร้อยแล้ว 🚀

### 1. Buying Plan (คำสั่งเคาะซื้อที่แนะนำตาม Blueprint)
- **Default View**: ตั้งค่าเริ่มต้นเป็น **Table View (Execution Terminal)** ทันทีที่เปิดหน้า Rebalance เพื่อให้เห็นตารางและข้อมูลครบทุกมิติ พร้อมปุ่มสลับ Card / Compact / Table
- **All Unchecked as Default**: คงระบบเริ่มต้นแบบติ๊กออกทั้งหมด งบเข้า Cash Reserve เต็มจำนวน พร้อมปุ่มเลือกทั้งหมด / ปุ่มเลือกหุ้นราคาน่าซื้ออัตโนมัติ

![Tab 1 Table View Default](file:///C:/Users/Admin/.gemini/antigravity-ide/brain/5d520e1c-4f81-4820-9a08-65def1748307/tab1_table_view_default_1788540334737.png)

---

### 2. Tab 2: Blueprint Gap Matrix (Visual Analytics)
เพิ่มชุดกราฟ Visualizations ครบชุดเหนือตารางรายละเอียด:
1. **4 KPI Metric Strip**:
   - `Total Tracked Assets`
   - `Overweight (> +1%)` (Blue badge)
   - `On Target (±1%)` (Emerald badge)
   - `Underweight (< -1%)` (Amber badge)
2. **Chart A: Actual vs Blueprint Target Allocation**:
   - Comparison Donut กราฟโดนัทเปรียบเทียบสัดส่วนปัจจุบันจริง (ซ้าย) vs สัดส่วนตาม Blueprint (ขวา) ด้วยโทนสีตามชนิด Asset ชัดเจน
3. **Chart B: Blueprint Gap Delta (%)**:
   - Diverging Horizontal Bar Chart ชี้ส่วนต่างจากเส้นศูนย์ (0% baseline) ให้เห็นทันทีว่าตัวไหนเกินเป้า (สีฟ้า) หรือขาดเป้าต้องเติม (สีส้ม)

![Tab 2 Gap Matrix Visualization](file:///C:/Users/Admin/.gemini/antigravity-ide/brain/5d520e1c-4f81-4820-9a08-65def1748307/tab2_gap_matrix_1788540370967.png)

---

### 3. Tab 4: Dividend DRIP Simulator (Visual Analytics)
เพิ่มชุดกราฟ Visualizations และระบบจำลองผลตอบแทนทบต้น:
1. **3 KPI Summary Cards**:
   - `Est. Annual Dividend`: รายได้ปันผลรวมต่อปี (USD)
   - `Avg. Monthly Cashflow`: กระแสเงินสดเฉลี่ยต่อเดือน (USD/mo)
   - `Portfolio Weighted YOC`: Yield on Cost ถัวเฉลี่ยตามต้นทุนจริงทั้งพอร์ต (%)
2. **Chart A: Dividend Contribution Donut**:
   - กราฟโดนัทแจกแจงสัดส่วนเงินปันผลที่ได้รับแยกตามหุ้นแต่ละตัวในพอร์ต พร้อม Tooltip และ Asset Pills
3. **Chart B: DRIP Snowball Compounding Projection**:
   - กราฟแท่งเปรียบเทียบการเติบโตของเงินปันผลระยะยาว (ปีที่ 0, 1, 3, 5, 10) ระหว่างการนำปันผลไปซื้อหุ้นเดิมซ้ำ (**With DRIP - สีเขียวมรกต**) เทียบกับถอนเงินสดออก (**No DRIP - สีเทาสลัว**) ชี้พลังของดอกเบี้ยทบต้น (Compound Interest) ให้เห็นอย่างทรงพลัง
4. **Clean Share Formatting**:
   - ปรับการแสดงผลจำนวนหุ้นในตารางให้กระชับ ไม่ติด floating-pointเศษทศนิยมยาว

![Tab 4 Dividend DRIP Visualization](file:///C:/Users/Admin/.gemini/antigravity-ide/brain/5d520e1c-4f81-4820-9a08-65def1748307/tab4_dividend_drip_1788540406466.png)
```

</details>

---

## 📝 Changelog

### Update 4 — 2026-09-04 23:51: Smart Rebalance Visual Analytics & Default Table View
- ✅ **Default View**: Set Table View (Execution Terminal) as default on page load.
- ✅ **Tab 2 (Gap Matrix)**: Added 4-card KPI strip, Actual vs Target Comparison Donut charts, and Blueprint Gap Delta Diverging Bar chart.
- ✅ **Tab 4 (Dividend DRIP)**: Added 3-card KPI strip, Dividend Contribution Donut chart, and Multi-Year DRIP Snowball Compounding Projection bar chart.
- ✅ **Decimal Clean-up**: Formatted share counts in the DRIP table to 4 decimal places.
- ✅ **Production & Remote Sync**: Vite built with 0 errors, cache bumped, deployed to VPS (`185.250.38.247`), browser automation validated, and committed to `master`.

---

## 🔗 GBRAIN Backlinks

### depends_on
- **2026-09-04 07:31** | [V2.1.1 Sidebar Portfolio Cleanup, Global Currency Toggle Overhaul, and Prompt Status Typography](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.1.1_[impl]_ui-polish-currency-toggle-and-status-typography.md) -- Baseline UI architecture and Prompt typography rules.
- **2026-09-04 00:05** | [V2.1.0 Global Institutional Typography, TWR Drawdown Engine, and Streamlined Holdings Table](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.1.0_[impl]_ui-typography-drawdown-twr-and-rebalancer-sync.md) -- Core Rebalancer sync and data linking foundation.

### enables
- **2026-09-04 14:50** | [V2.1.2 Hotfix & Smart Rebalance v2 Overhaul](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.1.2_[hotfix]_smart-rebalance-reference-error-and-error-boundary.md) -- Complete production release with Blueprint setup, Dividend DRIP simulator, Smart Selective Buy, Dashboard Option B, and Triple-Redundant Source of Truth Backups.

### related_to
- **2026-09-03 20:25** | [V2.0.0 Institutional Analytics, Risk & Smart Rebalancer](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.0.0_[impl]_institutional-analytics-risk-and-smart-rebalancer.md) -- Original Smart Rebalance mode specifications.
- **2026-09-04 23:05** | [Source of Truth Backups README](file:///c:/My%20Claw/MyProjects/My%20Stock%20Portfolio/server/backups/README.md) -- Raw data inventory and one-click disaster recovery procedures.

