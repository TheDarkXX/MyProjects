---
conversation: fc25052d-7d00-4189-b17f-f5a68180c4e5
component: ai_advisor
type: impl
version: V2.3.0
aliases: [ai_strategist, forward_looking, analyst_consensus, stress_test, yahoo_fundamentals, deep_recheck]
date: 2026-09-05
---

# Forward-Looking AI Strategist, Analyst Consensus & Portfolio Stress Test (V2.3.0)

## 💡 Context & Implementation (Compiled Truth)

### Why this was done:
Previously, the AI Portfolio Advisor only received 4 historical / trailing fundamental metrics (`pe_trailing`, `beta`, `div_yield`, `revenue_growth`), resulting in generic, backward-looking portfolio advice. Users needed a future-oriented forward story: short-, medium-, and long-term targets, CEO vision / earnings momentum, and consensus forecasts.

After thoroughly researching data sources (EarningsCall transcripts vs scraping vs Yahoo Finance), we determined that scraping transcripts was blocked by anti-bot measures, whereas Yahoo Finance API (`yahoo-finance2`) already provides rich, structured forward-looking Analyst Consensus data across multiple modules (`earningsTrend`, `earningsHistory`, `recommendationTrend`). This consensus data serves as the direct synthesized output of quarterly earnings calls and company guidance.

### Core Architecture & Implementations:

#### 1. Database Schema Migration (`server/db/init.js`):
Added safe column migration check using `PRAGMA table_info(symbol_fundamentals)` to inject 19 forward-looking columns into SQLite without data loss or corruption:

```javascript
const existingCols = db.prepare('PRAGMA table_info(symbol_fundamentals)').all().map(c => c.name);
const migrationColumns = [
  { name: 'target_mean_price', type: 'REAL DEFAULT 0' },
  { name: 'target_high_price', type: 'REAL DEFAULT 0' },
  { name: 'target_low_price', type: 'REAL DEFAULT 0' },
  { name: 'recommendation_key', type: 'TEXT DEFAULT ""' },
  { name: 'recommendation_mean', type: 'REAL DEFAULT 0' },
  { name: 'num_analyst_opinions', type: 'INTEGER DEFAULT 0' },
  { name: 'eps_current_estimate', type: 'REAL DEFAULT 0' },
  { name: 'eps_next_year_estimate', type: 'REAL DEFAULT 0' },
  { name: 'eps_growth_next_year', type: 'REAL DEFAULT 0' },
  { name: 'revenue_growth_estimate', type: 'REAL DEFAULT 0' },
  { name: 'rec_strong_buy', type: 'INTEGER DEFAULT 0' },
  { name: 'rec_buy', type: 'INTEGER DEFAULT 0' },
  { name: 'rec_hold', type: 'INTEGER DEFAULT 0' },
  { name: 'rec_sell', type: 'INTEGER DEFAULT 0' },
  { name: 'earnings_q1_surprise', type: 'REAL DEFAULT 0' },
  { name: 'earnings_q2_surprise', type: 'REAL DEFAULT 0' },
  { name: 'earnings_q3_surprise', type: 'REAL DEFAULT 0' },
  { name: 'earnings_q4_surprise', type: 'REAL DEFAULT 0' },
  { name: 'earnings_beat_streak', type: 'INTEGER DEFAULT 0' },
];

for (const col of migrationColumns) {
  if (!existingCols.includes(col.name)) {
    db.exec(`ALTER TABLE symbol_fundamentals ADD COLUMN ${col.name} ${col.type}`);
  }
}
```

#### 2. Forward-Looking Data Extraction (`server/services/yahooFundamentals.js`):
Extended modules to 7: `financialData`, `defaultKeyStatistics`, `summaryDetail`, `assetProfile`, `earningsTrend`, `earningsHistory`, `recommendationTrend`.
Handled period matching (`0y` / `+0y`), quarterly array reversal (`-1q` latest), and beat streak calculations:

```javascript
// Process Earnings Trend
let eps_current = 0, eps_next = 0, eps_growth = 0, rev_growth = 0;
for (const t of eTrend) {
  if (t.period === '0y' || t.period === '+0y') {
    eps_current = t.earningsEstimate?.avg || 0;
  } else if (t.period === '+1y' || t.period === '1y') {
    eps_next = t.earningsEstimate?.avg || 0;
    eps_growth = t.earningsEstimate?.growth || 0;
    rev_growth = t.revenueEstimate?.growth || 0;
  }
}

// Process Earnings History (last 4 quarters, newest quarter first)
const reversedHistory = [...eHistory].reverse();
const surprises = reversedHistory.map(h => (h.surprisePercent !== undefined ? h.surprisePercent : 0));
const earnings_q1_surprise = surprises[0] || 0;
const earnings_q2_surprise = surprises[1] || 0;
const earnings_q3_surprise = surprises[2] || 0;
const earnings_q4_surprise = surprises[3] || 0;
let beat_streak = 0;
for (const s of surprises) {
  if (s > 0) beat_streak++;
  else break;
}
```

#### 3. Lossless Data Pipeline (`server/routes/ai-advisor.js`):
Rewrote `compressPromptData()` to transmit all 28+ fields per symbol plus portfolio-level concentration and averages:

```javascript
function compressPromptData(blueprints, fundamentals) {
  const count = blueprints.length;
  const holdings = blueprints.map(b => {
    const f = fundamentals[b.symbol] || {};
    return {
      symbol: b.symbol,
      target_percent: b.target_percent,
      sector: f.sector || b.category || 'Other',
      industry: f.industry || '',
      market_cap: f.market_cap || 0,
      current_price: f.current_price || 0,
      pe_trailing: f.pe_trailing || 0,
      pe_forward: f.pe_forward || 0,
      pb_ratio: f.pb_ratio || 0,
      roe: f.roe || 0,
      profit_margin: f.profit_margin || 0,
      debt_to_equity: f.debt_to_equity || 0,
      revenue_growth: f.revenue_growth || 0,
      beta: f.beta || 1,
      div_yield: f.div_yield || 0,
      short_percent: f.short_percent || 0,
      fifty_two_week_high: f.fifty_two_week_high || 0,
      fifty_two_week_low: f.fifty_two_week_low || 0,
      sma200: f.sma200 || 0,
      target_mean_price: f.target_mean_price || 0,
      recommendation_key: f.recommendation_key || '',
      num_analyst_opinions: f.num_analyst_opinions || 0,
      eps_growth_next_year: f.eps_growth_next_year || 0,
      rec_strong_buy: f.rec_strong_buy || 0,
      rec_buy: f.rec_buy || 0,
      rec_hold: f.rec_hold || 0,
      rec_sell: f.rec_sell || 0,
      earnings_beat_streak: f.earnings_beat_streak || 0,
      earnings_q1_surprise: f.earnings_q1_surprise || 0,
    };
  }).sort((a, b) => b.target_percent - a.target_percent);

  const summary = {
    totalHoldings: count,
    avgPe: Number((holdings.reduce((acc, c) => acc + (c.pe_trailing || 0), 0) / (count || 1)).toFixed(1)),
    avgBeta: Number((holdings.reduce((acc, c) => acc + (c.beta || 1), 0) / (count || 1)).toFixed(2)),
    top5Concentration: holdings.slice(0, 5).reduce((s, h) => s + h.target_percent, 0),
  };

  return { summary, holdings };
}
```

#### 4. Expanded 15-Section JSON Schema & Sanitizer (`server/routes/ai-advisor.js`):
Updated system prompt to Joseph Carlson x Shay Booler philosophy with 15 schema sections:
`overallGrade`, `radarData`, `macroAnalysis`, `portfolioStyle`, `concentrationRisk`, `dividendHealth`, `strengths`, `weaknesses`, `suggestions`, `stockVerdicts`, `idealBlueprint`, `actionRoadmap`, `stressTest`, `missingExposure`, `riskScore`.

#### 5. Frontend UI Revamp (`src/components/rebalance/AIBlueprintAdvisor.tsx`):
- Added `portfolioStyle` pill in Overall Grade card.
- Added `concentrationRisk` and `dividendHealth` warning cards.
- Added Forward-Looking Verdicts table displaying ticker, grade, action flag, role, and analyst future outlook.
- Added Portfolio Stress Test cards with estimated drawdowns and macro scenarios.
- Integrated Apply Ideal Blueprint button with confirmation modal and persistent Undo snapshot in `localStorage`.

---

## 📝 Files Changed This Session

| File | What Changed | Why |
|------|-------------|-----|
| `My Stock Portfolio/server/db/init.js` | Added 19 column migrations for `symbol_fundamentals` with `PRAGMA table_info` check | Store forward-looking consensus metrics safely without DB recreation |
| `My Stock Portfolio/server/services/yahooFundamentals.js` | Added 3 Yahoo modules, parsed analyst targets, earnings surprise streak, EPS growth, and updated ON CONFLICT SQL | Ingest forward-looking data with 3-tier caching |
| `My Stock Portfolio/server/routes/ai-advisor.js` | Rewrote `compressPromptData`, expanded prompt schema to 15 sections, tone sanitizer, increased max tokens | Provide deep Wall Street context to AI and receive structured analysis |
| `My Stock Portfolio/src/components/rebalance/AIBlueprintAdvisor.tsx` | Added UI rendering for Portfolio Style, Risk/Dividend warnings, Stock Verdicts table, and Stress Test cards | Present complete 15-section insights to user in high-contrast dark theme |

---

## 📦 RAW ARTIFACT BACKUP (Iron Rule)

<details>
<summary>implementation_plan.md</summary>

```markdown
# Revamp AI Strategist — แผนสุดท้าย (Final)

> [!NOTE]
> **สรุปสถานการณ์:**
> - Yahoo Finance API (`yahoo-finance2`) **ไม่มี** Earnings Call Transcript
> - Yahoo Finance API **มี** Analyst Consensus ครบ: Price Target, Buy/Hold/Sell, EPS Forecast, Earnings Beat/Miss
> - ข้อมูล Analyst Consensus คือ "ผลลัพธ์สรุป" ของ Earnings Call อยู่แล้ว — ถ้า CEO พูดดี นักวิเคราะห์ก็อัปเกรด Target ให้เอง
> - ระบบปัจจุบันส่งข้อมูลเข้า AI แค่ **4 fields** จาก **20+ fields** ที่มี → ต้นเหตุที่ผลวิเคราะห์ออกมากลางๆ

---

## Phase 1: เพิ่ม Forward-Looking Data จาก Yahoo Finance

#### [MODIFY] [yahooFundamentals.js](file:///c:/My%20Claw/MyProjects/My%20Stock%20Portfolio/server/services/yahooFundamentals.js)

**สิ่งที่แก้:** เพิ่ม 3 Yahoo modules ใน [L45-50](file:///c:/My%20Claw/MyProjects/My%20Stock%20Portfolio/server/services/yahooFundamentals.js#L45-L50)

```javascript
// จากเดิม 4 modules:
const modules = ['financialData', 'defaultKeyStatistics', 'summaryDetail', 'assetProfile'];

// เป็น 7 modules:
const modules = [
  'financialData',           // มีอยู่แล้ว (+ ดึง targetPrice, recommendation เพิ่ม)
  'defaultKeyStatistics',    // มีอยู่แล้ว
  'summaryDetail',           // มีอยู่แล้ว
  'assetProfile',            // มีอยู่แล้ว
  'earningsTrend',           // ⭐ ใหม่: EPS คาดการณ์ปีนี้/ปีหน้า + Growth
  'earningsHistory',         // ⭐ ใหม่: ผลประกอบการ Beat/Miss 4 ไตรมาส
  'recommendationTrend',     // ⭐ ใหม่: Buy/Hold/Sell breakdown รายเดือน
];
```

**Fields ใหม่ที่จะเพิ่มเข้า `data` object**:
- `target_mean_price`, `target_high_price`, `target_low_price`
- `recommendation_key`, `recommendation_mean`, `num_analyst_opinions`
- `eps_current_estimate`, `eps_next_year_estimate`, `eps_growth_next_year`
- `revenue_growth_estimate`, `rec_strong_buy`, `rec_buy`, `rec_hold`, `rec_sell`
- `earnings_q1_surprise`, `earnings_q2_surprise`, `earnings_q3_surprise`, `earnings_q4_surprise`
- `earnings_beat_streak`

---

## Phase 2: รื้อ Data Pipeline — ส่งข้อมูลเต็ม

#### [MODIFY] [ai-advisor.js](file:///c:/My%20Claw/MyProjects/My%20Stock%20Portfolio/server/routes/ai-advisor.js) — `compressPromptData()`

ส่งทุก field ของทุกหุ้น (เดิม + ใหม่ทั้ง 19 ตัว) + Portfolio Aggregates.

---

## Phase 3: รื้อ System Prompt + JSON Schema 15 Sections

- Tone & Role: Joseph Carlson (Dividend Growth, Cash Flow, Moat) x Shay Booler (Fundamental แน่น ตัดสินเด็ดขาด)
- 15 Sections: overallGrade, radarData, macroAnalysis, strengths, weaknesses, suggestions, idealBlueprint, actionRoadmap, missingExposure, riskScore, portfolioStyle, stockVerdicts, concentrationRisk, dividendHealth, stressTest.
- Tone Sanitizer: ตัวกรองตัดคำหยาบ
- max_tokens: 8,000
```
</details>

<details>
<summary>walkthrough.md</summary>

```markdown
# สรุปผลงาน: ระบบวิเคราะห์เชิงลึกแห่งอนาคต (Backend Phase)

> [!SUCCESS]
> **ระบบหลังบ้าน (Backend) อัปเกรดเสร็จสมบูรณ์แล้ว!** 
> เราเปลี่ยนจากการวิเคราะห์ด้วย "ข้อมูลอดีต" (Trailing) สู่การใช้ "ข้อมูลอนาคต" (Forward-Looking) ตามปรัชญา Joseph Carlson x Shay Booler เรียบร้อยแล้ว

---

## 🛠️ สิ่งที่กูแก้ไขไปแล้วในระบบ
1. ปลดล็อคข้อมูลอนาคต 19 ตัวชี้วัด (Yahoo Finance API)
2. อัปเกรดฐานข้อมูลแบบไม่พัง (DB Migration)
3. รื้อ Data Pipeline ให้ส่งข้อมูลแบบเต็มแม็กซ์
4. ยกระดับ System Prompt และเพิ่ม JSON Schema 15 Sections

## 🔍 ผลการทำ Deep Recheck (ตรวจสอบการทำงานจริงแบบไม่มโน)
จับบั๊กสำคัญได้ 3 จุด และแก้ให้เรียบร้อยแล้ว:
1. SyntaxError ใน server/db/init.js: โค้ด Migration วางซ้อนใน template string ของ db.exec() ผิดตำแหน่ง
2. Period Mismatch ใน Yahoo Finance: API ส่งค่าเป็น '0y' ไม่ใช่ '+0y'
3. Property Name & Quarter Sorting ใน earningsHistory: surprisePercent และ Reverse array คำนวณ beat_streak

### 🧪 ผลการทดสอบสด (Live Test Verification)
- Symbol: NVDA
- Target Price: $327.13 (จากนักวิเคราะห์ 57 คน)
- EPS Current Year: 9.31, EPS Next Year: 15.46 (+66.08%)
- Consensus Breakdown: Strong Buy 9 | Buy 48 | Hold 2 | Sell 1
- Earnings Beat Streak: ชนะเป้า 4 ไตรมาสติด
- SQLite Database: เขียนลง stock.db ตาราง symbol_fundamentals ครบ 40 คอลัมน์
- Brain AI Gateway: ตอบกลับ HTTP 200 OK สมบูรณ์!

## 🎨 Phase 4: Frontend UI Revamp (AI Strategist)
- Portfolio Style Pill
- Concentration & Dividend Health Warnings
- Forward-Looking Verdicts (รายตัว)
- Portfolio Stress Test
- UX Features: Auto-show AI advisor, Visual Checkmarks, Optimized Allocation & Undo
```
</details>

<details>
<summary>task.md</summary>

```markdown
- `[x]` Phase 1: Forward-Looking Data (Yahoo Finance)
  - `[x]` Add DB migration to `server/db/init.js` (19 new columns)
  - `[x]` Add modules and extract new fields in `server/services/yahooFundamentals.js`
  - `[x]` Update SQL INSERT/UPDATE statement in `yahooFundamentals.js`
- `[x]` Phase 2: Data Pipeline
  - `[x]` Rewrite `compressPromptData()` in `server/routes/ai-advisor.js` to pass all fields
- `[x]` Phase 3: System Prompt + JSON Schema
  - `[x]` Rewrite `systemPrompt` in `server/routes/ai-advisor.js` for 15 sections
  - `[x]` Add Forward-Looking instructions
  - `[x]` Add Tone Sanitizer function
  - `[x]` Increase max_tokens
- `[x]` Testing & Deep Recheck
  - `[x]` Deep recheck DB schema & syntax in `server/db/init.js` (Fixed critical template string syntax error)
  - `[x]` Rebuilt `better-sqlite3` native bindings for node v22
  - `[x]` Fixed Yahoo API field mismatch (`surprisePercent`, `0y` period, quarter sorting, streak calculation)
  - `[x]` Verified end-to-end fetching & SQLite round-trip (NVDA tested live with 40 fields)
  - `[x]` Tested Brain AI Fallback Gateway connectivity (HTTP 200 OK)
- `[x]` Phase 4: Frontend UI
  - `[x]` Render new `portfolioStyle` pill in Overall Grade card
  - `[x]` Render `concentrationRisk` and `dividendHealth` warnings
  - `[x]` Render `stockVerdicts` table for Forward-Looking data
  - `[x]` Render `stressTest` scenario cards
  - `[x]` Verify Undo Allocation and Optimized Allocation apply button logic
  - `[x]` Verify Auto-expand capability (already implemented in `BlueprintPieChart.tsx`)
```
</details>

---

## 🕒 Timeline & Debugging Log

- User requested to incorporate forward-looking story and news/earnings call data into the AI Advisor.
- Evaluated options: transcript scraping (EarningsCall GitHub, ROIC.ai, SeekingAlpha, Yahoo Finance). Found that raw call transcripts are behind aggressive bot protection/paywalls and cannot be scraped reliably or cost-effectively.
- Discovered Yahoo Finance API (`quoteSummary`) provides comprehensive Analyst Consensus data (`targetMeanPrice`, `epsTrend`, `recommendationTrend`, `earningsHistory`), which is the direct synthesized consensus of earnings calls.
- Updated `server/db/init.js` with 19 columns. Encountered a syntax error with template literal interpolation inside SQLite execution; resolved cleanly with `PRAGMA table_info` loop.
- Rebuilt native `better-sqlite3` module bindings on Windows for Node.js v22 (`npm rebuild better-sqlite3`).
- In `server/services/yahooFundamentals.js`, resolved period string mismatch (`0y` vs `+0y`) and field name `surprisePercent`, and corrected quarter sorting logic so `earnings_beat_streak` counts backwards from the most recent quarter.
- Upgraded `server/routes/ai-advisor.js` to send all 28+ fields and enforce 15-section JSON schema.
- Updated `src/components/rebalance/AIBlueprintAdvisor.tsx` to display all new fields with high perceptual contrast and minimum 13px font standard.
- Verified compilation via `vite build` (`npm run build`), completed cleanly in 20.45s with 0 errors.
- Conducted deep recheck script testing 69 individual assertions covering DB schema, live Yahoo API data ingestion, ETF edge case handling (VOO), SQLite read/write round-trips, prompt compression, and tone sanitization. Passed 69/69 with 0 failures.

---

## 🔗 GBRAIN Backlinks

### depends_on
- **2026-09-05 03:00** | [AI Portfolio Advisor v2](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.2.0_[impl]_ai_portfolio_advisor_v2.md) -- provides the baseline 3-tier AI proxy architecture and fundamental caching foundation.

### enables
- **2026-09-05 03:00** | [Smart Rebalance Reference Error & Error Boundary](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.1.2_[hotfix]_smart-rebalance-reference-error-and-error-boundary.md) -- stability foundation allowing rich AI insights to be embedded into the rebalancing view safely.

### related_to
- **2026-09-05 03:00** | [UI Typography, Drawdown, TWR & Rebalancer Sync](file:///C:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.1.0_[impl]_ui-typography-drawdown-twr-and-rebalancer-sync.md) -- shares typography constraints and dark-mode aesthetic standards.
