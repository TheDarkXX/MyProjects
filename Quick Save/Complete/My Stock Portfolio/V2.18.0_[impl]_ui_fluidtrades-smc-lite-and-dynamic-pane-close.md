---
id: V2.18.0
title: "FluidTrades - SMC Lite, Dynamic Sub-Pane True Close, Anchored VWAP & Super Money Signal V3 Suite"
type: impl
component: ui
project: My Stock Portfolio
date: 2026-09-11
author: มารบูรพา 🔥
conversation_id: 564e13b5-13f0-4b54-aa61-93cb12f3793c
aliases: [smc-lite, smart-money-concepts, supply-demand, bos, lightweight-charts-primitive, subpane-close, trend-speed, anchored-vwap, super-money-signal, rich-symbols, vertical-padding]
tags: [smc-lite, smart-money-concepts, supply-demand, breakout-of-structure, lightweight-charts, custom-primitive, subpane-close, trend-speed, anchored-vwap, super-money-signal, rich-symbols, tradingview]
---

# FluidTrades - SMC Lite, Dynamic Sub-Pane True Close, Anchored VWAP & Super Money Signal V3 Suite

## 🧠 Context & Implementation (Compiled Truth)

### 1. วัตถุประสงค์และการตัดสินใจเชิงสถาปัตยกรรม (Architectural Decisions)
ในเซสชันนี้ ผู้ใช้ต้องการยกระดับ **X-Chart Trading Terminal** ให้รองรับอินดิเคเตอร์ระดับมืออาชีพเพิ่มเติม และแก้ปัญหาการทำงานของ Sub-Pane ในเชิงลึก:

1. **การแก้ปัญหา Sub-Pane Close พับเก็บ (True Pane Destroy Engine):**
   - **ปัญหาเดิม:** เมื่อผู้ใช้กดปุ่ม **X** บน Floating Toolbar ของ Sub-pane ระบบเดิมสั่งแค่ `toggleVisibility` ทำให้หน้าต่างย่อขนาดเหลือเส้นบาง 18px พร้อมข้อความ `(พับเก็บ)` ค้างอยู่บนชาร์ต
   - **การแก้ไข:** ปรับสถาปัตยกรรมใหม่ให้การกด **X** เป็นการ "ลบทิ้งจริง" (True Pane Destroy) โดยคำนวณ `activeSubPanes` แบบไดนามิก (เฉพาะ Indicator ที่เปิดอยู่) และทำ Dynamic Index Mapping ลงบน Panes 1..N ของ Lightweight Charts v5 โดยอัตโนมัติ หากไม่มี Sub-pane ใดเปิดอยู่เลย ชาร์ตหลัก Pane 0 จะขยายเต็มพื้นที่ 100% ทันที

2. **Trend Speed Analyzer by Zeiierman:**
   - ติดตั้งอินดิเคเตอร์วัดสปีดโมเมนตัมและแนวโน้มการวิ่งของแท่งเทียน แปลงจาก Pine Script v6
   - รองรับ Dynamic Trend EMA บน Pane 0, Trend Speed Histogram (เขียว/แดง 2 เฉด) บน Sub-pane, และ Dominance Wave Stats Table

3. **FluidTrades - SMC Lite (Smart Money Concepts Lite):**
   - พัฒนาระบบคำนวณและเรนเดอร์โครงสร้าง Smart Money Concepts ซ้อนทับลงบน Pane 0 (Candlestick Overlay) โดยตรง
   - **Mathematical Parity:** ถอดสมการคณิตศาสตร์จาก Pine Script v6:
     - `swingLength = 10`: ตรวจจับ Pivot High/Low
     - `ATR(50)`: คำนวณความกว้างกล่อง Supply/Demand ตามสัดส่วน `boxWidth`
     - `f_check_overlapping`: ป้องกันการสร้างโซนทับซ้อนกันในระยะ `2 * ATR`
     - `Break of Structure (BOS)`: เมื่อราคาปิดแท่งเทียนทะลุโซน จะตัดเป็นเส้นประแนวนอนพร้อมป้าย **BOS** ทันที
     - `Dual SMA Trend Filter`: Fast SMA (15) & Slow SMA (200) พร้อมสัญญาณ Strong Buy / Strong Sell Crossover
     - `Zigzag & Price Action Labels`: HH, LH, HL, LL
   - **Lightweight Charts v5 Custom Primitive (`SMCPrimitive`):**
     - ใช้งาน `ISeriesPrimitive<Time>` วาดลงบน Canvas Layer ของ Pane 0 โดยตรง
     - ใช้ `useMediaCoordinateSpace` ร่วมกับ `timeToCoordinate` และ `priceToCoordinate` ให้พิกัดมีความคมชัดระดับพิกเซล ไม่เบลอ ไม่กระตุก
     - วาดโซน Supply (สีกรมท่ากึ่งโปร่งแสง), โซน Demand (สีทองกึ่งโปร่งแสง), เส้นประ POI Equilibrium, ป้าย POI, ตัวหนังสือ SUPPLY / DEMAND ตรงกลางกล่อง, และป้าย BOS แนวนอน

4. **Anchored VWAP (Volume Weighted Average Price) Implementation:**
   - พัฒนาระบบ Anchored VWAP แบบ TradingView 1:1 (`src/utils/indicators/anchoredVWAP.ts`):
     - คำนวณสะสม $\text{VWAP} = \frac{\sum (Source \times Volume)}{\sum Volume}$ เริ่มจากแท่งเทียนที่ตรงกับ Anchor Date/Time
     - แท่งเทียนก่อนหน้า Anchor จะมีค่าเป็น `null` ทำให้เส้นเริ่มต้นลากจากจุด Anchor จริงเท่านั้น
     - คำนวณ Standard Deviation Bands (Length 30) แบบ Rolling 30 bars พร้อมตัวคูณ Multiplier (Upper & Lower Bands)
     - วาดเส้น 3 เส้นบน Pane 0 ด้วย Lightweight Charts LineSeries พร้อม Anchor Marker บนแท่งเทียนเริ่มต้น
     - สร้าง UI ใน `IndicatorManagerPopover.tsx` มีแท็บ Inputs (Source, Anchor Date/Time, Quick Presets: Lowest Low, Highest High, YTD, 90 Days) และ Style

5. **Super Money Signal V3 (Banker Money & Hot Money Flow):**
   - ถอดแบบสูตรจาก Pine Script v6 (`src/utils/indicators/superMoneySignal.ts`):
     - **Banker Money**: RSI Base 50, Period 50, Sensitivity 1.5 คำนวณแรงสะสมของสถาบัน/เจ้ามือ
     - **Hot Money**: RSI Base 30, Period 40, Sensitivity 0.7 คำนวณกระแสเงินร้อนเก็งกำไร
     - **Ready Signal (⚪)**: เกิดเมื่อ Banker สะสมต่อเนื่องครบ `readyDays` (เช่น 3 วันติด)
     - **Buy Signal (🟡)**: เกิดเมื่อ Banker สะสมแน่นเกิน `buyDays` (เช่น 5 วันติด) + มี Hot Money ไหลทะลักเข้ามาช่วยดันราคา Breakout พร้อมกัน
     - **No Signal (🔴)**: เกิดเมื่อ Banker หลุดต่อเนื่องครบ `noSignalDays` (เจ้ามือแจกของเสร็จ ทิ้งหุ้น)
     - **Consecutive Counters**: ปรับปรุงระบบนับแท่งเทียนไม่ให้สัญญาณพ่นรัว หรือรีเซ็ตเป็น 0 ไวกว่าความจริง ให้สัญญาณแม่นยำและเสถียรที่สุด

6. **Vertical Padding Calibration (สูตรฟิสิกส์เดียวกับ V1 100%):**
   - **ปัญหาเดิม**: ตัวคูณเดิม `avgRange * (padding * 0.12)` มีค่าน้อยเกินไปบนพิกัดหน้าจอ ทำให้เมื่อปรับ Padding 0-20 สัญลักษณ์แทบไม่ขยับ
   - **สูตรใหม่**:
     ```typescript
     const baseWickOffset = avgRange * 0.45;       // เคลียร์พ้นไส้เทียนล่างทันที
     const extraPadding = avgRange * (padding * 0.15); // ตัวคูณระยะห่างตามสเกลที่เลื่อนปรับ
     const offset = baseWickOffset + extraPadding;
     ```
   - **Reactive setMarkers**: เพิ่ม `useEffect([pane0Markers])` ใน `LWChart.tsx` ให้ส่งพิกัดใหม่เข้า `markersPluginRef.current?.setMarkers()` ทันทีที่ผู้ใช้ปรับสไลเดอร์ใน Popover ไม่ดีเลย์

7. **Rich Symbol Catalog (23 สัญลักษณ์ แยก 4 หมวด):**
   - ออกแบบ `RICH_SYMBOL_OPTIONS` ใน `IndicatorManagerPopover.tsx` แบ่งเป็น 4 หมวด `<optgroup>`:
     - **Arrows & Pointers**: ▲ Arrow Up, ▼ Arrow Down, ► Pointer Right, ⇈ Double Up
     - **Classic Shapes**: ● Circle Solid, ○ Circle Hollow, ■ Square Solid, □ Square Hollow, ◆ Diamond Solid, ◇ Diamond Hollow, ▲ Triangle Solid, △ Triangle Hollow, ▼ Triangle Down, ⬡ Hexagon
     - **Stars & Sparkles**: ★ Star Solid, ☆ Star Hollow, ✦ Sparkle Star
     - **Badges & Indicators**: ✚ Plus Cross, ✖ Multiply / X, ✔ Checkmark, ⚡ Lightning Bolt, 🎯 Target, 🔥 Momentum Fire, ⚑ Flag Marker, 💲 Dollar Flow
   - สร้าง `resolveSuperVisual` ใน `LWChart.tsx`: หากเป็น Native Shape ของ Lightweight Charts จะเรนเดอร์ผ่าน Canvas Engine โดยตรง ส่วนรูปทรงที่เป็น Unicode Glyphs จะเรนเดอร์เป็น Text Markers ความละเอียดสูงตามสีของสัญญาณนั้นๆ

8. **ตรรกะในโลกความจริง (Real-Market Mechanics):**
   - **Ready (ศรขาว)** = "เจ้ามือเริ่มแอบเก็บของที่ก้นเหว" (ราคาซึม กองทุนดูดของเงียบๆ เตรียมตัว ดึงเข้า Watchlist ห้าม All-in)
   - **Buy (สามเหลี่ยมเหลือง)** = "เจ้าเก็บของเสร็จ + จุดพลุเรียกแขก วิ่งทะลุฟ้า" (Banker แน่น + Hot Money เข้า = จุดเข้าทำที่แม่นที่สุด มีคนค้ำราคาด้านล่างและมีแรงส่งข้างบน)
   - **No Signal (ศรแดง)** = "งานเลี้ยงเลิก เจ้าแจกของหมด ชิ่งหนี" (สัญญาณเตือนล็อกกำไรหรือ Stop Loss ทันที)

---

### 2. โครงสร้าง Data Flow และ Component Architecture

```
                                      [ Indicator Settings Store ]
                                                    │
        ┌───────────────────┬───────────────────────┼───────────────────────┬───────────────────┐
        ▼                   ▼                       ▼                       ▼                   ▼
  [ SMC Lite ]      [ Multi-Pane ]          [ Anchored VWAP ]     [ Super Money Signal ]  [ Trend Speed ]
(computeSMCLite) (activeSubPanes 1..N)    (computeAnchoredVWAP)   (computeSuperMoney)    (TrendSpeedEngine)
        │                   │                       │                       │                   │
        ▼                   ▼                       ▼                       ▼                   ▼
 [ SMCPrimitive ] [ SubPaneToolbar ]       [ LWChart Pane 0 ]      [ LWChart Pane 0 ]     [ Sub-Pane 1..N ]
(ISeriesPrimitive)(Live/True Close)       (LineSeries + Bands)    (23 Rich Markers)      (Histogram/Wave)
```

---

## 📁 Files Changed This Session

| File | What Changed | Why |
|------|-------------|-----|
| `src/types/indicatorConfig.ts` | เพิ่ม `SMCLiteConfig`, `AnchoredVWAPConfig`, `SuperMoneySignalConfig`, `MarkerSymbolType` (23 symbols), `SubPaneIndicatorId` | กำหนดโครงสร้างข้อมูลและค่าเริ่มต้นของทุก Indicator ให้เป็นมาตรฐานเดียวกับ TradingView |
| `src/stores/useIndicatorStore.ts` | เพิ่ม `updateSMCLite`, `updateAnchoredVWAP`, `updateSuperMoneySignal` พร้อมบันทึกลง localStorage | จัดการ State การเปิด/ปิด และการปรับตั้งค่าของ Indicator ทุกตัวแบบ Persistent |
| `src/utils/indicators/smcLite.ts` | สร้าง Calculation Engine สำหรับ SMC Lite (ATR, Pivots, S/D Zones, BOS, Dual SMA, Signals) | คำนวณคณิตศาสตร์ตาม Pine Script v6 แบบ Pure TypeScript บน Client-side |
| `src/utils/indicators/smcPrimitive.ts` | สร้าง Custom Series Primitive สำหรับ Lightweight Charts v5.2.1 | เรนเดอร์กล่อง Demand/Supply, เส้น POI, เส้นประ BOS, และป้ายข้อความบน Canvas |
| `src/utils/indicators/anchoredVWAP.ts` | สร้าง Calculation Engine สำหรับ Anchored VWAP พร้อม Standard Deviation Bands (Length 30) | คำนวณ VWAP สะสมจาก Anchor Date/Time แบบ 1:1 กับ TradingView |
| `src/utils/indicators/superMoneySignal.ts` | สร้าง Calculation Engine สำหรับ Super Money Signal V3 (Banker RSI, Hot Money RSI, Trigger Counters) | คำนวณสัญญาณ Ready, Buy, No Signal ตาม Pine Script v6 พร้อมระบบนับแท่งต่อเนื่อง |
| `src/components/project2x/LWChart.tsx` | เชื่อมต่อ `SMCPrimitive`, Anchored VWAP series, Super Money Signal markers, ปรับสูตร Vertical Padding, เพิ่ม `useEffect([pane0Markers])`, และ `resolveSuperVisual` | วาดเส้น แบนด์ กล่อง และมาร์กเกอร์สัญลักษณ์ 23 แบบบน Pane 0 พร้อมซิงก์ตำแหน่งทันที |
| `src/components/xchart/panes/SubPaneHeaderToolbar.tsx` | ปรับการทำงานของปุ่มปิด (X) ให้เป็นการปิดถาวร (True Close) | ลบ Sub-pane ออกจากหน้าจอแทนการพับเก็บเป็นแถบ 18px |
| `src/components/xchart/IndicatorManagerPopover.tsx` | เพิ่มเมนู SMC Lite, Anchored VWAP, Super Money Signal V3 พร้อม `RICH_SYMBOL_OPTIONS` 23 แบบจัดหมวด `<optgroup>` | ให้ผู้ใช้ปรับแต่งค่าพารามิเตอร์ สี สไตล์ ตัวคูณแบนด์ สัญลักษณ์ และระยะห่างได้ครบวงจร |
| `index.html` / `dist/index.html` | รัน `node bump-cache.js` อัปเดตเวอร์ชันแคช | ป้องกันเบราว์เซอร์โหลดไฟล์ Bundle เก่าที่ค้างในแคช |

---

## 📦 RAW ARTIFACT BACKUP (Iron Rule)

### Artifact: `implementation_plan.md` (Anchored VWAP)
```markdown
# Implementation Plan: Anchored VWAP (Volume Weighted Average Price)

Implement **Anchored VWAP** matching TradingView 1:1 based on the provided Pine Script v5 and TradingView screenshots.

---

## 1. System Architecture & Mathematical Formulation

### 1.1 Core Formulas
1. **Source Price ($src$)**:
   - Default: `hlc3` = $(High + Low + Close) / 3$
   - Options: `close`, `hl2`, `hlc3`, `ohlc4`, `open`, `high`, `low`
2. **Cumulative VWAP from Anchor Point**:
   $$\text{VWAP}_t = \frac{\sum_{i=anchor}^{t} (src_i \times volume_i)}{\sum_{i=anchor}^{t} volume_i} \quad \text{for } t \ge anchor$$
   For bars before $anchor$, VWAP is `null` (not rendered).
3. **Standard Deviation Bands (Length 30)**:
   - Rolling 30-bar standard deviation of the VWAP series:
     $$\mu = \frac{1}{N} \sum_{k=0}^{N-1} \text{VWAP}_{t-k}, \quad \sigma = \sqrt{\frac{1}{N} \sum_{k=0}^{N-1} \text{VWAP}_{t-k}^2 - \mu^2}$$
   - **Upper Band**: $\text{VWAP}_t + (\text{multiplier} \times \sigma)$
   - **Lower Band**: $\text{VWAP}_t - (\text{multiplier} \times \sigma)$
   - Default multiplier = `0.5` (from user screenshot) / `1.0` (Pine default)

---

## 2. Component Design & User Interface

### 2.1 Inputs Tab (Screenshot 2 Matching)
- **Source**: Select dropdown (`(H + L + C) / 3`, `(H + L) / 2`, `Close`, `Open`, `(O + H + L + C) / 4`, `High`, `Low`)
- **Start Calculation (Anchor)**:
  - Date input (`YYYY-MM-DD`) with calendar picker
  - Time input (`HH:mm`) with time picker
  - Quick-Anchor presets: `Lowest Low Bar` 📉, `Highest High Bar` 📈, `YTD`, `90 Days`
- **Show Bands**: Checkbox (default: `true`)
- **Band Multiplier**: Number input (step `0.1`, default: `0.5` / `1.0`)

### 2.2 Style Tab (Screenshot 1 Matching)
- **VWAP**: Checkbox (checked), Color picker (White `#FFFFFF`), Line style (Solid / Dashed), Width (`3px`)
- **Upper Band**: Checkbox (unchecked by default), Color picker (Gray `#94A3B8`), Line style (`Dashed`), Width (`2px`)
- **Lower Band**: Checkbox (unchecked by default), Color picker (Gray `#94A3B8`), Line style (`Dashed`), Width (`2px`)
- **Output Values**:
  - Labels on price scale (checkbox)
  - Values in status line (checkbox)

### 2.3 Pane 0 Chart Rendering
- 3 Lightweight Charts `LineSeries` on **Pane 0** (Main Candlestick Pane):
  - `anchoredVwapSeries`: White dashed/thick line
  - `anchoredUpperBandSeries`: Gray dashed band
  - `anchoredLowerBandSeries`: Gray dashed band
- **Anchor Marker**: Yellow triangle marker (`arrowUp`) at the anchor bar date on the chart to clearly indicate where the calculation started.

---

## 3. Files to Create and Modify

1. **[NEW] `src/utils/indicators/anchoredVWAP.ts`**:
   - `computeAnchoredVWAP(bars, config)` returning `{ vwap, upperBand, lowerBand, anchorIndex, anchorTime }`.
2. **[MODIFY] `src/types/indicatorConfig.ts`**:
   - Define `AnchoredVWAPConfig` interface and add to `IndicatorSettings` & `DEFAULT_INDICATOR_SETTINGS`.
3. **[MODIFY] `src/stores/useIndicatorStore.ts`**:
   - Add `updateAnchoredVWAP` and `toggleAnchoredVWAP`.
4. **[MODIFY] `src/components/project2x/LWChart.tsx`**:
   - Add series refs, `useMemo` calculation, Pane 0 series creation, data feeding, and option synchronization.
5. **[MODIFY] `src/components/xchart/IndicatorManagerPopover.tsx`**:
   - Add Anchored VWAP to indicator list and build dedicated View with Inputs & Style tabs matching screenshots 1:1.

---

## 4. Verification Plan

1. **Type & Build Verification**:
   - Run `npx tsc --noEmit` and `npm run build` to confirm zero compilation errors.
2. **Calculation Precision**:
   - Verify that VWAP before the anchor date remains `null` and begins accumulating exactly from the anchor bar onward.
3. **Deploy & Push**:
   - Run `node bump-cache.js`, SCP to VPS (`185.250.38.247`), reload PM2, git commit & push to `master`.
### Artifact: `implementation_plan.md` (Vertical Padding & Rich Symbol Catalog)
```markdown
# Implementation Plan: Fix Vertical Padding & Rich Symbol Catalog (Super Money Signal V3)

Fix vertical padding so that signals visibly and smoothly move downward from candle wicks in both Super Money Signal and V1, and expand the symbol dropdown from 4 basic options to a rich library of 20+ icons.

## Executive Brief (บรีฟสรุปการแก้ไข)

1. **ปัญหา Vertical Padding ไม่ทำงาน (สาเหตุและทางแก้):**
   - **สาเหตุ:** สูตรเดิมใช้ `offset = avgRange * (padding * 0.12)` ซึ่งบนชาร์ตหุ้น `avgRange * 0.12` มีขนาดเพียง ~0.001 - 0.002 ของราคาทั้งหมด (แปลงเป็นพิกเซลหน้าจอได้ไม่ถึง 1 พิกเซล) เมื่อผู้ใช้กดปรับจาก 0px ไป 8px หรือ 14px ไอคอนจึงขยับน้อยมากจนมองด้วยตาเปล่าไม่เห็น
   - **การแก้ไข:**
     1. ปรับสูตรคำนวณ Offset ให้มี Base Gap ป้องกันทับไส้เทียน และเพิ่มตัวคูณระยะพิกเซลที่แท้จริง (`baseWickGap + paddingOffset`) เพื่อให้เมื่อกด Snug (0px), 8px, 14px, 20px ไอคอนจะทิ้งระยะห่างลงมาอย่างชัดเจน แม่นยำ และสวยงาม
     2. เพิ่ม Dedicated `useEffect` สำหรับ `[pane0Markers]` ใน `LWChart.tsx` เพื่อสั่งให้ `markersPluginRef.current?.setMarkers(pane0Markers)` รีเฟรชตำแหน่งทันทีที่ผู้ใช้ขยับค่าใน Popover
     3. นำสูตรปรับปรุงนี้ไปใช้ร่วมกันทั้งใน Super Money Signal V3 และ V1 เพื่อให้ความรู้สึกและระยะห่างเป็นมาตรฐานเดียวกัน 100%

2. **เพิ่ม Symbol Option ให้เลือกได้เยอะจุใจ (20+ ตัวเลือก):**
   - เพิ่มตัวเลือกสัญลักษณ์ใน Dropdown แบ่งหมวดหมู่อย่างเป็นระเบียบ:
     - **ลูกศรและทิศทาง (Arrows):** `▲ Arrow Up`, `▼ Arrow Down`, `► Pointer Right`, `⇈ Double Arrow Up`
     - **รูปทรงเรขาคณิต (Classic Shapes):** `● Circle (Filled)`, `○ Circle (Hollow)`, `■ Square (Filled)`, `□ Square (Hollow)`, `◆ Diamond (Filled)`, `◇ Diamond (Hollow)`, `▲ Triangle (Filled)`, `△ Triangle (Hollow)`, `⬡ Hexagon`
     - **ดวงดาวและประกาย (Stars & Glow):** `★ Star (Filled)`, `☆ Star (Hollow)`, `✦ Sparkle Star`
     - **ไอคอนสัญญาณพิเศษ (Action Badges):** `✚ Plus / Cross`, `✖ Multiply / X`, `✔ Checkmark`, `⚡ Lightning Bolt`, `🔥 Momentum Fire`, `🎯 Target Bullseye`, `⚑ Flag Marker`, `💲 Dollar Flow`
   - รองรับทั้ง Native Shapes และ Unicode Glyph Rendering ที่แสดงสีตามที่ผู้ใช้เลือกใน Color Picker ได้ 100%
```

### Artifact: `walkthrough.md`
```markdown
# Walkthrough: Super Money Signal V3 — Vertical Padding & Rich Symbol Catalog

## Changes Completed

1. **Vertical Padding Dynamic Physics & Calibration**:
   - Replaced flawed offset formula with the exact working V1 standard:
     ```ts
     const baseWickOffset = avgRange * 0.45;
     const extraPadding = avgRange * (padding * 0.15);
     const offset = baseWickOffset + extraPadding;
     ```
   - Added instant reactive `useEffect([pane0Markers])` in `LWChart.tsx` so marker position updates on the chart immediately upon slider adjustment.

2. **Expanded 20+ Rich Symbol Catalog**:
   - Added `RICH_SYMBOL_OPTIONS` across 4 categories in `IndicatorManagerPopover.tsx`:
     - **Arrows & Pointers**: ▲ Arrow Up, ▼ Arrow Down, ► Pointer Right, ⇈ Double Up
     - **Classic Shapes**: ● Circle Solid, ○ Circle Hollow, ■ Square Solid, □ Square Hollow, ◆ Diamond Solid, ◇ Diamond Hollow, ▲ Triangle Solid, △ Triangle Hollow, ▼ Triangle Down, ⬡ Hexagon
     - **Stars & Sparkles**: ★ Star Solid, ☆ Star Hollow, ✦ Sparkle Star
     - **Badges & Indicators**: ✚ Plus Cross, ✖ Multiply / X, ✔ Checkmark, ⚡ Lightning Bolt, 🎯 Target, 🔥 Momentum Fire, ⚑ Flag Marker, 💲 Dollar Flow
   - Built hybrid visual resolver (`resolveSuperVisual`): native Lightweight Charts shapes retain hardware acceleration; Unicode glyphs render high-res text markers matched to the signal color.

3. **Deploy & Sync**:
   - Vite build verified (`npm run build`).
   - Cache bumped (`v=20260911_031748`).
   - Deployed to VPS (`185.250.38.247`) and PM2 reloaded.
   - Committed and pushed to `master` branch.
```

---

## ⏱️ Timeline & Debugging Log

- **01:15**: พัฒนาระบบ Trend Speed Analyzer by Zeiierman สำเร็จ ผู้ใช้ทดสอบการแรเงาและแสดงผลบน Sub-pane พอใจ 100%
- **01:45**: ผู้ใช้แจ้งปัญหาเรื่องการกดปิด Sub-pane: "ตอนนี้ปัญหาคือ ปิดทิ้งไม่ได้ จริง เมือกด x ควรปิดทิ้ง ไม่ใช่พับเก็บ"
- **01:50**: แก้ไขระบบ Sub-pane ให้ทำลายหน้าต่างทิ้งจริง (True Pane Destroy) เมื่อกดปุ่ม X พร้อม Re-index Dynamic Sub-Panes อัตโนมัติ ดีพลอยขึ้น VPS และ Git Commit `4d9fb5f9`
- **01:55**: ผู้ใช้ส่งโค้ด Pine Script v6 ของ **FluidTrades - SMC Lite** พร้อมภาพสกรีนช็อต 3 รูป (Inputs, Style, และการแสดงผลบนชาร์ต)
- **02:00 - 02:10**: พัฒนา SMC Lite:
  - สร้าง `src/utils/indicators/smcLite.ts` คำนวณ ATR, Pivots, S/D Zones, BOS, SMAs, Signals, Zigzag
  - สร้าง `src/utils/indicators/smcPrimitive.ts` Custom Primitive บน Canvas สำหรับ Lightweight Charts v5.2.1
  - อัปเดต `LWChart.tsx` เรนเดอร์บน Pane 0 และซิงก์ Markers
  - อัปเดต `IndicatorManagerPopover.tsx` สร้างแท็บ Inputs และ Style ถอดแบบ TradingView 1:1
- **02:11**: รัน `npm run build` ผ่าน 100% (12.22s), รัน `bump-cache.js` ได้ `v=20260911_021143`
- **02:12**: อัปโหลด SCP ไปยัง VPS `root@185.250.38.247:/root/stock-portfolio/dist/`, สั่ง `pm2 reload stock-api`, และ Git Commit & Push `6ff1ab89` สำเร็จ 100%
- **02:15**: ผู้ใช้ส่งโค้ด Pine Script v5 ของ **Anchored VWAP** พร้อมสกรีนช็อต 3 รูป
- **02:20**: พัฒนา Anchored VWAP (`src/utils/indicators/anchoredVWAP.ts`), สร้างแท็บ Inputs & Style, ซิงก์บน Pane 0 และ Anchor Marker ดีพลอยขึ้น VPS เรียบร้อย
- **02:35**: ผู้ใช้ส่งโค้ด Pine Script v6 ของ **Super Money Screener V3** เพื่อสร้างอินดิเคเตอร์ **Super Money Signal V3**
- **02:40 - 02:50**: วิเคราะห์ตรรกะการนับแท่งแดง/เขียว และแก้จุดบกพร่องเรื่องการสั่ง Reset เป็น 0 ไวกว่ากำหนด เพื่อป้องกันสัญญาณหลอนหรือพลาดจุดเข้าคอนเฟิร์ม
- **02:55**: พัฒนา `src/utils/indicators/superMoneySignal.ts`, เพิ่มการตั้งค่าใน `indicatorConfig.ts`, และเชื่อมต่อ Markers บน Pane 0 ของ `LWChart.tsx`
- **03:05**: ผู้ใช้สั่งเพิ่มออปชัน: Signal label on/off, Vertical padding slider, Size slider, และ Symbol dropdown
- **03:10**: ผู้ใช้แจ้งปัญหา Vertical padding ไม่ขยับตามที่ต้องการ พร้อมสั่งให้ปรับสูตรฟิสิกส์ให้เป็นมาตรฐานเดียวกับ V1 และเพิ่มตัวเลือก Symbol ให้เยอะๆ ("g-upo[iua,kg]p" -> "เขียนบรีฟมาเลย")
- **03:13**: วิเคราะห์สาเหตุของปัญหา Padding (Sub-pixel issue บน Price coordinate และขาด Reactive effect สำหรับ markers) ออกแบบสูตรใหม่ที่มี Base Clearance + Calibrated Extra Padding
- **03:15**: แก้ไข `LWChart.tsx`, `IndicatorManagerPopover.tsx`, และ `indicatorConfig.ts`:
  - ใช้สูตร `baseWickOffset = avgRange * 0.45; extraPadding = avgRange * (padding * 0.15)`
  - เพิ่ม `useEffect([pane0Markers])` ให้กราฟอัปเดตตำแหน่งทันทีแบบ Reactive
  - เพิ่มคลังสัญลักษณ์ 23 รูปทรง แยก 4 หมวด `<optgroup>` พร้อมระบบเรนเดอร์ Canvas / Unicode Glyph
- **03:17**: รัน `npm run build` ผ่านสมบูรณ์ (11.87s), รัน `bump-cache.js` ได้ `v=20260911_031748`
- **03:18**: ดีพลอยขึ้น VPS ผ่าน SCP และสั่ง `pm2 reload stock-api` สำเร็จ, Git Commit & Push ขึ้น master (`cb520358`)
- **03:22**: ผู้ใช้ขอให้อธิบายตรรกะเบื้องหลังสัญญาณ Super Money Signal V3 แบบเข้าใจง่ายในเหตุการณ์จริง (`vTb[kp สนเรแ` -> `อธิบาย logic`) อธิบายอย่างละเอียดถึงพฤติกรรม Banker แอบเก็บของ (ศรขาว), Hot Money ช่วยจุดพลุคอนเฟิร์มรอบใหญ่ (สามเหลี่ยมเหลือง), และการเทขายทิ้งของเจ้ามือ (ศรแดง)
- **03:25**: ผู้ใช้สั่ง `/save` เพื่อบันทึกความคืบหน้าของระบบทั้งหมดเข้าสู่ระบบ Quick Save ถาวร

---

## 🔗 GBRAIN Backlinks

### depends_on
- **2026-09-10 20:55** | [X-Chart Dynamic Multi-Pane Engine, Ultimate RSI, Pane Reordering & Floating Toolbar (V2.17.0)](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.17.0_%5Bimpl%5D_ui_xchart-multi-pane-ultimate-rsi-reorder-and-floating-toolbar.md) -- โครงสร้าง Multi-Pane และ Floating Toolbar ที่ถูกนำมาต่อยอดเป็นระบบ True Close
- **2026-09-10 19:10** | [X-Chart USD/THB 4H Timeframe & Preferences Toggle (V2.16.0)](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.16.0_%5Bimpl%5D_ui_xchart-usd-thb-4h-timeframe-and-preferences-toggle.md) -- สถาปัตยกรรม X-Chart Terminal

### enables
- **2026-09-11 03:25** | [Super Money Signal V3, Vertical Padding & Rich Symbol Catalog (V2.18.0)](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.18.0_%5Bimpl%5D_ui_fluidtrades-smc-lite-and-dynamic-pane-close.md) -- ระบบมาร์กเกอร์และสัญลักษณ์อัจฉริยะ 23 แบบสำหรับ Indicator ทุกตัวในอนาคต

### related_to
- **2026-09-10 02:30** | [TradingView-Style Indicator Manager, Dynamic EMA & Max Lifetime Backfill (V2.14.0)](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.14.0_%5Bimpl%5D_ui_tradingview-indicator-manager-and-max-lifetime-backfill.md) -- สถาปัตยกรรม Indicator Manager Popover

