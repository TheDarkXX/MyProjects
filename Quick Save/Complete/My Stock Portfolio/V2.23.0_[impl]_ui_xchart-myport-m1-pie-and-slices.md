---
id: V2.23.0
title: X-Chart 'My Port' M1 Finance Interactive Canvas & Slices Revamp
type: impl
component: ui
category: system-architecture
created: 2026-09-13 10:10
author: Antigravity AI (มารบูรพา 🔥)
conversation: 65ebad73-0439-4455-be1b-ec2764ff63b5
tags: [xchart, myport, m1-finance, interactive-pie, slices-table, drift-indicator, bento-grid, holding-drawer]
aliases: [xchart_myport_m1, m1_pie_and_slices, portfolio_slices_canvas]
---

# 🚀 V2.23.0: X-Chart 'My Port' M1 Finance Interactive Canvas & Slices Revamp

> **Mission**: ยกเครื่องแท็บ `My Port` ภายในระบบ X-Chart Multi-tab ทั้งหมด จากเดิมที่แสดงกราฟแท่งเทียนแท่งเดี่ยวซ้ำซ้อนกับแท็บอื่น มาเป็น **M1 Finance Interactive Portfolio Command Canvas** ที่ผสาน **Interactive Donut Wheel (Pies & Slices)**, **Dynamic Center Hub**, **Pro Slices Holdings Table with Drift Tracking**, **Bento Glassmorphic Cards**, และ **Slide-over Quick Inspect Drawer** พร้อมปุ่มลัดเปิดดูกราฟแท่งเทียนตัวเต็มในคลิกเดียว

---

## 🧠 Context & Implementation (Compiled Truth)

### 1. ปัญหาเดิมและวิวัฒนาการสู่ M1 Finance Paradigm
- **ปัญหาเดิม:** ในหน้า `My Port` แท็บถูกเขียนให้เรนเดอร์ Candlestick Chart ของหุ้นตัวเดียว ทำให้เสียพื้นที่ และผู้ใช้ไม่สามารถเห็นภาพรวมของสินทรัพย์ทั้งหมดในพอร์ต
- **The M1 Finance Paradigm:**
  1. **The Iconic Interactive Donut Wheel:** ชิ้นพายคำนวณจาก **Actual Holdings Value ($)** ณ วินาทีจริง
  2. **Dynamic Center Hub:** เมื่อไม่มีการชี้ จะสรุปยอดพอร์ตรวม (Total Net Worth, Today's P&L, Total Return) แต่เมื่อเมาส์ชี้ Slice ไหน ชิ้นเค้กจะขยายขึ้น +8px เรืองแสง Glow และ Center Core จะสลับแสดงสถิติเจาะลึกของหุ้นตัวนั้นแบบ Real-time
  3. **Segmented Linear Allocation Bar:** หลอดแสดงสัดส่วนพอร์ตแนวนอน multi-color ใต้ Donut มองปราดเดียวรู้สัดส่วนพอร์ตทั้งหมด
  4. **Two-Way Hover Sync:** ชี้ในวงล้อ -> ไฮไลต์แถวในตาราง / ชี้ในตาราง -> ไฮไลต์ชิ้นเค้กบนวงล้อ
  5. **Slices Holdings Table:** แสดง 6 คอลัมน์หลัก สะอาดตา ไม่รก แสดง Actual Weight % เทียบ Target Weight % พร้อมป้ายกำกับ **Drift** (`+Over` / `-Under`)
  6. **Triple View Modes:** สลับมุมมองได้ 3 แบบตามต้องการ (`M1 Split`, `Full Table`, `Bento Cards`)
  7. **Slide-over Holding Drawer:** เลื่อนเปิดจากขอบขวาเมื่อคลิกหุ้น แสดงประวัติไม้ที่ซื้อทั้งหมด (Trade Lots) และเป้ากลยุทธ์ Blueprint

---

### 2. สถาปัตยกรรมและการไหลเวียนของข้อมูล (Data Architecture)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             useHoldings Hook                                │
│       (Aggregates Portfolio Transactions, Blueprints, & Live Prices)        │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                 MyPortTab.tsx (Master Canvas Orchestrator)                   │
│   - Calculates: slices[], totalNetWorth, totalPnl, todaysProfit, drift      │
│   - Manages: viewMode ('split' | 'table' | 'bento'), hoveredSymbol, drawer  │
└──────┬──────────────────────┬────────────────────────┬──────────────────────┘
       │                      │                        │
       ▼                      ▼                        ▼
┌───────────────┐     ┌────────────────┐      ┌───────────────────────────────┐
│MyPortKPIHeader│     │ MyPortM1Pie    │      │ MyPortTableView / BentoGrid   │
│- Net Worth    │     │ - Donut Wheel  │      │ - Consolidated 6 Cols Grid    │
│- Today P&L    │     │ - Center Core  │      │ - Drift Badges & Filters      │
│- Total Return │     │ - Segmented Bar│      │ - De-nested Bento Cards       │
│- Cash Cushion │     │ - Tag Chips    │      │ - Actions (Inspect / Chart)   │
└───────────────┘     └────────────────┘      └───────────────┬───────────────┘
                                                              │
                                                              ▼
                                              ┌───────────────────────────────┐
                                              │ MyPortHoldingDrawer.tsx       │
                                              │ - Cost basis & share count    │
                                              │ - Blueprint Strategy Target   │
                                              │ - Full Trade Lots Exec History│
                                              └───────────────────────────────┘
```

---

## 📁 Files Changed This Session

| File | What Changed | Why |
|---|---|---|
| `My Stock Portfolio/src/components/xchart/myport/MyPortKPIHeader.tsx` | ยุบ 5 กล่องเป็น Bloomberg Single-row Strip, เชื่อมโยงสวิตช์ Currency `$ USD` / `฿ THB`, และแก้ `clsx` import | ลด visual density และสลับค่าเงินทั่วทั้งหน้าจอได้แบบ Real-time |
| `My Stock Portfolio/src/components/xchart/myport/MyPortM1Pie.tsx` | ขยายสัดส่วน Donut Wheel (`innerRadius: 114`, `outerRadius: 166`, `h-[370px] xl:h-[390px]`), ใส่ Ambient Glow, เพิ่ม Segmented Linear Bar | ให้สมดุลกับพื้นที่ฝั่งซ้ายที่ขยายเป็น 2/5 (40%) |
| `My Stock Portfolio/src/components/xchart/myport/MyPortTableView.tsx` | รวม Search + Filter, ลดเหลือ 6 คอลัมน์หลัก, เชื่อมต่อ `formatPriceVal` และปรับตัวเลขเป็น `font-normal text-slate-200` | แสดงราคาต่อหุ้นตามสกุลเงินที่เลือก และแก้ปัญหาตาล้าจากการเน้นตัวหนามากเกินไป |
| `My Stock Portfolio/src/components/xchart/myport/MyPortBentoGridView.tsx` | รื้อกรอบซ้อนกล่อง (Box-in-a-box) ทั้งหมด จัด Typography เรียบหรู และเชื่อมต่อ `formatPriceVal` | ให้ราคาและข้อมูลใน Bento Card สอดคล้องกับค่าเงินที่เลือก |
| `My Stock Portfolio/src/components/xchart/myport/MyPortHoldingDrawer.tsx` | เพิ่มจำนวน Shares ต่อท้าย Avg Cost (`Avg $X · Y shares`) | รองรับข้อมูลที่ย้ายมาจากตารางหลักอย่างสมบูรณ์ |
| `My Stock Portfolio/src/components/xchart/myport/MyPortTab.tsx` | ปรับขนาดฝั่งซ้ายเป็น `w-full lg:w-[40%] xl:w-[40%] 2xl:w-[40%]` (2/5 หน้าจอ) และแก้ `addTab({ type: 'STOCK' })` | ขยายวง Donut ให้สมส่วนและเปิดกราฟแท่งเทียนได้ถูกต้อง |
| `My Stock Portfolio/src/components/xchart/myport/types.ts` | เพิ่มฟังก์ชัน `formatPriceVal` และ `formatSecondaryPriceVal` (ทศนิยม 2 ตำแหน่ง) | รองรับการแปลงราคาต่อหุ้นตาม FX Rate สด |
| `My Stock Portfolio/src/hooks/useHoldings.ts` | ดักกรอง `t.portfolio_id === activePortfolioId` | แก้บักสลับพอร์ตแล้วธุรกรรมและยอดเงินปนกัน |
| `My Stock Portfolio/src/components/xchart/XChartWatchlistDock.tsx` | ลบปุ่ม Sparkles ✨ และ Refresh 🔄, เพิ่ม Twin-Pill Tab Switcher `Watchlist (87)` vs `My Port (N)`, บันทึกสถานะ Dock ลง `localStorage` | ขจัดความสับสนและเพิ่มการสลับดูหุ้นที่ถือครองจริงบน Dock ด้านขวา |
| `My Stock Portfolio/src/components/xchart/myport/MyPortWatchlist.tsx` | ปรับ Container ยืดเต็มความสูง Dock, เชื่อมโยง Currency Format และซิงก์คลิกแถวหุ้นเปลี่ยนกราฟ Candlestick หน้าหลักทันที | ให้ใช้งานเป็น Dock View ในหน้าหลักได้อย่างสมบูรณ์แบบ |
| `My Stock Portfolio/index.html` | อัปเดต Cache Buster `v=20260913_124053` | บังคับเบราว์เซอร์โหลด Assets ล่าสุด ป้องกันบั๊กจำแคช |

---

## 📜 RAW ARTIFACT BACKUP (Iron Rule)

<details>
<summary><b>Artifact 1: implementation_plan.md (คลิกเพื่อดูเนื้อหาดิบทั้งหมด)</b></summary>

```markdown
# V2.23.2 — UI Elegance & Clarity Refinement: My Port M1 Finance Canvas

> **มหาเทพ's Diagnosis**: ปัญหาไม่ได้อยู่ที่ฟีเจอร์ขาด แต่อยู่ที่ **ข้อมูลทุกชิ้นมี visual weight เท่ากันหมด** ตาผู้ใช้ไม่มีจุดพัก ไม่มี focal point ที่ชัดเจน สิ่งที่ต้องทำไม่ใช่เพิ่มอะไรเข้าไป แต่คือ **ลดเสียงรบกวนออก** แล้วให้ตัวเลขสำคัญส่งเสียงดังขึ้น

---

## 🔍 Root Cause Analysis (จาก Screenshot จริง)

### Problem 1: KPI Header Strip — "ของชิดจนหายใจไม่ออก"
- **สิ่งที่เห็น**: KPI กล่อง 5 ก้อน + Portfolio Selector + View Mode Switcher + Badge "M1 Interactive Canvas" ยัดอยู่ในแถบบรรทัดเดียว
- **Blast Radius**: แถบบนกินพื้นที่แนวตั้ง ~70px แต่ข้อมูลแน่นจน label ตัวเล็กอ่านไม่ทัน
- **Root Cause**: ทุก KPI อยู่ใน `border box` ที่เหมือนกันหมด → ไม่มี hierarchy → ตาวิ่งไปวนมา

### Problem 2: Table — "8 คอลัมน์เยอะเกินไปสำหรับ Split View"
- **สิ่งที่เห็น**: ในโหมด Split View (ด้านขวาที่มีตาราง) ยัด 8 คอลัมน์ลงไป: Symbol, Weight, Price, Cost, Value, P&L, Blueprint, Actions
- **Blast Radius**: ข้อความในแต่ละเซลล์ถูกบีบจนอ่านยาก ตัวเลขชิดกัน
- **Root Cause**: ไม่ได้แยก columns ตาม context — ในโหมด Split View ควรลดเหลือ 5-6 คอลัมน์หลัก ส่วน Blueprint + Cost ให้ไปอยู่ใน Drawer แทน

### Problem 3: Donut Legend Grid ล่าง — "ซ้ำซ้อนกับตาราง"
- **สิ่งที่เห็น**: Legend Grid 2 คอลัมน์ใต้ Donut แสดง Symbol + % + Value + Return% — ข้อมูลเดียวกับที่ตารางด้านขวาแสดงอยู่แล้ว
- **Root Cause**: ออกแบบ Legend เหมือนมันเป็น standalone view แต่จริง ๆ ตอน Split View มันอยู่ข้าง ๆ ตารางอยู่แล้ว

### Problem 4: Filter Chips Bar — "กินพื้นที่แนวตั้งโดยไม่จำเป็น"
- **สิ่งที่เห็น**: Search + Filter Chips (`All`, `Winners`, `Drawdowns`, `Overweight`, `Underweight`) อยู่ในแถบแยกกินพื้นที่ ~50px ก่อนถึง table header
- **Root Cause**: กรอง Filter มีข้อความยาว ปุ่ม icon + text เรียงยาว

### Problem 5: Visual Uniformity — "ไม่มีอะไรที่ดูพิเศษ"
- **สิ่งที่เห็น**: ทุก card, KPI box, pill, badge ใช้ `bg-[#141824] border border-slate-800 rounded-xl` เหมือนกันหมด → เฟลต, ไม่มีความลึก
- **Root Cause**: ขาด layering depth ที่ต่างกัน และขาด subtle gradient / glow ที่บอกว่า "ตรงนี้สำคัญกว่าตรงนั้น"

---

## 📋 Proposed Changes (Surgical Precision)

### Component 1: `MyPortKPIHeader.tsx` — **Slim Down & Breathe**

#### [MODIFY] MyPortKPIHeader.tsx
- **ลดกล่อง KPI จาก 5 กล่องแยก → Single-Row Inline Flow** — ถอด border box ออก เหลือแค่ตัวเลขเรียงตัวกัน คั่นด้วย `·` divider แบบ Bloomberg Terminal
- **รวม Portfolio Name + Net Worth เข้าด้วยกัน** — ชื่อพอร์ตเป็น label ยอดเงินเป็น hero number
- **ย้าย Winners/Drawdowns tally → ลง Donut legend หรือซ่อน** — มันไม่สำคัญพอจะอยู่ top-level
- **ลบ Badge "M1 Interactive Canvas"** — มึงรู้อยู่แล้วว่ามันคืออะไร ป้ายนี้ไม่ได้ช่วยใคร
- **ลบ Subtitle "Portfolio Overview & Slices Allocation"** — ซ้ำซ้อน
- **เป้าหมาย**: KPI strip สูงไม่เกิน **40-44px** (จากเดิม ~70px)

### Component 2: `MyPortM1Pie.tsx` — **Donut First, Legend Minimal**

#### [MODIFY] MyPortM1Pie.tsx
- **ลบ Legend Grid 2 คอลัมน์ออกทั้งหมด** เมื่ออยู่ในโหมด Split View (ข้อมูลอยู่ในตารางด้านขวาอยู่แล้ว)
- **เปลี่ยนเป็น Inline Color Dots แถวเดียว** ใต้ Donut แทน — แค่ `● VRT  ● NVDA  ● CRWD  ● META  ● MELI  ● RBRK  ● CASH`
- **ขยาย Donut ให้กินพื้นที่มากขึ้น** — เพิ่ม outerRadius จาก 135 → 150 และ innerRadius จาก 90 → 100 ให้มัน breathe มากขึ้น
- **เพิ่ม outerRadius ให้ donut ใหญ่ขึ้น** — ลดเส้นผ่านศูนย์กลางว่างด้านใน เพื่อให้ตัวเลขใน Center Core อ่านง่ายขึ้น
- **ลบ header "M1 INTERACTIVE DONUT WHEEL" ออก**
- **เพิ่ม subtle radial gradient glow** รอบ Donut เป็น ambient background

### Component 3: `MyPortTableView.tsx` — **Column Consolidation**

#### [MODIFY] MyPortTableView.tsx
- **ลดจาก 8 คอลัมน์ → 6 คอลัมน์** ในโหมด Split View:
  1. **Symbol & Category** (รวม color dot)
  2. **Weight** — Actual % เท่านั้น (Target + Drift จะแสดงเป็น sub-text ถ้ามี)
  3. **Market Price & 24h** — รวมเป็น 1 cell: `$86.65` ↓ `-2.54%`
  4. **Holding Value** — `$783.09` ↓ `≈ ฿27,017`
  5. **Total P&L** — `+$83.33` ↓ `+11.91%` (หรือแดง)
  6. **Actions** — ปุ่ม `Inspect` + `Chart` icons only (ไม่มี text)
- **ถอดคอลัมน์ออก**: `Avg Cost / Shares` (ไปอยู่ Drawer), `Blueprint Milestone` (ไปอยู่ Drawer)
- **ย้าย Filter Chips ไปอยู่ในบรรทัดเดียวกับ Search** — ลด vertical footprint
- **Filter Chips ใช้ icon-only** — `📈`, `📉`, `⚖️`, `🎯`
- **Action Buttons → Icon-only** — Eye icon + Chart icon เท่านั้น ขนาด 28x28px ปุ่มกลม

### Component 4: `MyPortHoldingDrawer.tsx` — **Minor Polish**

#### [MODIFY] MyPortHoldingDrawer.tsx
- เพิ่ม **Avg Cost / Shares** section ที่ชัดเจนขึ้น (ย้ายมาจากคอลัมน์ตาราง)

### Component 5: `MyPortTab.tsx` — **Layout Ratio Adjustment**

#### [MODIFY] MyPortTab.tsx
- **ปรับ Donut Column Width** จาก `lg:w-[420px] xl:w-[460px]` → `lg:w-[360px] xl:w-[400px]`
```
</details>

<details>
<summary><b>Artifact 2: walkthrough.md (คลิกเพื่อดูเนื้อหาดิบทั้งหมด)</b></summary>

```markdown
# Walkthrough - X-Chart 'My Port' Bento Grid & Donut Segmented Bar Polish (V2.23.3)

Completed the follow-up design upgrades:
1. **Bento Cards View Polish**: Eradicated "box-in-a-box syndrome", delivering an ultra-slick, cohesive financial card interface.
2. **Segmented Linear Allocation Bar**: Added an interactive multi-color allocation progress bar directly underneath the Donut Wheel, providing an instant visual bridge between the wheel and the slice tags.

---

## 💎 Specific Improvements

### 1. MyPortBentoGridView.tsx — Pure Card Cohesion
- **Eliminated Nested Box Fatigue**: Removed 3 layers of sub-boxes inside each card. Instead of enclosing Price, Value, and Target inside their own borders, they now flow naturally with unified padding and clear typography hierarchy.
- **Clear Hero Metric**: Positioned the current holding value `$X,XXX` and unrealized P&L pill in a balanced, breathable horizontal layout.
- **Integrated Market & Drift Row**: Price `$86.65 (-2.54%)` and drift badge `+3.1% Over` are cleanly separated by a hairline divider.
- **Sleek Micro Progress Bar**: 4px subtle rounded bar tracking actual weight vs target.

### 2. MyPortM1Pie.tsx — Segmented Linear Allocation Bar
- **Multi-color Proportion Bar**: Positioned directly under the Donut Wheel. Shows the relative scale of all holdings at a single glance (similar to Apple Health / GitHub language bars).
- **Interactive Synchrony**: Hovering any section of the bar dims other slices, expands the corresponding donut segment, and highlights the central metric core.

---

## 🚀 Deployment Status

- **Build**: 0 errors (`built in 23.96s`)
- **Cache Buster**: `v=20260913_103114`
- **VPS Deploy**: Synced `dist/*` to `185.250.38.247:/root/stock-portfolio/dist/`
- **PM2**: `stock-api` reloaded successfully
- **Git**: Commit `3fbe646` pushed to `master`
```
</details>

<details>
<summary><b>Artifact 3: implementation_plan.md V2.23.5 (คลิกเพื่อดูเนื้อหาดิบทั้งหมด)</b></summary>

```markdown
# Implementation Plan - X-Chart 'My Port' UI Refinement & Typography Polish (V2.23.5)

ปรับปรุง 3 จุดยุทธศาสตร์ของหน้า **X-Chart 'My Port' M1 Finance Canvas** ตามข้อเสนอแนะของผู้ใช้ เพื่อเพิ่มความสอดคล้องของระบบ Currency, ขยายสัดส่วน Allocation Wheel ให้เด่นชัดระดับ 2/5 หน้าจอ, และจัดระเบียบ Hierarchy ของฟอนต์ในตาราง ลดความล้าของสายตา

---

## User Review Required

> [!IMPORTANT]
> **การสลับ Currency ของคอลัมน์ Price/24h:**
> กูแนะนำให้ **สลับ Currency 100%** เพื่อความ Consistent ทั้งหน้าจอ โดยจัด Layout 2 ชั้นเหมือนคอลัมน์ Holding Value:
> - **บรรทัดบน:** ราคาปัจจุบันตามสกุลเงินที่เลือก (เช่น `฿1,213.02` หรือ `$35.16` ทศนิยม 2 ตำแหน่งคงที่) ด้วย `font-normal`
> - **บรรทัดล่าง:** ราคาสกุลเงินรองในวงเล็บ `($35.16)` พร้อมตัวเลข 24h Change `+0.86%` ในบรรทัดเดียวกันอย่างกะทัดรัด
> รูปแบบนี้ทำให้นักลงทุนเช็คได้ทั้งมูลค่ารวมและราคาต่อหุ้นเป็นเงินบาททันที ไม่ต้องมานั่งคูณในหัว!

---

## Proposed Changes

### Component 1: Central Currency Formatters

#### [MODIFY] types.ts
- เพิ่มฟังก์ชัน `formatPriceVal(val, currency, exchangeRate)`: ฟอร์แมตราคาหุ้นโดยบังคับแสดงทศนิยม 2 ตำแหน่งเสมอ (`฿1,213.02` หรือ `$35.16`) ต่างจาก `formatCurrencyVal` ของมูลค่าพอร์ตที่เงินบาทจะปัดเศษจำนวนเต็ม
- เพิ่มฟังก์ชัน `formatSecondaryPriceVal(val, currency, exchangeRate)` สำหรับราคาอ้างอิงในวงเล็บ

---

### Component 2: Layout Rebalancing (2/5 vs 3/5)

#### [MODIFY] MyPortTab.tsx
- ปรับ Container ฝั่งซ้ายใน Split View:
  - จากเดิม: `w-full lg:w-[360px] xl:w-[400px] 2xl:w-[440px]` (กว้างแค่ ~23-27% ของจอ)
  - ปรับเป็น: `w-full lg:w-[40%] xl:w-[40%] 2xl:w-[40%]` (40% หรือ 2/5 ของหน้าจอเป๊ะๆ)
- ฝั่งขวา (Table): คงเป็น `flex-1` ซึ่งจะกินพื้นที่ 60% (3/5) สอดรับกันพอดี

#### [MODIFY] MyPortM1Pie.tsx
- เมื่อฝั่งซ้ายกว้างขึ้น 2/5:
  - ขยายความสูง Container ชาร์ตจาก `h-[330px]` เป็น `h-[370px] xl:h-[390px]`
  - ปรับขนาด Donut Radius:
    - `innerRadius`: ขยายจาก `96` เป็น `116`
    - `outerRadius`: ขยายจาก `142` เป็น `168`
  - Center Hub Metrics (Net Worth, Return Badge) จะมีพื้นที่หายใจกว้างขึ้น โอ่อ่า ไม่อึดอัด
  - Segmented Linear Allocation Bar ด้านล่างจะกว้างขึ้น มองเห็นสัดส่วนได้ชัดเจนขึ้น

---

### Component 3: Data Grid Typography & Price Currency

#### [MODIFY] MyPortTableView.tsx
- **1. คอลัมน์ Price / 24h:**
  - บรรทัดบน: แสดง `formatPriceVal(slice.lastPrice, currency, exchangeRate)`
  - บรรทัดล่าง: แสดง `formatSecondaryPriceVal` ควบคู่กับเปอร์เซ็นต์ 24h Change (`+0.86%`)
- **2. จัดระเบียบ Typography Hierarchy (แก้ตาล้า):**
  - **Anchor หลักคงความหนา:** รหัสหุ้น (`slice.symbol`) ยังคงเป็น `font-bold text-white` เด่นชัดที่สุดเพื่อเป็นหมุดนำสายตา
  - **ปลดความหนาของตัวเลขทั้งหมดให้เป็น `font-normal text-slate-200`:**
    - `Weight`: จาก `font-black` (น้ำหนัก 900) -> เปลี่ยนเป็น `font-normal text-slate-200 font-mono`
    - `Drift badge`: ปรับเป็น `font-medium`
    - `Price`: จาก `font-bold` -> เปลี่ยนเป็น `font-normal text-slate-200 font-mono`
    - `Holding Value`: จาก `font-extrabold` (น้ำหนัก 800) -> เปลี่ยนเป็น `font-normal text-slate-200 font-mono` (ซับเท็กซ์ `font-normal text-slate-400`)
    - `Total P&L`: จาก `font-bold` -> เปลี่ยนเป็น `font-normal font-mono`, Badge เปอร์เซ็นต์ปรับเป็น `font-medium`
```
</details>

<details>
<summary><b>Artifact 4: walkthrough.md V2.23.5 (คลิกเพื่อดูเนื้อหาดิบทั้งหมด)</b></summary>

```markdown
# Walkthrough - X-Chart 'My Port' UI Refinement & Typography Polish (V2.23.5)

สำเร็จเรียบร้อยตามแผนบรีฟ! ทำการปรับแต่ง 3 จุดหลักของหน้า **X-Chart 'My Port' M1 Finance Canvas**:

---

## 💎 สรุปการปรับแต่งที่สำเร็จ

### 1. เชื่อมโยงระบบ Currency เข้ากับคอลัมน์ Price / 24h
- เพิ่มฟังก์ชัน `formatPriceVal` และ `formatSecondaryPriceVal` ใน types.ts ให้ฟอร์แมตทศนิยม 2 ตำแหน่งคงที่เสมอ
- ใน MyPortTableView.tsx:
  - **บรรทัดบน:** แสดงราคาตามสกุลเงินที่เลือก เช่น เมื่อเลือก THB จะแสดง `฿1,213.02`
  - **บรรทัดล่าง:** แสดงราคาสกุลเงินรองในวงเล็บ `($35.16)` พร้อมตัวเลข 24h Change `+0.86%` ในบรรทัดเดียว
- ซิงก์การฟอร์แมตราคาไปยัง MyPortBentoGridView.tsx ให้ตอบสนอง Currency Toggle ทันที

### 2. ขยายสัดส่วน Allocation Wheel ฝั่งซ้ายเป็น 2/5 (40%) ของหน้าจอ
- ใน MyPortTab.tsx: ปรับ Split View ฝั่งซ้ายให้เป็น `lg:w-[40%] xl:w-[40%] 2xl:w-[40%]` (2/5 หน้าจอ) ปลดล็อกพื้นที่ที่เคยแคบแค่ 23-27%
- ใน MyPortM1Pie.tsx:
  - ขยายความสูง Container ชาร์ตเป็น `h-[370px] xl:h-[390px]`
  - ขยายรัศมี Donut Wheel: `innerRadius={114}`, `outerRadius={166}`
  - เพิ่มพื้นที่ภายใน Center Hub Core ทำให้ตัวเลข Net Worth และ Return Badge หายใจได้โล่ง โอ่อ่า สวยงาม

### 3. จัดระเบียบ Typography ในตารางด้านขวา (ลดความล้าสายตา)
- ใน MyPortTableView.tsx:
  - **Anchor หลัก:** รหัสหุ้น (`slice.symbol`) คงความหนา `font-bold text-white` เป็นหมุดนำสายตาจุดเดียว
  - **ปลดความหนาของตัวเลขข้อมูลทั้งหมด:**
    - `Weight`: จาก `font-black` (900) -> `font-normal text-slate-200 font-mono`
    - `Target weight` & `Cash`: ปรับเป็น `font-normal`
    - `Drift badge`: ปรับเป็น `font-medium`
    - `Price`: จาก `font-bold` -> `font-normal text-slate-200 font-mono`
    - `Holding Value`: จาก `font-extrabold` (800) -> `font-normal text-slate-200 font-mono` (ซับเท็กซ์ `font-normal text-slate-400`)
    - `Total P&L`: จาก `font-bold` -> `font-normal font-mono`, Badge เปอร์เซ็นต์ปรับเป็น `font-medium`

---

## 🚀 สถานะการ Deploy ขึ้นระบบจริง

1. **Build**: `vite build` สำเร็จ 100% ใน 14.34s ไร้ข้อผิดพลาด
2. **Cache Buster**: อัปเดตเวอร์ชันแคชเป็น `v=20260913_121913`
3. **VPS Sync**: ส่งไฟล์ขึ้น `185.250.38.247:/root/stock-portfolio/dist/` เรียบร้อย
4. **PM2 Reload**: รีสตาร์ท `stock-api` บน VPS เรียบร้อย
5. **Git Push**: Commit `98bc482` (พร้อม Hotfix คืนชีพ `import clsx`) push ขึ้น GitHub `master` เรียบร้อย
```
</details>

<details>
<summary><b>Artifact 5: implementation_plan.md V2.23.6 Right Dock Tab Switcher (คลิกเพื่อดูเนื้อหาดิบทั้งหมด)</b></summary>

```markdown
# Implementation Plan - X-Chart Dock: Clean Header & Add 'My Port' Tab Switcher

อัปเดต Header ของ Right Dock ในหน้า X-Chart:
1. **ลบปุ่มรูปดาว ✨ และปุ่มรีเฟรช 🔄 ออกถาวร** ตามคำขอเพื่อลดความสับสนและคลีนหน้าจอ
2. **เพิ่ม Tab Switcher บนหัว Dock** ให้สลับระหว่าง **`Watchlist`** และ **`My Port`** ได้อย่างอิสระในคลิกเดียว

---

## User Review Required

> [!IMPORTANT]
> **สถาปัตยกรรม Dock Tab Switcher ที่ออกแบบ:**
> 
> ```
> ┌────────────────────────────────────────────────────────────────────────┐
> │  [ 📋 Watchlist (87) ] [ 💼 My Port (3) ]                   +   [+]   >│
> └────────────────────────────────────────────────────────────────────────┘
> ```
> - **เมื่อเลือกแท็บ `Watchlist`:**
>   - แสดงลิสต์ 87 หุ้นแบ่ง 6 หมวดหมู่ (STOCKS, STRONG GROWTH, ฯลฯ) พร้อมตัวกรองและการค้นหาเดิม
>   - ปุ่มเครื่องมือฝั่งขวา: `+` (เพิ่มหุ้น), `[+]` (เพิ่มหมวดหมู่), `>` (ย่อ Dock)
> - **เมื่อเลือกแท็บ `My Port`:**
>   - แสดงลิสต์หุ้นที่ผู้ใช้ **ถือครองจริงในพอร์ต (Actual Holdings)** จาก `useHoldings()`
>   - มี Dropdown สลับพอร์ต, สรุปมูลค่าพอร์ตรวม, กำไร/ขาดทุนรวม (Total Unrealized P&L), และน้ำหนักพอร์ต
>   - **คลิกหุ้นตัวไหนใน My Port -> กราฟแท่งเทียน Candlestick บนหน้าหลักจะสลับไปที่หุ้นตัวนั้นทันที!**

---

## Proposed Changes

### Component 1: XChartWatchlistDock.tsx

#### [MODIFY] XChartWatchlistDock.tsx
- เพิ่ม State: `dockTab: 'watchlist' | 'myport'` (ดึง/บันทึกลง `localStorage` อัตโนมัติเพื่อจำสถานะ)
- เชื่อมต่อ `useHoldings` เพื่อนับจำนวนหุ้นที่ถือครองจริง (`portHoldingsCount`)
- **Header:**
  - เปลี่ยนจากตัวหนังสือเดี่ยว `Watchlist (87)` เป็น **Twin Pill Segmented Switcher**:
    - ปุ่ม `Watchlist` (แสดง Badge จำนวน 87)
    - ปุ่ม `My Port` (แสดงไอคอน `Briefcase` + Badge จำนวนหุ้นในพอร์ต)
  - **ลบปุ่มรูปดาว (`Sparkles`) และปุ่ม Refresh (`RefreshCw`) ออกทั้งหมด**
  - ปุ่ม `+` และ `[+]` จะแสดงเฉพาะเมื่ออยู่แท็บ `Watchlist`
  - ปุ่ม `>` (Collapse) จะแสดงตลอดเวลา
- **Body:**
  - ถ้า `dockTab === 'watchlist'`: แสดง Watchlist เดิม
  - ถ้า `dockTab === 'myport'`: เรนเดอร์ `<MyPortWatchlist selectedSymbol={activeSymbol} onSelectSymbol={handleStockClick} />`

---

### Component 2: MyPortWatchlist.tsx (Responsive Container & Design Polish)

#### [MODIFY] MyPortWatchlist.tsx
- ปรับ Container จาก `w-[340px] border-l` เดิม ให้เป็น `w-full h-full flex-1` เพื่อให้สวมเข้ากับ Resizable Dock ด้านขวาอย่างไร้รอยต่อ
- ปรับ Typography และ Color Contrast ให้สอดคล้องกับมาตรฐาน Iron Rules (ฟอนต์ >= 13px, Contrast >= 70%)

---

## Verification Plan

### Automated Tests
- Type checking: `npx.cmd tsc --noEmit`
- Production build: `npm.cmd run build`

### Manual Verification
- ตรวจสอบว่าปุ่มดาว ✨ และปุ่มรีเฟรช 🔄 หายไปแล้ว
- กดสลับระหว่างแท็บ `Watchlist` และ `My Port` บนหัว Dock
- ในแท็บ `My Port` คลิกเลือกหุ้น แล้วสังเกตกราฟแท่งเทียนด้านซ้ายว่าสลับเป็นหุ้นตัวที่เลือกทันทีหรือไม่
- ตรวจสอบการย่อ/ขยาย Dock ด้วยปุ่ม `>`
```
</details>

<details>
<summary><b>Artifact 6: walkthrough.md V2.23.6 Right Dock Tab Switcher & Cleanup (คลิกเพื่อดูเนื้อหาดิบทั้งหมด)</b></summary>

```markdown
# Walkthrough - X-Chart Right Dock: Tab Switcher (Watchlist & My Port) + Button Cleanup

เรียบร้อยตามแผนบรีฟ! ทำการปรับแต่งส่วนหัวและโครงสร้างของ Dock ด้านขวาในหน้าหลัก X-Chart เพื่อขจัดความสับสนและเพิ่มความสะดวกในการสลับดูหุ้นพอร์ตจริง:

---

## 💎 สรุปการปรับแต่งที่สำเร็จ

### 1. ลบปุ่ม Sparkles (ดาว) และ Refresh (รีเฟรช)
- ลบปุ่มไอคอนรูปดาว (`Sparkles` - Reset TV 87 list) และปุ่มลูกศรหมุน (`RefreshCw`) ออกจาก Header ของ Dock เพื่อลดความสับสนและคลีนสายตาตามคำขอ
- ย้ายปุ่ม Add/Import หุ้น (`Plus` / `ListPlus`) ไปไว้ด้านขวาของ Header เพื่อความกระชับ

### 2. เพิ่ม Twin-Pill Tab Switcher บนหัว Dock
- เพิ่มปุ่มสลับแท็บแบบ Twin-Pill ใน Header ของ Dock:
  - **แท็บ `Watchlist (87)`**: รายการหุ้นตาม TradingView List ทั้งหมด 87 ตัว พร้อมระบบ Search & Filter เดิม
  - **แท็บ `My Port (N)`**: แสดงจำนวนหุ้นที่ถือจริงในพอร์ต (เช่น `My Port (14)`)
- บันทึกสถานะแท็บที่เลือกลง `localStorage` (`xchart_dock_tab`) เพื่อให้เปิดกลับมาเจอแท็บเดิมเสมอ
- ปรับแถบด้านข้างตอนย่อ Dock (Collapsed Sidebar) ให้แสดงป้ายชื่อตามแท็บที่เลือก (`Watchlist` หรือ `My Port`)

### 3. เชื่อมต่อแท็บ `My Port` เข้ากับ Dock เต็มผืน
- ใน MyPortWatchlist.tsx:
  - ปรับปรุงเลย์เอาต์ให้ยืดเต็มความสูง (`w-full h-full flex flex-col`) เข้ากับ Dock ด้านขวาอย่างไร้รอยต่อ
  - เชื่อมต่อการจัดรูปแบบราคาและมูลค่าให้ตอบสนอง Currency Toggle (THB / USD) อัตโนมัติ
  - ซ่อนปุ่ม Add หุ้นเมื่ออยู่แท็บ `My Port` (เนื่องจากเป็นรายการหุ้นพอร์ตจริงที่ถืออยู่)
  - **คลิกเปลี่ยนกราฟทันที**: เมื่อคลิกแถวหุ้นตัวใดในแท็บ `My Port` กราฟ Candlestick ด้านซ้ายจะสลับไปที่หุ้นตัวนั้นทันที

---

## 🚀 สถานะการ Deploy ขึ้นระบบจริง

1. **Build**: `vite build` สำเร็จ 100% ใน 18.42s ไร้ Type Error
2. **Cache Buster**: อัปเดตเวอร์ชันแคชเป็น `v=20260913_124053`
3. **VPS Sync**: ซิงก์ไฟล์ขึ้น `185.250.38.247:/root/stock-portfolio/dist/` เรียบร้อย
4. **PM2 Reload**: รีสตาร์ท `stock-api` บน VPS เรียบร้อย
5. **Git Push**: Commit `40c1829` ดันขึ้น GitHub `master` เรียบร้อยแล้ว
```
</details>

---

## ⏱️ Timeline & Debugging Log

### Update 1 — 10:10 (V2.23.0 Initial Shipped)
- ✅ สร้างและประกอบคอมโพเนนต์ M1 Finance ทั้งหมด (`MyPortTab`, `MyPortKPIHeader`, `MyPortM1Pie`, `MyPortTableView`, `MyPortBentoGridView`, `MyPortHoldingDrawer`, `types.ts`)
- ✅ Build ผ่าน 100%, Bump cache, Deploy ขึ้น VPS และ Push Master

### Update 2 — 10:18 (V2.23.1 English Pro Standard)
- ✅ แปลง Label, Subtitle, Tooltip, Filter chips และ Action ทั้งหมดเป็นภาษาอังกฤษมาตรฐานสากล
- ✅ Deploy ขึ้น VPS และ Push Master สำเร็จ

### Update 3 — 10:30 (V2.23.2 UI Elegance & Clarity Refinement)
- ✅ มหาเทพ 👁️‍🗨️ วินิจฉัยจุดรกรุงรังและวางแผนผ่าตัดความหนาแน่นทางสายตา
- ✅ มารบูรพา 🔥 เข้าลุยรีแฟกเตอร์:
  - ยุบ KPI Header 5 กล่องเป็น Bloomberg Single-row Inline Strip
  - ขยายวง Donut เพิ่มรัศมีและใส่ Ambient Radial Glow
  - ลดตารางจาก 8 เหลือ 6 คอลัมน์ ย้ายข้อมูลลึกไป Drawer
  - ปรับสัดส่วน Layout ให้ตารางมีพื้นที่มากขึ้น
- ✅ Deploy ขึ้น VPS และ Push Master สำเร็จ

### Update 4 — 10:33 (V2.23.3 Bento Polish & Donut Segmented Bar)
- ✅ ปราบโรค Box-in-a-box ใน `MyPortBentoGridView.tsx` จัด Typography เรียบหรู
- ✅ เพิ่ม Segmented Linear Allocation Bar ใต้ Donut Wheel ใน `MyPortM1Pie.tsx`
- ✅ Deploy ขึ้น VPS และ Push Master สำเร็จ

### Update 5 — 11:16 (V2.23.4 Currency Connection & Portfolio Switching Fix)
- ✅ แก้ไข Bug สำคัญใน `useHoldings.ts`: เพิ่มตัวกรอง `t.portfolio_id === activePortfolioId` ป้องกันธุรกรรมจากพอร์ตอื่นรั่วไหลปนกัน
- ✅ เชื่อมโยง Global Currency Toggle (`$ USD` / `฿ THB`) จาก `useUiStore` เข้ากับทุกคอมโพเนนต์ใน MyPort:
  - เพิ่มปุ่มสลับ Currency บน `MyPortKPIHeader.tsx`
  - ทำฟังก์ชันจัดรูปแบบ `formatCurrencyVal` และ `formatSecondaryVal` ใน `types.ts`
  - อัปเดต Donut Center Hub, Slices Table, Bento Cards, และ Slide-over Drawer ให้ตอบสนองการสลับ Currency แบบ Real-time ทันที
- ✅ Build ผ่าน 100%, Deploy ขึ้น VPS และ Push Master สำเร็จ

### Update 6 — 12:14 (V2.23.5 2/5 Layout Expansion, Price Dual Currency & Typography Hierarchy)
- ✅ ขยายสัดส่วน Allocation Wheel ฝั่งซ้ายเป็น 2/5 (40%) ของหน้าจอ พร้อมขยายรัศมี Donut Wheel (`innerRadius={114}`, `outerRadius={166}`) และปรับความสูงเป็น `h-[370px] xl:h-[390px]` เพิ่มความโอ่อ่าให้ Center Hub
- ✅ เชื่อมโยงระบบ Currency เข้ากับคอลัมน์ Price/24h ในรูปแบบ Dual-layer: แสดงราคาหุ้นตามสกุลเงินหลัก (`formatPriceVal`) พร้อมราคารองและ 24h Change ในบรรทัดเดียว
- ✅ ปรับ Typography Hierarchy ใน Slices Table: คงความหนา `font-bold` เฉพาะชื่อย่อหุ้น (`slice.symbol`) เพื่อเป็นสมอยึดสายตา และปลดความหนาของตัวเลขทั้งหมด (Weight, Price, Holding Value, Total P&L) เป็น `font-normal text-slate-200` ลดความล้าของสายตา
- ✅ เชื่อมโยงการเปิดกราฟแท่งเทียนผ่าน `addTab({ type: 'STOCK', ... })` ให้ถูกต้องตาม Store Interface
- ✅ Hotfix: คืนชีพ `import clsx from 'clsx'` ใน `MyPortKPIHeader.tsx` ป้องกันหน้าจอโมดูลแครช
- ✅ Build ผ่าน 100%, Cache Buster `v=20260913_121913`, Deploy ขึ้น VPS และ Push Master สำเร็จ

### Update 7 — 12:41 (V2.23.6 Right Dock Tab Switcher & Header Cleanup)
- ✅ ลบปุ่มรูปดาว (`Sparkles` - Reset TV 87 list) และปุ่มหมุนรีเฟรช (`RefreshCw`) ออกจาก Header ของ Watchlist Dock ทางขวาเพื่อขจัดความสับสนและคลีนพื้นที่
- ✅ เพิ่ม Twin-Pill Segmented Tab Switcher บนหัว Dock:
  - แท็บ `Watchlist (87)`: ดูรายการ TradingView หุ้น 87 ตัว พร้อมระบบ Search & Category เดิม
  - แท็บ `My Port (N)`: แสดงจำนวนหุ้นจริงที่ถือครองในพอร์ต (`useHoldings()`)
- ✅ บันทึกสถานะแท็บ Dock ลง `localStorage` (`xchart_dock_tab`) เพื่อจดจำแท็บที่เลือกไว้ตลอดเวลา
- ✅ ซ่อนปุ่ม Add หุ้นเมื่อสลับมาแท็บ `My Port` (เนื่องจากเป็นรายการที่สะท้อนจากบัญชีพอร์ตจริง)
- ✅ ปรับปรุง `MyPortWatchlist.tsx` ให้ยืดเต็มหน้า Dock (`w-full h-full flex-1`), รองรับ Dual-Currency Toggle, และเชื่อมโยงการคลิกแถวหุ้นให้กราฟ Candlestick หน้าหลักสลับไปที่หุ้นตัวนั้นทันที
- ✅ Build ผ่าน 100% ใน 18.42s, Bump cache เป็น `v=20260913_124053`, Deploy ขึ้น VPS (`185.250.38.247`), และ Commit (`40c1829`) Push ขึ้น GitHub Master เรียบร้อย

---

## 🔗 GBRAIN Backlinks

### depends_on
- **2026-09-12 12:19** | [X-Chart Trendline, Price Range Ruler, VPVR, VPS Cloud Sync, and My Port Tab Integration](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.20.0_%5Bimpl%5D_ui_xchart-trendline-vpvr-ruler-cloudsync-and-myport-tab.md) -- การสร้างแท็บ My Port ดั้งเดิมบน X-Chart Multi-tab
- **2026-09-12 12:19** | [X-Chart Anchored VWAP Custom Marker, In-Canvas Legend Dark Pills, and VPVR Config Integration](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.21.0_%5Bimpl%5D_ui_xchart-anchored-vwap-custom-marker-and-legend-overlay-pill.md) -- มาตรฐาน Dark Pills UI และ In-Canvas Legends
- **2026-09-13 08:27** | [UI: X-Chart 'My Port' M1 Finance Interactive Canvas & Slices Revamp (Design Spec)](file:///c:/My%20Claw/MyProjects/Quick%20Save/Active/My%20Stock%20Portfolio/V2.22.2_%5Bdesign%5D_ui_xchart-myport-m1-finance-interactive-canvas.md) -- สเปคดีไซน์ตั้งต้นของโมเดล Pies & Slices

### enables
- **Next Horizon** | Automated Portfolio Rebalancing Executor (คำนวณและสร้างคำสั่งซื้อขายปรับสมดุลพอร์ตตาม Drift)
- **Next Horizon** | Multi-Portfolio Aggregate Pie (รวมสัดส่วนทุกพอร์ตแสดงใน Donut วงเดียว)
- **Next Horizon** | Historical Allocation Slider (ย้อนดูสัดส่วนพอร์ตในอดีตย้อนหลัง 1W / 1M / 1Y)

### related_to
- **2026-09-13 09:58** | [Universal Cloud Settings Sync LED Indicator and Auto-Save Status](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.22.1_%5Bimpl%5D_ui_universal-cloud-settings-sync-led-indicator.md) -- ระบบ Cloud Settings Sync บน Header
- **2026-09-13 09:58** | [Universal Cloud Settings Sync Across Devices & Anchored VWAP Interactive Drag-and-Drop](file:///c:/My%20Claw/MyProjects/Quick%20Save/Complete/My%20Stock%20Portfolio/V2.22.0_%5Bimpl%5D_ui_universal-cloud-settings-sync-and-anchored-vwap-drag.md) -- สถาปัตยกรรม Settings Store และ Hook การซิงก์ข้อมูล
