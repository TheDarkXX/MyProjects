---
version: "4.6.0"
type: infra
status: deepfreeze
outcome: deferred
date: 2026-04-13
brain_task_id: ~
source: dev-agent
tags: [agent, core, infra, teardown]
conversation: "c32a43b1-b812-4d46-ab43-bd30b8b51011"
conversation_title: "Hermes Agent Evaluation & AG Skills Engine Design"
related_plans_same_conversation:
  - "V4.5.0_[impl]_ag-skills-engine-and-openclaw-teardown.md"
related:
  - discord-bot/lib/ai.js
  - discord-bot/scripts/cron-ai-monitor.js
summary: >
  Split from V4.5.0: The teardown of the legacy OpenClaw gateway on port 18790 was deferred. 
  This plan captures the required steps to fully purge OpenClaw from the VPS once we decide to clean it up.
---

# 🧊 Icebox Plan: OpenClaw Teardown

This plan was originally Phase 1 of the `V4.5.0` implementation but was deferred by user request on 2026-04-13. The codebase still has references to the legacy OpenClaw runner that need to be cleaned up.

## 2. Phase 1: OpenClaw Teardown Plan

Since `openclaw-runner.mjs` is useless in our current architecture, we will purge it to keep the VPS lean.

### Tasks:
- [ ] SSH into VPS.
- [ ] Stop and delete `openclaw-runner` from PM2: `pm2 delete openclaw-runner`
- [ ] In `discord-bot/lib/ai.js`: Remove `callOpenClaw()` function and rewrite fallbacks to use `callAIDirect()`.
- [ ] In `discord-bot/scripts/cron-ai-monitor.js`: Remove port 18790 from health checks.
- [ ] *Optional:* Clean up `~/.openclaw` directory unless needed for historical config reference.

*(Note: `kie-proxy.js` on port 18800 must strictly remain untouched; it is independent of OpenClaw.)*
